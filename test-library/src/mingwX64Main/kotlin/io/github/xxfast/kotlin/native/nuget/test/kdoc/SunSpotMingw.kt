package io.github.xxfast.kotlin.native.nuget.test.kdoc

// ADR-150 fixture: the mingwX64 actual for `SunSpot.kt`, deliberately bare of KDoc.

actual class SunSpot {
  actual fun sunbeam(): String = "warm on mingw"
}

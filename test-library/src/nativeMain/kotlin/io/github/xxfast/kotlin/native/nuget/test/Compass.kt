package io.github.xxfast.kotlin.native.nuget.test

/**
 * The undotted half of issue #233's fixture pair. It sits beside `Compass.native.kt` to pin that a
 * platform-suffixed neighbour does not merge into this holder (ADR-007 amendment).
 */
fun compassSize(): Int = 32

# Architecture Decision Records

> **Note on outdated references.** ADRs are point-in-time records and are kept as written. File paths and identifiers in the ADRs below may predate the July 2026 rename that reserved "sample" for the demo submodule: `sample-library` → `test-library`, `sample-app/*` (tests flattened to root) → `IntegrationTests` / `GeneratedBindingsCheck`, `sample-dependency` → `TestDependency`, `nuget-metadata-reader` → `NugetMetadataReader`, NuGet ids `SampleLibrary` → `TestLibrary` and `SampleDependency` → `TestDependency`, Kotlin package `...nuget.sample` → `...nuget.test`, native `baseName` `sample` → `test`, and C# namespaces `Sample.*` → `Test.*` (reverse fixtures) and `SampleApp.Tests` → `IntegrationTests`.

- [001](001-csharp-codegen-in-consumer.md) — Moved ClangSharp from Gradle to .targets in NuGet (later replaced by KSP)
- [002](002-nullable-two-call-pattern.md) — `_has_value` + `_value` functions → C# `T?`
- [003](003-memory-management-across-bridge.md) — StableRef pins Kotlin objects; C# disposes; each GC manages its own side
- [004](004-cir-intermediate-representation.md) — Type-safe C# AST (aligned with Swift Export's SIR)
- [005](005-object-return-semantics.md) — New wrapper per access; identity not preserved (matches ObjC/Swift)
- [006](006-enum-mapping.md) — Ordinal-backed C# enum + extension methods for properties
- [007](007-top-level-function-class-naming.md) — File name as class name; `Kt` suffix only on conflict
- [008](008-data-class-mapping.md) — Regular class + delegate to Kotlin; explicit `Copy()` instead of C# record
- [009](009-sealed-class-mapping.md) — Abstract base + nested sealed subclasses with runtime discriminator
- [010](010-generics-mapping.md) — Type-erased bridge + generic C# class
- [011](011-collection-type-mapping.md) — Eager copy with interface return types mirroring Kotlin mutability
- [012](012-lambda-function-type-mapping.md) — Opaque handle for Kotlin→C#; function pointer deferred to Phase 5; 2026-09-22 amendment: a nullable type argument is spelled (`KotlinAction<string?>`) and `null` crosses the returned-lambda boundary in both directions instead of erasing or crashing the host
- [013](013-extension-property-mapping.md) — `GetXxx()`/`SetXxx()` extension methods for Kotlin extension properties
- [014](014-value-class-mapping.md) — `readonly record struct` with unwrapped bridge for Kotlin value classes
- [015](015-generic-type-constraint-mapping.md) — C# `where` clauses from Kotlin upper bounds; `CirTypeParameter` model
- [016](016-generic-variance-mapping.md) — `out`/`in` variance on C# interfaces only; dropped on classes (C# restriction)
- [017](017-inline-function-mapping.md) — Inline functions treated as regular functions; existing pipeline handles them
- [018](018-type-alias-mapping.md) — Transparent expansion to underlying type; aliases erased (matches all interop targets)
- [019](019-suspend-function-mapping.md) — Callback-based bridge with TaskCompletionSource; suspend → Task/async; coroutine launched on Dispatchers.Default
- [020](020-suspend-lambda-mapping.md) — `KotlinSuspendFunc<R>` / `KotlinSuspendAction` with `InvokeAsync()` for suspend lambda properties
- [021](021-structured-concurrency.md) — Scope-per-class with `SupervisorJob`; `Dispose()` cancels in-flight coroutines; `ObjectDisposedException` guard
- [022](022-cancellation-token-support.md) — `CancellationToken` parameter on async methods; Job StableRef returned from export; `nuget_job_cancel` shared export
- [023](023-exception-propagation.md) — `KotlinException.KotlinType` property; `Pair<String,String>` error bridge; `nuget_error_type`/`nuget_error_message` exports
- [024](024-sync-exception-propagation.md) — Error out-parameter for sync functions; wrap-all (no `@Throws` required); mirrors ObjC `NSError**` ABI
- [025](025-async-disposable.md) — `IAsyncDisposable` + `DisposeAsync()` for graceful drain; `nuget_scope_drain` export joins all child jobs before releasing handles
- [026](026-flow-mapping.md) — `Flow<T>` → `KotlinFlow<T>` (`IAsyncEnumerable<T>`); Channel-buffered push→pull bridge; three-callback ABI (`onNext`, `onComplete`, `onError`)
- [027](027-stacktrace-propagation.md) — `KotlinException.KotlinStackTrace` property; `Triple<String,String,String>` error bridge; `nuget_error_stacktrace` export; `ToString()` override appends Kotlin trace
- [028](028-exception-cause-chain.md) — `e.cause` → `KotlinException.InnerException`; recursive `NugetError` data class error bridge (replaces `Triple`); `nuget_error_cause_*` exports; each cause a full `KotlinException`
- [029](029-exception-type-mapping.md) — `IKotlinException` interface; 8 Kotlin stdlib exceptions mapped to .NET analogs (e.g. `IllegalArgumentException` → `KotlinArgumentException : ArgumentException`); `KotlinException` fallback for unmapped types; `BuildMapped` switch in generated helper
- [030](030-property-exception-propagation.md) — ADR-024 error-out-parameter extended to property getter/setter exports; both callable forms of nullable two-call pattern wrapped independently
- [031](031-constructor-exception-propagation.md) — Constructor export returns `COpaquePointer?` (null on error) + `errorOut`; C# constructor checks error before assigning `_handle`; data class `copy()` receives same treatment
- [032](032-generic-constructor-exception-propagation.md) — ADR-031 error-out applied to all 14 generic `create_*`/`create_object` exports; C# error check centralized in `NugetMarshal.CreateBox<T>` (unconstrained) + per-constructor for constrained `Create_object`
- [033](033-value-class-constructor-exception-propagation.md) — Value-class ctor exceptions via private `CreateChecked` helper called from `: this(...)` (keeps `new Email(...)`, no static-factory redesign); primitive underlying uses ADR-024 dummy-return pattern
- [034](034-secondary-constructor-exceptions.md) — Export all constructors via `getConstructors()`; primary keeps `_create`, secondaries get `_create_N`; ADR-031 error-out per constructor; fail-fast KSP error on C# signature collision
- [035](035-value-class-primary-constructor-validation.md) — Value-class primary `init` enforced by switching to a hand-written `record struct` where every ctor has a validating body (also removes ADR-033's `: this(...)` delegation); get-only property prevents `with`/initializer bypass; reference-underlying deferred
- [036](036-reverse-interop-mechanism.md) — C#→Kotlin reverse interop via delegate + `GCHandle.Alloc` + `Marshal.GetFunctionPointerForDelegate`; consistent with existing Flow/async callback pattern; `[UnmanagedCallersOnly]` and vtable interface bridging deferred; 2026-09-22 amendment: a nullable lambda *type* keeps binding (with `ArgumentNullException` on a null delegate), a nullable *payload or return* is a named skip instead of a build abort or an uncatchable crash
- [037](037-stored-callbacks.md) — Stored callbacks (event handlers/observers) via `IDisposable` subscription handle; `add*`/`remove*` pair detection; bridge-lambda registry on Kotlin side; no new Kotlin→C# mechanism needed; 2026-09-22 amendment: a nullable payload refuses both halves of the pair together, since the bridge lambda strips the nullability and neither half's generated Kotlin compiled
- [038](038-aot-compilation.md) — NativeAOT compatibility; generics reflection path (`GetField`/`Activator`) is the concrete trimmer blocker; `CSharpProfile` makes the dialect (`DllImport`→`LibraryImport`, generic dispatch) a generation-time choice; deferred
- [039](039-interface-bridging.md) — C# implementing Kotlin interfaces via N flat function pointers per method; `add*/remove*` pair detection (same as ADR-037); Kotlin bridge generates anonymous `object : Interface`; same `IListener` for both directions; `IDisposable` subscription
- [040](040-interface-return-type-mapping.md) – Kotlin returning interface-typed values to C#; concrete `sealed class Foo : IFoo` per interface; Kotlin interface-dispatch exports (`asStableRef<Foo>().get()`); `IPet?` return type. **Revalidated 2026-07** against ADR-062/063/064/066: the defect is a **verified silent drop** (Tier 1 probe: no warning, no member, clean build), not the broken `new Pet(handle)` emit the ADR originally described; the work lands in `forward/` (new `BridgeType` + planner/projection/emitter branches), not in `cir/CirClassTranslator.kt`; the "`_handle` via reflection" parameter claim was wrong (the plan route emits direct `pet._handle`), so handle extraction from an `IFoo` is now an **open sub-decision** (an `internal` abstract member on `IFoo` is a **verified** CS0535 break for consumer implementers; a *default* one compiles); and whether the backing class is generated proactively or **reachability-driven** per ADR-066 is the second open question
- [041](041-kotlin-to-csharp-call-mechanism.md) — Phase 8 Kotlin → managed C# calls via `[UnmanagedCallersOnly]` thunks registered at startup from `[ModuleInitializer]`; Kotlin stubs `requireNotNull`-guard the pointer table and fail fast if unregistered; standalone Kotlin hosts requiring `hostfxr` CLR hosting explicitly out of scope for v1
- [042](042-assembly-metadata-extraction.md) — Phase 8 metadata extraction: `dotnet restore` (synthetic `.csproj` → `project.assets.json`) for resolution + bundled dotnet tool using `System.Reflection.Metadata.MetadataReader` to read the ECMA-335 public API without loading the assembly; pure-JVM both halves deferred as Future Improvement
- [043](043-bridgeable-subset-boundary.md) — Phase 8 C-ABI-bridgeable subset: overload sets, `ref struct`/`Span<T>`, open generics, `dynamic`, default interface methods structurally excluded; v1 auto-mappable subset only + per-member fail-fast diagnostics naming each skipped member; Kotlin subclassing a C# class out of scope, implementing a C# interface in scope
- [044](044-nuget-dependency-dsl.md) — Phase 8 NuGet dependency DSL: `dependency()` + `bind {}` opt-in inside `nuget { dependencies { } }`; absent `bind {}` = resolve only; `packageName`/`include`/`exclude`/`alias` in `bind`; downstream data contract for `nugetGen`/`nugetExtractApi` tasks
- [045](045-nuget-resolution-pipeline.md) — Phase 8 resolution pipeline: two-task split `nugetGen` (pure csproj generation, cacheable) + `nugetRestore` (`dotnet restore`, `@DisableCachingByDefault`); `net8.0` TFM floor; conditional `<RestoreSources>`; `obj/project.assets.json` as inter-task manifest; `nugetImport` umbrella task; config-cache via `ExecOperations`
- [046](046-reverse-ir-model-and-json-contract.md) — Phase 8 Reverse IR: `RirFile`/`RirAssembly`/`RirNamespace`/`RirType`/`RirMethod`/`RirProperty`/`RirTypeRef` sealed data classes in `nuget-plugin/` module + `kotlinx.serialization` JSON with `"kind"` discriminator; `parseReverseIr()` + `deriveDllPaths()` pure functions; `NugetExtractApiTask` inputs/outputs + plugin wiring; `nuget-metadata-reader/` C# `MetadataReader` project; five-slice TDD breakdown
- [047](047-per-package-namespace-filters-at-reader-cli.md) — Phase 8 per-package namespace filters: inline stateful `--include`/`--exclude` flags adjacent to each `--package` triple (vs JSON manifest); fixes flattening bug where Package A's filters were applied to Package B's assemblies; `metadataReaderCommand` signature changes from `List<String>` to `Map<String, List<String>>` for includes/excludes; `CliArgs.Parse` in Program.cs becomes stateful
- [048](048-kotlin-stub-generation-from-reverse-ir.md) — Phase 8 Kotlin stub generation: `nugetGenerateBindings` task; `object`-per-type stubs for C# static classes; two-file split (`{TypeName}Bindings.kt` + `{TypeName}.kt`); generated sources into `build/nuget-interop/kotlin/nativeMain/` wired into `nativeMain` source set; `expect`/`actual` `freeManagedString` helper for string deallocation; registration export naming convention `nuget_{ns_snake}_{type_snake}_register`; v1 type-mapping table (RirTypeRef → Kotlin type)
- [049](049-csharp-registration-shim-generation.md) — Phase 8 C#-side registration shim generation: new sibling `nugetGenerateShims` task emitting `{TypeName}Registration.cs` per bound `RirClass`; corrected inverse type-mapping table (`IntPtr` for strings, `byte`/`ushort` narrowing for non-blittable `bool`/`char`); `[UnmanagedCallersOnly]` thunks + `[ModuleInitializer]` calling the Kotlin registration export via `sharedLib.baseName`-targeted `[DllImport]`; catch-and-sentinel exception handling per ADR-041; shim delivery merged into the existing packed-nupkg `contentFiles/cs/any/` convention from ADR-001 (packaging wiring deferred to the "Phase goal" end-to-end ROADMAP item); shared `bridgeableStaticMethods()` extraction to prevent Kotlin/C# parameter-order drift
- [050](050-end-to-end-packaging-integration.md) — Phase 8 end-to-end packaging integration (phase goal): sample package selection (`MimeMapping`; `Newtonsoft.Json` blocked by overload sets), real `.nupkg` consumption via the ADR-050 local feed, reverse shims merged into `contentFiles/cs/any/`, bound packages pinned at exact resolved versions in `<dependencies>`, single-`afterEvaluate` bridging in the plugin
- [051](051-csharp-objects-as-opaque-handles.md) — Phase 9 C# objects as opaque handles in Kotlin: `GCHandleType.Normal` crossing as `GCHandle.ToIntPtr` → `COpaquePointer` (mirror of ADR-003's `StableRef`); new wrapper + new handle per crossing, no identity caching, Kotlin-default equality (mirror of ADR-005); `Cleaner`-primary lifetime + explicit idempotent `close()` (`kotlin.AutoCloseable`) — deliberate inversion of forward `IDisposable`-first; one shared `nuget_runtime_register`/`FreeGcHandle_Thunk` free export (no per-type `_free` slot, ADR-048 contract unchanged); `RirObjectHandleType(namespace, name)` + `RirClass.isStatic` RIR extensions; object returns `Foo?` (`IntPtr.Zero` = null), parameters non-null until `NullableAttribute` mapping lands
- [052](052-csharp-instance-constructors-in-kotlin.md) — Phase 9 C# instance constructors in Kotlin (Proposed): `new Foo(...)` → a Kotlin secondary `constructor(...)` on the ADR-051 wrapper, delegating via `: this(construct(...))` to a file-private helper that runs a non-null handle-returning `Ctor_Thunk` (constructor → constructor, matching Kotlin-consuming-Java / ObjC-import; factory and companion-`invoke` rejected as non-idiomatic / fake-constructor); constructor return is non-null `Foo` (never `Foo?`, unlike ADR-051 factories); `RirConstructor` + `RirClass.constructors` RIR node (mirror of forward `CirConstructor`/ADR-034); metadata reader narrows the `.ctor` `SpecialName` skip to admit one public instance ctor, ctor pointer registered first; multiple public ctors are an overload set (skip + diagnose, deferred to line 156); fixture: make `Sample.Text.Template`'s ctor public
- [053](053-nullable-reference-types-in-kotlin.md) – Phase 9 C# nullable reference types in Kotlin (Accepted): `NullableAttribute` (0=oblivious/1=non-null/2=nullable) + `NullableContextAttribute` decoded from ECMA-335 via `MetadataReader`, fallback chain member → method → type → oblivious; **oblivious binds as non-null `T`** (platform types `T!` are unspellable in generated Kotlin source, so the permissive middle every other interop layer uses is unavailable; non-null + a fail-fast bridge guard is its closest expressible approximation, matches C#'s own no-warning-on-oblivious behaviour, and preserves today's non-null string bindings), with an `info_oblivious_nullability` diagnostic (once per legacy assembly, per member for a `#nullable disable` island); nullability encoded as a `nullable` flag on the reference-typed `RirTypeRef` variants (not on the parameter/property carrier, which cannot express `List<string?>` when generics land); corrects ADR-051 (object returns are `Foo?` only when annotated) and unblocks the settable handle property (`var bar: Foo`), since a C# property carries one annotation shared by getter and setter; prerequisite fix: parameter names must key on `Parameter.SequenceNumber` (a nullable return adds a `Sequence == 0` `Param` row and shifts every positional lookup); `Nullable<T>` value types deferred (a closed generic struct, needs its own wire format)
- [054](054-reverse-bridge-registration-observability.md) – Tooling: reverse-bridge registration observability (Proposed): the ADR-041 function-pointer table becomes observable via three parts, deliberately not one. (1) An always-on generated `NugetRegistry` bakes the expected registration-export set in at generation time and records each one as it lands, so the `requireNotNull` guard's message becomes *computed*: "0 of 7 registrations have fired, nothing registered at all, the shims are not compiled into the host assembly (usually a stale `obj/project.assets.json`)" versus "6 of 7 fired, only `Sample.Text.Template` is missing". Those are the two sentences nobody could get an answer to during ADR-053. (2) An always-on registration-time contract self-check: every register export gains two leading scalars (`slotCount: Int`, `contractHash: Long`, both from one shared `RirBridging.kt` function) and its pointer params become nullable; Kotlin refuses to store *any* pointer on mismatch, turning stale-shim/fresh-dylib arity or type drift from silent memory corruption into a named "one of these is stale" error (an amendment to ADR-048's registration contract). (3) An opt-in `NUGET_INTEROP_TRACE=1` registration trace, stderr by default with a `NUGET_INTEROP_TRACEFILE` redirect (the `LD_DEBUG`/`LD_DEBUG_OUTPUT` and `DOTNET_HOST_TRACE`/`DOTNET_HOST_TRACEFILE` shape; xunit 2.9.3 does not capture Console, and a crashed host loses it). Registration granularity only, matching JNI's `-verbose:jni`; per-call tracing, verbosity levels, subsystem categories and an `EventSource` provider all rejected for v1 (EventSource is C#-only, so the Kotlin half of the trace could not participate). Zero cost on the bridge call path
- [055](055-forward-abi-contract-check.md): Tooling (Accepted). Forward C# `CirDllImport` declarations and Kotlin `@CName` exports are generated independently, so collect a normalized wire-level signature from both and fail KSP generation on a missing or mismatched symbol/return/ordered parameter contract. This catches source drift before packaging without changing every forward export ABI or adding runtime overhead; the final Kotlin/Native ABI lowering remains explicitly validated by walking-skeleton integration tests.
- [056](056-csharp-structs-in-kotlin.md) — Phase 9 C# structs (value types) in Kotlin (Accepted): the ROADMAP's "blittable pass-by-value vs boxed handle" is a false dichotomy — **decompose the struct into its components and never put a struct on the wire**. Pass-by-value is not expressible here (verified: a hand-written `CStructVar` cannot appear in a `CFunction`'s `invoke`, "not a structure or too complex"; only a *cinterop-generated* one can, and ADR-048 generates plain Kotlin source, not a klib), and it covers strictly fewer structs anyway (verified: `[UnmanagedCallersOnly]` blittability is enforced at **runtime**, is **transitive**, and rejects `bool`/`char` with `InvalidProgramException` even nested in a struct — so `readonly record struct Person(string Name, int Age)` could never cross by value). The boxed `GCHandle` is correct but destroys value semantics (`close()`, `Cleaner`, reference equality on a `Point`), the exact trap ADR-014 rejected forward. So: a struct parameter expands to one ABI argument per component, a struct return to `void` + one **out-pointer per component** (verified end-to-end in the real Kotlin-dylib-in-.NET-host topology for `string`/`int`/`bool`/`char`/enum components, both directions), and the Kotlin surface is an immutable `data class` — value equality, `copy()`, no handle, no `close()`, zero registration slots. Partly a mirror of ADR-014 (unwrap-don't-box transfers exactly; arity generalises 1 → N, which is what forces out-pointers). v1 = Shape A structs (one public ctor whose parameters cover all stored state; components = ctor params, matched case-insensitively to properties); new `RirStruct`/`RirStructComponent`/`RirStructType` + `skipped_unsupported_struct`; **`contractHash` must expand struct components** or a struct gaining a field silently changes thunk arity under a stale shim (ADR-054); both generators must share one `abiArgs`/`abiOutArgs` expansion (one `RirParameter` is no longer one ABI argument). Fixes a verified latent bug: the reader currently emits every struct as `{"kind":"class"}`. Unblocks `Nullable<T>` (ADR-053 Decision 3) by fixing its wire format — `byte hasValue` + out-pointer, one crossing, one evaluation — without subsuming it
- [057](057-csharp-overload-sets-in-kotlin.md): Phase 9 C# overload sets in Kotlin (Accepted): preserve ordinary Kotlin method/constructor overloads; derive stable internal thunk/pointer identity from a canonical managed signature plus a checked SHA-256 digest; canonically sort constructor/static/instance/property registration slots instead of trusting MethodDef/declaration order (verified unstable under a scratch declaration-reorder probe); map overloads independently, then fail fast if distinct CLR signatures collapse to one Kotlin signature (ADR-034 mirror); multiple ADR-056 Shape A struct constructors use one unique state-covering constructor for data-class components while alternate constructors get bridge-backed out-pointer slots
- [058](058-csharp-shape-b-structs-in-kotlin.md): Phase 9 C# **Shape B** structs in Kotlin (Accepted): a struct with no state-covering constructor (public fields and/or settable auto-properties) binds by taking its **settable public state** as the component model and reconstructing with an **object initializer** (`new T { A = a, B = b }`). Nothing about ADR-056's wire changes: components in, out-pointers out, immutable `data class`, zero slots when memberless; the Kotlin generator needs no change at all, and the only new C# text is the `{ … }` braces at the two existing `new T(...)` reconstruction sites. Three verified spikes settle the three concerns ADR-056 deferred on. (1) *Field extraction*: an auto-property's backing field is `Private` + `[CompilerGenerated]` (so an accessibility filter alone excludes it), and `GetFields()` yields declaration order **interleaving** real fields with backing fields, which is what makes one unified component order recoverable in a single pass. Coverage is therefore *structural*, not a count: every non-static field must be either a settable public field or the backing field of a settable public auto-property, else the struct is skipped (a count-based rule silently drops state, and cannot order a manual property anyway). (2) *The `init` landmine is defused by the choice of syntax, not by detecting it*: an object initializer is exactly the context where an `init` accessor is callable, cross-assembly (verified), so `set` and `init` are both just "settable" and no `IsExternalInit` detection is needed. (3) *Order* is the real cost: FieldDef declaration order becomes the `data class` primary-constructor order, and a C# field reorder is source-compatible for C# callers but silently reorders positional Kotlin construction (alphabetical sorting rejected: it would make the Kotlin order permanently disagree with the C# declaration, trading a rare hazard for an always-armed one; the defence is generated KDoc plus named arguments). Also **amends ADR-056 Decision 3a rule 3** (a constructor parameter may now match a public *field*, not only a property) because otherwise the ordinary `struct { public int A; public int B; public S(int a, int b) }` falls to Shape B, its ctor becomes an ADR-057 alternate, and its Kotlin signature collides with the generated primary constructor, hard-failing the build; and **fixes a latent ADR-054 hole** (`signaturePart()` expands a struct *reference* to component types only, so swapping two same-typed components is invisible to the stale-shim contract check, which Shape B promotes from theoretical to plausible). Alternate constructors on Shape B structs deferred (same collision), manual non-auto settable properties deferred (coverage unprovable from metadata); nested-struct and class-typed components stay deferred
- [059](059-nested-struct-components-in-kotlin.md): Phase 9 **nested struct components** in Kotlin (Accepted): a struct-typed component is **flattened recursively, depth-first, pre-order**, so the flat argument list is the leaves of the component tree; the Kotlin surface **stays nested** (`val mother: Profile`, a `data class` inside a `data class`, with deep structural equality and composing `copy()`); the C# side **reconstructs recursively**, each struct using its own ADR-058 shape, so the four A/B nesting combinations need no rules of their own (all four verified round-tripping through a real C-ABI function pointer, at depth 3, with a `string` leaf inside the nested struct). The RIR keeps the struct **reference** and does **not** pre-flatten (verified: `RirStructComponent.type` is already a `RirTypeRef` admitting `RirStructType`, so **the JSON schema does not change at all**; pre-flattening would destroy the nesting the Kotlin surface needs and make `readName` a lie), and `signaturePart()` **already recurses**, so ADR-054's contract hash covers nesting with no code change. Termination is verified, not assumed: **CS0523** rejects direct, mutual, generic-mediated and `Nullable<T>`-mediated struct cycles (a cycle through a class/array/static field is legal C# but is not a component, and a handle component would be a wire *leaf* anyway), and the reader classifies by **fixed-point iteration** so it cannot recurse on a cyclic IL assembly the C# compiler never saw. The one genuinely new constraint, and the reason this is an ADR: **Kotlin/Native's `CFunction.invoke` accepts at most 22 arguments** (verified: 23 fails with "none of the following candidates is applicable", at the *call site*, mentioning neither arity nor structs), a ceiling nothing has been close to before and that flattening makes reachable with ordinary types (two 8-leaf structs in and one out is 24), so an over-arity member is **skipped with a `skipped_abi_arity_limit` diagnostic from the shared registrable filter**, never left for the Kotlin compiler to reject inside generated code. Also fixes a verified diagnostic bug: today a nested struct is skipped with a message naming the *wrong rule* (a Shape B auto-property rule for a Shape A struct; "not a bridgeable bound **class**" for a struct), because `ExtractStruct` discards the Shape A reason when it falls through to Shape B
- [060](060-adversarial-forward-fixture.md): Tooling — the adversarial forward fixture, one red seam + two green guards (Accepted; steps 1–3 shipped): the mirror of `Test.Household` is **not one artefact**, because the forward direction has three gates where the reverse has one (generated Kotlin compiled by `:test-library:compileKotlinMingwX64`; generated C# compiled *inside every consumer* via `contentFiles buildAction="Compile"`; and `ForwardAbiContract` at generation time). **Every defect cell would break a build rather than fail an assertion** if simply added to `test-library` — twelve emit invalid Kotlin (no `.nupkg`, nothing downstream runs), one emits `CS0111` inside `Interop.cs` (every consumer fails), five are valid-but-lying C# whose *shape* can only be asserted at a compile-time call site (`CS0029`/`CS1503`, hard errors regardless of warning settings), and one silently vanishes (`CS1061`); no cell's red is free — an earlier draft thought `Char` could be red at runtime, but it leaks `IntPtr` into the *public* signature (`string Tag(IntPtr)`), so it is a call-site defect too. Driving the **real** processor (KSP2's own `KotlinSymbolProcessing` entry point — **no kctfork**; 2.4s first run, then ~130ms) and the **real** konanc settled the design further than the alternatives describe: **all red lives in Tier 1** as strict-`@XFail` JUnit tests (an unexpected pass **fails the build**, so a fix cannot land leaving its marker behind), while Tiers 2 and 3 become **regression** tiers landing green *with* each fix. Consequently `scripts/verify.sh` stays green on a tree with all twelve defects and needs no change: the **expected-errors manifest is rejected** (inverting `GeneratedBindingsCheck`'s exit code would blind the gate to *unrelated* regressions — the kind it caught on day one — for the whole xfail period; a red CI is ignored, a lying one is believed), and **Tier 1 must not compile C#** (it would make `nuget-processor`'s unit tests need the .NET SDK, eroding MVP.md line 62's stated property that the forward direction needs none; a structural assertion on the generated `Interop.cs` carries the red just as well). Verified re-classifications: there is **no runtime-only class** (`Char` leaks `IntPtr` into the *public* signature — `string Tag(IntPtr)` — so it is a call-site defect); **cell 2** (`object` returning an object) is invalid Kotlin, not a leak; **cell 13** (`Char` return) is invalid Kotlin because `defaultValueFor("kotlin.Char")` is `"0"`; and **cell 21**'s Kotlin *does* compile (`@CName` accepts a Kotlin-class parameter), so its `CS0111` would brick every consumer — but its correct end state is ADR-034's fail-fast diagnostic, making it a permanent Tier 1 cell. The corpus therefore splits **by observability class**, not one-shape-per-fix: cells 1/8/9/12/14 land now, the K cells are quarantined in the harness until their fix (`Test.Household` was itself never red: it landed after ADR-053's fixes as regression hardening, which is the tension with MVP.md's "fixtures first" this ADR resolves). Corrects the seam-matrix axis from "declaration kind × type" to **(carrier code path) × (position) × (type facet)**, valued by (Kotlin outcome, C# outcome, observability class): position, not declaration kind, selects the path — `isMarkedNullable` is re-read only at property and top-level-return positions (four sites in the CIR translators, four more in the export generators), never at a class method return, and `parameterizedBy` appears **0 times**, so `.declaration` is the lossy step and every defect is a dropped facet nobody re-read. Verified both unverified reports: **BUG-010 is real** (all four reported failure modes reproduced) and **BUG-008 is real but mis-framed** (it is not a type bug but a *position* bug). Found four defects no report mentions, including that **the forward direction cannot export a top-level factory function returning a class** (`fun admit(name: String): Patient` → invalid Kotlin; every factory in the corpus is a companion, which routes through a different path that does use `StableRef`) and that **an `object`'s methods are not PascalCased** (`Clinic.greet` → C# `greet`, not `Greet`, while every class method is PascalCased — cell 25, added after the ADR was drafted). Twelve defects across nineteen cells (12 K + 1 B + 5 C + 1 N). MF-001 and MF-003 confirmed not fixture-expressible
- [061](061-method-return-marshalling.md): Forward — non-primitive and nullable return marshalling for class methods and extension functions (Accepted): route both method-return positions through the same property-getter cascade (object → `StableRef` carrier + `new T(handle)`; `List<T>` → object carrier materialized by the shared `NugetListNative` helpers, which need `needsListSupport` widened to also scan method/extension returns; nullable object → `IntPtr.Zero`-sentinel; nullable `String` → single-call `PtrToStringUTF8`), and resolve the one genuine decision — a nullable **primitive** method return cannot reuse ADR-002's two-call `hasValue`+`value` pattern because a method with side effects must not be invoked twice — with a **single-call out-parameter**: the export returns `Boolean` has-value and writes the value through a `valueOut: COpaquePointer?` (mirror of ADR-024's `errorOut` out-write), C# reads `Native_X(_handle, out int value, out IntPtr error)`. Boxing (the ObjC/Swift `KotlinInt` / JNI `Integer` precedent) considered and rejected for its `StableRef` alloc + second unbox crossing. Closes ROADMAP L61 return positions + ADR-060 cells 4/5/6. Load-bearing inferred (not spiked): `reinterpret<IntVar>().pointed.value` round-trips on the real mingwX64 toolchain — the walking-skeleton integration test must confirm before relying on it; `Boolean?`/`Char?` returns and nullable **parameters** deferred as separate items. Amended 2026-09-16: a nullable collection method/extension return (`List<T>?`/`Set<T>?`/`Map<K,V>?`), the one return shape the original matrix missed, now takes the nullable object-handle route verbatim (null pointer is the null, no `valueOut` involved)
- [063](063-forward-declaration-level-export-scoping.md): Forward, declaration-level export scoping (MF-001, Accepted): the processor bridges every public declaration (`resolver.getAllFiles()`, no filter) because there is no export set; `rootPackage` only renames the C# namespace (`mapPackageToNamespace`), it does not scope. Adds an explicit **package include/exclude filter in `publish {}`**: `include(vararg)`/`exclude(vararg)` on `NugetPublishConfig`, mirroring the reverse `bind { include/exclude }` (ADR-044/047) verb-for-verb and prefix predicate exactly (exclude wins; empty include = all; `pkg == p || pkg.startsWith("$p.")`). Threaded through the existing `rootPackage` path (three KSP options → three `NugetContext` fields, the third being `boundPackages`) and applied at the single `allDeclarations` choke point, so every downstream category is scoped for free. Default: an empty `include` falls back to `rootPackage` when set, else all packages, so `rootPackage` is now a scoping knob and not only a renaming one (a deliberate, documented behaviour change). A reverse-bound package (from a module's own `bind {}`) is always exported regardless of `include`/`exclude`, checked first in the predicate: `test-library`'s own forward/reverse round trip broke on the first cut of this feature when a bound package was dropped from scope. Rejected: a per-declaration annotation (opt-in contradicts GOALS #1 plug-and-play like the already-rejected `@ExperimentalNugetApi`; opt-out doesn't isolate unrelated same-module API), deferred as a future `@HiddenFromObjC`-style layer; a full reachability closure now (Alt 4, the MF-001/MF-003-unified framing) is rejected because MF-003 is cut from MVP and sequenced *after* forward diagnostics + the value-class-parameter fix, so building it now re-arms the constructs module isolation hides. This ADR **chooses the roots-are-packages framing** so MF-003 generalises the include set into seed roots rather than replacing it. Filter runs before the `ForwardCallablePlanner`, so an out-of-scope declaration is silently not bridged and never triggers the forward "unsupported" diagnostic. Prior art (all inferred): Kotlin framework `export()`/`transitiveExport` scopes at module granularity (the MF-003 analogue, dep-pull opt-in); ObjC/Swift export = all-public-leaves + opt-out `@HiddenFromObjC`; JS = opt-in `@JsExport`; reverse direction = package filter (verified in source)
- [064](064-forward-unsupported-declaration-diagnostics.md): Forward unsupported-declaration diagnostics (the mirror of ADR-043, Accepted): the forward direction has no written bridgeable subset and no coherent skip diagnostic, so meeting its ceiling emits invalid Kotlin, a lying C# API, or a silent drop, the inversion of the reverse direction, which names each skipped member and why. Adds a `ForwardDiagnostic`/`ForwardDiagnosticKind` model deliberately parallel to reverse `RirDiagnostic`/`RirDiagnosticKind` (same `SKIPPED_/INFO_/ERROR_` prefix-encodes-severity convention, ADR-057 fatal-vs-warn policy unchanged), but carrying a `KSNode` source location the reverse side structurally cannot (it reads ECMA-335 metadata) so the message lands on the author's own Kotlin, not on generated `CNameExports.kt`. Default is **skip + warn** (member absent, never an `IntPtr`/`"0"` fallback); the one fatal case stays the ADR-034 C# constructor-signature collision (cell 21). Two producers, one sink: the `ForwardCallablePlanner` already emits `ForwardCallableCatalogEntry.Skipped` with a `droppedFromCSharp`-flagged `ForwardPlanSkipReason` (verified) and `warnDroppedForwardCallables` already logs a generic string (verified), upgrade it to structured, per-kind messages, closing ROADMAP L78-79's silent `Map`/`Set`-input and nullable-Boolean-return skips; the **legacy routes** (`exports/`) are the real work: the cell-23 generic+suspend+inline+reified extension returning `Result<T>` (BUG-010) is currently classified as a `SpecializedProtocol` deferred to a legacy route that does not exist, so `addExtensionFunctionExports` renders raw `Function1`/`Result` (invalid Kotlin); the fix narrows it to a genuine drop (`SKIPPED_UNSUPPORTED_COMBINATION`) and makes the legacy emitter consult the drop set instead of emitting. **Sequencing prerequisite** for the reachability-closure export item (ROADMAP L14): module isolation accidentally hides cell 23 and the `CharSequence`-value-class today, and reachability would re-arm them as build breaks without this. No new test harness: ADR-060's Tier 1 diagnostic assertion mode already captures `logger.warn`/`error`; cell 23 is a permanent Tier 1 diagnostic cell. Deferred (each its own item): a structured/queryable report covering **both** directions (reverse has the same gap, ROADMAP L162), the value-class-inheritance product decision (L77), and an un-skip/remap escape hatch. External prior art (inferred): ObjC/Swift export uses opt-out `@HiddenFromObjC` and documents its ceiling rather than diagnosing per-declaration; Xamarin skips + MSBuild-warns with a `Metadata.xml` escape. KSPLogger's `(message, symbol: KSNode?)` signature verified from KSP source; the source-location *rendering* is inferred
- [065](065-stateflow-mapping.md): Forward — `StateFlow<T>` mapping (Accepted): the hot, conflated, always-current-value cousin of ADR-026's cold `Flow<T>`, mapped to a generated `KotlinStateFlow<T> : KotlinFlow<T>` that both is-a `IAsyncEnumerable<T>` (reusing the ADR-026 `_collect` export, three-callback triple, unbounded-`Channel<T>` bridge, cancellation and error propagation **verbatim**) and adds a synchronous `T Value { get; }` — mirroring Kotlin's own `StateFlow : Flow` subtyping, SKIE's `SkieSwiftStateFlow` (AsyncSequence + `.value`) and Rx.NET's `BehaviorSubject.Value`. The one new export shape is a per-member `_value` getter that boxes `stateFlow.value as Any` into a `StableRef` (byte-for-byte a single `onNext` emission) and is unwrapped C#-side by the already-shipped `NugetMarshal.FromHandle<T>` (primitives/strings unwrapped + box disposed; objects → `new T(handle)`, ADR-005 new-wrapper-per-access), so no new marshalling is written; no `errorOut` in v1 (a deliberate narrowing of ADR-030, since `StateFlow.value` cannot throw). Replay-1 and conflation fall out of `StateFlow.collect` on the Kotlin side with **zero** change to `KotlinFlowEnumerator<T>`; a StateFlow `await foreach` never completes on its own (hot/open) and must be bounded by cancellation/`break`. KSP detection adds a **separate** `STATE_FLOW_TYPES` set checked **before** `FLOW_TYPES` and keyed on the declared type's exact `qualifiedName` (not `isAssignableFrom`), so a `StateFlow` never falls through to the Flow branch and loses `.Value` (the task-flagged hazard; today a StateFlow is simply skipped, verified-in-repo). Rejected: plain property + `INotifyPropertyChanged` (always-running collector + UI-thread affinity; deferred opt-in adapter), bare `KotlinFlow<T>` dropping `.value` (loses StateFlow's defining feature), `IObservable<T>` (Rx.NET dependency, per ADR-026). v1: `StateFlow<T>` property + non-suspend-function return, `MutableStateFlow<T>` as a read-only `KotlinStateFlow<T>` view; primitives/`String`/objects. Deferred: settable `.Value` (reverse write), `SharedFlow<T>` (configurable replay, distinct), nullable `StateFlow<T?>`/`StateFlow<T>?`, StateFlow as parameter/generic-arg. Load-bearing inferred (not spiked): the `_value` export + `KotlinStateFlow` subclass compile and round-trip on the real mingwX64 toolchain — the walking-skeleton integration test must confirm. SKIE `.value` accessor inferred, not verified against SKIE source this session
- [066](066-forward-export-reachability-closure.md): Forward, export set as a **reachability closure from module roots** (MF-003, Accepted): `getAllFiles()` defines the export set by *file provenance*, so a type one Gradle module away is never translated, and the same missing type breaks in two different ways depending on position (verified in source): a plain return type hits the classifier's `declaration is not in the exported object-handle set` branch and the callable is dropped, while `Flow<T>` never consults that set at all and maps the element by **simple name** at two sites (the ADR originally counted five; three of those turned out to be an unrelated, still-open `List`/`Set` element-type gap, corrected post-implementation), emitting `KotlinFlow<TopStoriesState>` into `Interop.cs` for a C# type that was never generated (CS0246 in every consumer, NYTimes-KMP BUG-004). Takes ADR-063's own stated framing literally: roots stay the module's files, and **ADR-063's package predicate becomes the closure's admission boundary**, so no new DSL verb, and a models module under `rootPackage` is admitted with zero config (GOALS #1) while third-party packages outside it can never be swallowed. Closure follows returns, parameters, property types, type arguments of admitted carriers, sealed subclasses and primary-constructor parameters, terminating on a visited-set; supertypes, cross-module generics and cross-module suspend/Flow members are deferred. A reachable-but-unsupported type is a **named diagnostic + skip**, never a hard error and never a member-less opaque handle (ADR-064 policy, plus a new `SKIPPED_UNEXPORTED_DEPENDENCY_TYPE` whose hint names the exact `include(...)` line, and an aggregate `INFO_EXPORTED_FROM_DEPENDENCY` manifest line, deliberately once-per-run rather than per-member). Namespacing reuses `mapPackageToNamespace` unchanged, so an admitted dependency keeps its own package identity as a sub-namespace (Swift Export's `moduleName` convention) rather than folding into the exporting module's short name. Rejected: per-dependency `export(project(...))` opt-in (the ecosystem precedent from Kotlin framework/Swift export, but it costs the plug-and-play default; **explicitly out of scope**, deferred to its own item), an unbounded classpath closure (Kotlin's own `transitiveExport` is documented as not recommended for exactly this), and opaque-handle passthrough. **Spiked against a real klib on `kspKotlinMacosArm64`, not inferred**: cross-module resolution yields the full model (ctor params with nullability, props, funs, `getSealedSubclasses()`, enum entries, bounds), `getDeclarationsFromPackage` on a klib returns **empty** (so reachability is the only way in, not merely the nicest), `containingFile` is null (no source location in diagnostics), and a `@CName` export referencing a dependency type links into the `.dylib` with plain `implementation(project(...))`. Duplicate types across two published packages (both admitting one dependency type) are an **accepted known limitation**, the same hazard Kotlin/Native framework export has, tracked as its own ROADMAP item. Forces **two ADR-064 amendments, both in scope for this feature rather than split out**, both of which silently produce valid-compiling wrong output if followed literally: a cross-module `value class` reports `Modifier.INLINE` and **not** `Modifier.VALUE` (so it would export as a handle, valid-compiling and wrong), and every member of a `KOTLIN_LIB` declaration has `origin == KOTLIN_LIB`, so `SKIPPED_INHERITED_MEMBER`'s shipped `origin != Origin.KOTLIN` filter (`ForwardCallablePlanner.kt:261`/`:306`) would drop **every** member of an admitted type, while `CharSequence by value` delegation becomes indistinguishable from a declared member by both `origin` and `parentDeclaration` (the replacement rule is a supertype simple-name match, landing here with its own Tier 1 cell; the \`VALUE\`/\`INLINE\` fix touches three named sites). Load-bearing inferred: that a dependency-klib change invalidates the KSP task and re-generates (aggregating deps can't reference a null `containingFile`), which must be checked first with a two-build edit test
- [067](067-nullable-stateflow-mapping.md): Forward — nullable `StateFlow` mapping (Accepted): the two deferred nullable shapes from ADR-065, decided independently and composed. **Nullable element `StateFlow<T?>`** → `KotlinStateFlow<T?>`: Kotlin emits `IntPtr.Zero` for a null current value / collected item (the `_value` return widens to `COpaquePointer?` and both `_value` and `_collect` bodies gain an `if (v != null) StableRef.create(v) else null` guard, because `value as Any` cannot box null — **inferred**, no Kotlin toolchain in-session). **Reference** elements (`String?`, `Cat?`) reuse the shipped `FromHandle<T>` unchanged (null pointer → `default!` → null); **value** elements (`Int?`) need a new `Nullable<T>`-aware unwrap, because **spiked this session** `typeof(int?) != typeof(int)` so `FromHandle<int?>(nonZero)` throws `MissingMethodException` at the `Activator` fallback (only the null case works today, via the zero-guard's `default(int?)==null`). The `onNext` `isCancelled` byte already distinguishes a null element (`0`) from cancel (`1`), so the collect path needs no new callback. **Nullable member `StateFlow<T>?`** → `KotlinStateFlow<T>?`: a StateFlow does not cross as a single null-checkable handle (it is reconstructed C#-side from per-member exports), so a new per-member `_has_value` `Boolean` presence-probe (ADR-002 family; idempotent on a `val`) drives the getter to return `null` before any subscription, else construct the ADR-065 `KotlinStateFlow<T>` with a defensive `?.` in its bodies. Rejected: sentinel for null primitives (ADR-002's rejected option, no `String`/object generalization), by-value `{bool,T}` struct (ADR-002's rejected `@CStruct`), folding member-absence into a nullable `_value` (conflates "absent" with "present, current value null" — they co-occur in `StateFlow<T?>?`), an always-non-null stream that empties when absent (lies about nullability, GOALS #2). Purely additive — a nullable-element/-member StateFlow is currently skipped, never mis-rendered. `qualifiedElementCsType` must learn to emit `?`/`Nullable<T>` and thread the element's `isMarkedNullable`. v1: property + non-suspend return, reference and blittable-numeric value elements; deferred `Boolean?`/`Char?` elements (ADR-056 width), settable nullable `.Value`, nullable `SharedFlow`, nullable StateFlow as parameter/generic-arg. Load-bearing inferred (not spiked): the null-guarded Kotlin bodies + `COpaquePointer?`/`bool` exports compile and round-trip on the real toolchain — walking-skeleton integration test (ADR-055/060) must confirm
- [068](068-suspend-returning-stateflow.md): Forward, `suspend fun` returning `StateFlow<T>` maps to `Task<KotlinStateFlow<T>> XxxAsync()` (Proposed) — **corrects** ADR-065/026's deferred "treat as non-suspend returning StateFlow/Flow" wording. Keeping the outer suspend as `Task` (compose ADR-019 over ADR-065) is the honest mapping: SKIE, Swift/ObjC export, JS and Java all keep suspend as their async primitive and map the flow return separately; none collapses it. The literal collapse to a synchronous `KotlinStateFlow<T>` is rejected because it requires `runBlocking` (ADR-019 Alt-3, explicitly rejected) plus per-`.Value`/per-`foreach` re-invocation of a suspend body, and the plain drop-suspend reading is a compile error (a suspend fun cannot be called from a non-suspend `@CName` export). Verified-in-repo: suspend-before-flow partition (`CirClassTranslator.kt:277-284`) currently mis-routes it to `Task<StateFlow>` over an undefined C# type, and ADR-065's per-member `_value`/`_collect` re-invoke `obj.member(...)` so cannot host a suspend receiver (`ClassExports.kt:416,448`). Needs new machinery: two shared **generic** exports keyed on the awaited flow handle (`nuget_stateflow_collect`/`nuget_stateflow_value` on a star-projected `StateFlow<*>`) and a handle-owning `KotlinStateFlow<T>` variant that disposes the flow `StableRef` — the same infrastructure the deferred StateFlow-as-parameter/type-argument and suspend-returning-`Flow` (ROADMAP line 118) items will reuse. The suspend `_async` export is reused unchanged (already boxes the return via `StableRef`). Load-bearing inferred (not spiked): the star-projected `StateFlow<*>` handle exports compile and round-trip; confirm via the walking-skeleton integration test
- [069](069-nullable-boolean-marshalling.md): Forward, nullable `Boolean` marshalling (Accepted), closing ADR-061's deferred width. Establishes by spike that C# `out bool` under default P/Invoke marshalling reads **4 bytes** while Kotlin's `BooleanVar` writes **1**, so ADR-061's `valueOut` shape would have surfaced a Kotlin `false` as `true`. `Boolean?` is a matrix extension, not a new shape: each position keeps its existing ABI (single-call `valueOut` for method/extension/object/companion returns, ADR-002 two-call for top-level, unchanged for properties/constructors/parameters) and `Boolean` adds only an explicit 1-byte contract, `[MarshalAs(UnmanagedType.I1)]`, on the two seams where C#'s default is 4 bytes. Also records that three of the five positions already work today and that a top-level `fun f(): Boolean?` **crashes `packNuget`** rather than skipping. `Char?` stayed deferred (2-byte UTF-16, needs `ushort` narrowing, not `MarshalAs(I1)`), later closed by [ADR-098](098-narrow-primitive-and-char-collection-components.md)'s 2026-09-22 amendment
- [070](070-csharp-interfaces-in-kotlin.md): Reverse — C#-declared interfaces in Kotlin (Proposed): a bound `RirInterface` is extracted today but consumed by neither generator, and `RirTypeRef` has no interface variant, so every interface-typed return/parameter/property is skipped with a misleading `skipped_unbound_type_reference` ("bind this type's namespace" — it already is). **Spiked against the real reader and a real net8.0 assembly, not inferred**: the ADR-040 × ADR-051 mirror holds, because an `[UnmanagedCallersOnly]` thunk may cast a `GCHandle` target to an **interface** and dispatch virtually to a runtime type that is `internal`, unbound, and never named by the shim (proved end-to-end through a `delegate* unmanaged[Cdecl]`). So each admissible interface gets its **own** `nuget_{ns}_{IFoo}_register` export and shim (a concrete class's slots cannot be used — there is no runtime type tag on the wire), and slot tables are handle-agnostic, which makes `interface IDerived : IBase` free. Generates a **pure** Kotlin `interface IFoo` plus a handle-backed `IFooHandle : IFoo, NugetHandleOwner, AutoCloseable` — exactly Xamarin's Invoker pattern (*"the Invoker type is only necessary when obtaining JNI references to Java-created instances"*), where Kotlin-consuming-Java gets this free (one vtable) and COM buys it with a vtable in the object. The handle lives on a `NugetHandleOwner` marker on the *implementations*, never as an interface member, mirroring ADR-040's rejected `internal IntPtr NugetHandle` and its shipped `NugetMarshal.HandleOf` + throw; **this is what keeps Phase 13 (Kotlin implementing a C# interface) separable** — it becomes one extra branch, no signature change. A bound class declares `: IFoo` only when every interface member is an identically-signed bridged member (verified: an explicit C# implementation is non-public in metadata, so the member is simply absent and the supertype would not compile). Rejected: reusing `RirObjectHandleType` (the ABI is identical, but a new sealed variant makes the compiler enumerate every `when` that must change, where reuse is a silent fall-through), dispatch through the concrete class's slots (not implementable), flattening to a class only (blocks Phase 13). v1 admits public, top-level, **non-generic** interfaces with instance members in the ADR-043 vocabulary; verified new skips for generic interfaces (the reader emits the mangled `IGeneric` + backtick + `1`), interface statics (`static abstract` is **CS8926**), indexers (reach the RIR as a parameterless property named `Item`; a generated `receiver.Item` is CS1546) and events (dropped silently today) — the last two are **pre-existing class-side gaps** this fixes too. Load-bearing **inferred**: that the generated Kotlin compiles and round-trips on the real toolchain (no Kotlin toolchain this session, and the reverse direction still has no generated-Kotlin compile check) — the walking skeleton must confirm. Deferred: Kotlin-implemented interfaces, downcast/`QueryInterface`, cross-type object identity, synthesized overrides for explicit implementations, generic interfaces, DIM consumption
- [071](071-mutable-stateflow-mapping.md): Forward, `MutableStateFlow<T>` mapping, settable `.Value` (Accepted): corrects ADR-065/ROADMAP:115's wrong Phase 7 claim, the write is ordinary forward-direction parameter marshalling; a `MutableStateFlow<T>`-declared member maps to `KotlinMutableStateFlow<T> : KotlinStateFlow<T>` with a `new`-shadowed settable `Value`, keyed on the declared type so the read-only `StateFlow<T>` idiom is unaffected. Verified end to end: the setter export compiles on konanc (`packNuget`) and a write from an arbitrary .NET threadpool thread is safe (`SettableValue_WriteFromThreadpoolThread...`, `GeneratedBindingsCheck` green)
- [072](072-closed-constructed-generics-in-kotlin.md): Reverse, closed constructed generics in Kotlin (Accepted): a bound package's generic class binds as ONE real Kotlin `class Box<T>` over an erased ADR-051 handle, with a per-instantiation witness bridge, bindings table, registration export and C# thunk set (`[UnmanagedCallersOnly]` cannot be generic nor live in a generic type, CS8895, verified). Amends ADR-043: open generic *parameters* stay excluded (generic methods keep `skipped_open_generic`), a generic type *definition reachable through a bound closed instantiation* is now bindable. The metadata reader enumerates instantiations in a two-phase fixed point (the reverse of ADR-010's KSP usage scan). Verified latent bug closed: a package-declared generic class reaches the RIR as ``Box`1`` today, with bridgeable members, and that name is interpolated straight into a C# class name, a cast, a filename and a C export symbol. Verified nullability bug: `NullableAttribute` byte arrays at instantiation positions are non-uniform (`Box<string?>` is `[1,2]`), so the reader's index-0 decode silently loses a type argument's nullability. BCL instantiations (`List<int>`) are deliberately NOT bound (an unbound definition has no extracted members), they get `skipped_unbound_generic_instantiation` and stay with the collections item
- [073](073-map-and-set-parameters.md): Forward, `Map`/`MutableMap`/`Set`/`MutableSet` as method parameters (Accepted): closes the gap ADR-062 named and ADR-064 only gave a diagnostic to. Copy-in over the existing list write-side, with new `nuget_map_create`/`nuget_map_put`/`nuget_set_create`/`nuget_set_add` exports and `NugetMarshal.CreateMap`/`CreateSet` built on a factored-out `Wrap<T>`; **all four spiked end to end** on konanc 2.2.10 + a C driver, including that boxed values dedup structurally in the intermediate `mutableMapOf<Any?, Any?>`. Public C# types mirror the return position unchanged (`IReadOnlyDictionary`/`IDictionary`/`IReadOnlySet`/`ISet`), and **a mutable parameter does not write back**, matching the shipped `MutableList` parameter (verified) and C++/WinRT's documented *"Results are not moved back"*. Rejected the ObjC-export design (a Kotlin-owned `KotlinMutableDictionary`): the spike shows passing the platform's own `NSMutableDictionary` to that analogue **segfaults**, exit 139. Introduces an input-position `isWrappableComponent()` allow-list (the six `nuget_wrap_*` primitives plus `ObjectHandle`) rather than reusing `isBridgeableComponent()`, which is too permissive and already makes `List<String?>`/`List<StoryCode>` parameters crash `packNuget` and `List<Mood>` throw at runtime. Deferred and now on the roadmap: narrowing the `List` branch the same way, enum/narrow-primitive/nullable/value-class/nested-collection components, write-back, and the pre-existing handle leak when the callee throws
- [074](074-expect-actual-declarations.md): Forward, `expect`/`actual` declarations (Accepted): **the `actual` is the export root**, so `allDeclarations` drops every `isExpect` declaration at the funnel, which is the same rule, at the same three declaration kinds, as **Kotlin/Native's own C export** (`CAdapterGenerator`: `isExportedFunction`, `isExportedClass` with the comment "Do not export expect classes.", `visitPropertyDescriptor`), ObjC export in **both** implementations (K1 `ObjCExportMapper.shouldBeExposed`/`shouldBeVisible`, Analysis-API `isVisibleInObjC`) and Wasm/JS klib export (`WasmExportUtils`, "expect declarations are eliminated during wasmLowerings"), all four verified in upstream source. Those backends run after actualization and see one declaration per pair; KSP is a per-compilation, per-file view and sees two, which is why every shape of `expect`/`actual` currently kills generation for the whole module with a raw `IllegalArgumentException` from a catalog duplicate guard. **Spiked against a real two-target KMP project on `kspKotlinMacosArm64` (Kotlin 2.4.10 / KSP 2.3.10), not inferred**: `getAllFiles()` returns `commonMain` + `nativeMain` + `{target}Main`; `Modifier.EXPECT`/`ACTUAL` are reliable; **`findActuals()`/`findExpects()` return empty for everything**, so KSP's first-class linking API is not a usable dedupe key and the modifier filter is; KDoc, annotations and **default parameter values exist only on the `expect`** (Kotlin forbids defaults on an `actual`), which is a named prerequisite for the open Phase 4 default-parameter item; an `expect class` with no explicit constructor has `ctors=0` while its `actual` has a synthetic one, so the `expect` side structurally cannot be the root; a type reference to an `expect`/`actual` **class** pair resolves to the **actual**, but one to a class actualized by an **`actual typealias` resolves to the `expect` class** from every source set, and `KSType.declaration` is a `KSClassDeclaration` so `expandAliases()` (ADR-018) can never see through it. Hence `actual typealias` needs its own rule: **erase to the alias target** (ADR-018 precedent, and the JS-export fix in KT-64951 does the same) plus a **name-based redirect** built from the `KSTypeAlias`'s `findActualType()`, applied in the classifier and the ADR-066 closure before the export-set lookup, with a new `SKIPPED_ACTUAL_TYPEALIAS_TARGET` when the target is a platform-library/stdlib/out-of-scope type (`SKIPPED_UNEXPORTED_DEPENDENCY_TYPE`'s `include(...)` hint is wrong there). Also fixes a per-target naming hazard nobody had named: a top-level `actual fun`/`val`'s C# static class comes from `containingFile.fileName` (ADR-007), so `PlatformMacos.kt`/`PlatformMingw.kt` would generate **different C# class names per target** while `packNuget` packages exactly one target's `Interop.cs` and ships every target's binary (verified in source); the file class is therefore taken from the **`expect`'s** file. Rejected: `expect` as the root (cannot express the implicit-constructor row, drops members an `actual` adds, no backend does it), a merged synthetic declaration (deferred, nothing consumes KDoc/annotations today), and skipping the pair with a diagnostic. Accepted limitation with its own follow-up item: two targets' `actual`s may declare different public API and only one target's C# is packaged, which no per-run check can see (`ForwardAbiContract` is per-run; the other target's source set is not in this compilation), so the fix is a plugin-level cross-target ABI-manifest diff. Koin is confirmed **not** a factor. Tier 1 can host every cell: `KSPConfig.commonSourceRoots` was **verified** to process an `expect`/`actual` fixture on a JVM KSP2 run with `EXIT=OK`. Amended 2026-09-05: the v1 deferred list is closed (exercised or rejected, nothing deferred).
- [075](075-collection-property-getter-setter-independence.md): Forward, collection property getters and setters are planned independently (Accepted): closes both post-migration hardening items as one change, because both are facets of `ForwardPropertyPlanner.propertyPlan:139`. Shipped with two amendments the ADR's Decision section didn't originally cover but its own fixture needed: a value-class extension-property receiver (`ChartId.symptomTags`) now plans, and `Nullable(Collection)` now plans as an ordinary callable parameter (constructor/method/`copy()`), not only as a property setter. A `var` with a collection type loses its whole property today (the getter regression), and a `val` nullable collection property crashes KSP at `ForwardPropertyKotlinEmitter:72` while the C# side would return a raw `IntPtr` typed as `IReadOnlyList<T>?`. Getters become total with no element restriction (verified: `collectionMaterialize` loops over the single runtime-generic `FromHandle<T>`). Setters reuse ADR-073's `isWrappableComponent()` for **every** collection kind including `List`, deliberately stricter than the callable `List` parameter branch: there is no compatibility debt to preserve because no collection property setter binds today, and the wider `isBridgeableComponent()` is known to crash `packNuget` (`List<String?>`) and throw at runtime (`List<Mood>`) at parameter positions. Verified `NugetMarshal.Wrap<T>` boxes exactly string/int/long/float/double/bool plus a reflective `_handle`, then throws. An ineligible setter yields a **get-only** C# property (verified: `CirClassRenderer:363` already renders `prop.setter == null`, the shape every Kotlin `val` ships as) plus a `SKIPPED_UNSUPPORTED_INPUT` diagnostic with partial-skip wording; rejected a throwing setter (ADR-064 skips what it knows statically) and the status-quo whole-property skip. Verified trap for the implementer: `needsCollectionParamWrap` is computed from `callableCatalog.plans` only, so a class whose only collection input is a property setter would call `nuget_wrap_string` against a library that never exported it. Mutable collection property types are in scope and do **not** inherit ADR-073's no-write-back problem, a setter is an assignment, not a copy-in. Nullable collection setters are **in** v1 on the ordinary `Direct` route with a `COpaquePointer?` value (an earlier draft deferred them on the retracted grounds that `nuget_list_add`'s non-null `COpaquePointer` blocked it — that constrains *elements*, and is never reached when the collection reference is null); verified that `kotlinInputType` already maps `Nullable(Collection)` to `COpaquePointer?` and that `NullableDispatch`'s `value.HasValue` branch is unreachable for anything but `Nullable(Primitive)`, so it needs no widening. Deferred: enum/`Char`/narrow-primitive/nullable/value-class/nested-collection *components* at a setter position
- [076](076-instant-mapping.md): Forward, `kotlin.time.Instant` → `System.DateTimeOffset` over a single `Int64` of .NET ticks (Accepted): a first-class known stdlib type in the same category as `String`, two hardcoded qualified-name checks (classifier + `ForwardReachabilityClosure.SCALAR_TERMINALS`), never a user mapper. **Verified in stdlib sources**: `Instant` is a plain `public class` (not `value`, not `expect`) of `epochSeconds: Long` + `nanosecondsOfSecond: Int`, stable since 2.3. **Verified by source reading**: the ADR-062 plan model *can* carry multiple scalars per crossing at every position (`nativeInputParameters` returns a `List` and callers `flatMap`; `ForwardResultShape.extraParameters` is a list of OUT pointers), so a seconds+nanos wire form was not eliminated by the machinery, it is rejected on merit, because the exactness is discarded the instant it becomes a `DateTimeOffset`. The wire form is chosen to *be the destination type's own domain*: 100ns ticks since 0001-01-01, always non-negative. **Verified by dotnet spike**: `DateTime.MaxValue.Ticks = 3155378975999999999`, `UnixEpoch.Ticks = 621355968000000000`, `DateTimeOffset.UtcTicks` normalizes any offset, `Equals`/`==` compare the instant so a round trip asserts equal without normalizing, negative ticks throw. **Verified by running the real conversion arithmetic against `kotlin-stdlib-2.4.10.jar`**: pre-1970 truncating division hands `fromEpochSeconds` a negative nanosecond adjustment and it normalizes correctly; all six boundary cases round-trip exactly. `Instant?` → `DateTimeOffset?` rides the shipped nullable-primitive `INT64` machinery with no width hazard (ADR-069's `Boolean?` problem does not recur), widening four `is BridgeType.Primitive` sites. New `BridgeType.Instant` variant modelled on `BridgeType.Enum` (semantic type, primitive wire, conversion + helper), not a `Primitive(LONG)` re-use, so the compiler enumerates the `when`s. Rejected: ISO-8601 `String` (verified: .NET *rounds* >7 fractional digits while Kotlin truncates, and an out-of-range instant is a `FormatException` deep in marshalling), epoch nanoseconds (`Int64` covers only 1677-2262, narrower than the destination), opaque `KotlinInstant` handle (what ObjC export does; the roadmap rejects it). Range contract: years 0001-9999 only, sub-100ns truncated, `DISTANT_PAST`/`DISTANT_FUTURE` throw through the existing `errorOut` slot. Also removes the unactionable `include(kotlin.time)` hint `SKIPPED_UNEXPORTED_DEPENDENCY_TYPE` gives today. Deferred: `Duration` → `TimeSpan` (cheap second branch, but not required to ship `Instant` and not in this fixture), legacy `kotlinx.datetime.Instant`, `LocalDate`/`LocalDateTime`/`TimeZone`, `Instant` as a collection element or extension receiver, sub-100ns precision, custom type mappers
- [077](077-value-classes-at-ordinary-positions.md): Forward, value classes at ordinary positions (Accepted): closes the four ROADMAP sub-items ADR-066's fixture flushed out: String-underlying parameters, String-underlying property getter/setter, `Nullable(ValueClass)`, and the Primitive/Enum/ObjectHandle underlyings. All ride ADR-014's shipped design: the underlying wire value crosses, tagged with the single-slot `BOX_VALUE_CLASS`/`UNBOX_VALUE_CLASS` conversion the validator already demands (verified), with the underlying's own step (UTF-8, `.ordinal`, StableRef) composed inside the emitted expressions, exactly like the shipped String-underlying result. `Nullable(ValueClass(String))` surfaces as a C# nullable **value** type (`Nullable<StoryUri>`) riding the null pointer, no has-value pair; nullable over primitive/enum underlyings is deferred. Names the four genuinely silent `else ->` fallthroughs (`ForwardPropertyPlanner.conversion()`'s `DIRECT`, `nativeCsharpType()`'s missing `string?`, `valueArgument()`'s bare `name`, `checkedGetter`'s `return nativeResult;`) and the inferred prerequisite that no enum-underlying value class fixture exists, so `CirClassTranslator`'s struct rendering for that underlying is unverified. All four sub-items have since shipped
- [078](078-forward-abi-legacy-contract-coverage.md): Tooling, forward ABI contract coverage for the specialized legacy protocols (Accepted): ADR-055's generation-time `ForwardAbiContract.assertMatches` check only collects `CirDllImport` nodes from ordinary callables, so the ~700 legacy-route `DllImport`s (generics, sealed classes, suspend, Flow, lambdas, stored callbacks, interface bridges, the shared helper classes) are invisible to it in both directions, since those are raw-text renderer output, not CIR nodes. Adds a text-based collector (`ForwardAbiContract.csharpLegacy`) that scans the same rendered `Interop.cs` text the build ships, normalizes each `DllImport`/`static extern` pair with the existing rules, and folds duplicates (`nuget_dispose` declared 6 times, all shape-identical, verified). Rejected: hand-written structured signature emitters per legacy helper (duplicates the renderer's shape knowledge by hand, so a renderer edit can silently desync from the check, the exact failure class this item exists to close) and migrating the legacy renderers to `CirDllImport` nodes (ADR-062's long-term direction, out of scope here). First real run found and fixed two pre-existing generator defects neither caused by this feature: `NugetMarshal.CreateBox<T>` hardcoded `box_create_*` entry points regardless of which generic class was being constructed (only masked because `test-library` happens to declare a class literally named `Box`), and the C# `nuget_wrap_*`/`nuget_dispose` imports were gated more widely than their Kotlin `@CName` exports
- [079](079-nullable-primitive-enum-underlying-value-classes.md): Forward, `Nullable(ValueClass)` over Primitive- and Enum-underlying value classes (Accepted): ADR-077 shipped value classes at every ordinary position but deferred `Nullable(ValueClass)` over a `Primitive`/`Enum` underlying, since unlike `String`/`ObjectHandle` the wire has no spare null pointer, a null and a legitimate `0`/first-ordinal value are indistinguishable in-band. Reuses each position's already-shipped nullable-primitive has-value fan-out (input parameter pair, ADR-061 single-call `valueOut`, ADR-002 `LegacyTwoCall`/`NullableDispatch`, top-level two-call), with the value slot carrying the underlying's own wire (the primitive's wire, or the enum's `int` ordinal) and the box/unbox step composed on top, the same template ADR-076 proved for `Instant`. `Dosage?`/`Temperament?` bind at every ordinary position as C# `Nullable<T>`; a Boolean-underlying value class's nullable return inherits ADR-069's `[MarshalAs(UnmanagedType.I1)]` for free. Bare `Nullable(Enum)` (`Mood?`, not value-class-wrapped) stayed out of scope, including its latent property-getter crash, closed by [ADR-080](080-bare-nullable-enum.md)
- [080](080-bare-nullable-enum.md): Forward, bare nullable enums (`Mood?`) (Accepted): closes the property-getter `packNuget` crash ADR-079 left open, by binding rather than skipping. `ForwardPropertyPlanner.isPlannable`'s `Nullable` branch accepts a bare `Enum` inner, but `hasValueFanOutInner()`/`ForwardPropertyKotlinEmitter.addGetter` have no bare-`Enum` case, so the property's getter takes the `Direct` route and `packNuget` aborts (verified by a runtime spike); bare `Mood?` parameters and returns don't crash but silently skip with the mislabelled `NULLABLE`/`SKIPPED_UNSUPPORTED_RETURN` diagnostic. Same latency class as ADR-069's top-level `Boolean?` crash: an accepted plan whose emitter has no route. Chosen: bind `Mood?` at every ordinary forward position (property, constructor/method/extension/top-level parameter, method return, top-level return), riding ADR-079's has-value shapes with the value-class box/unbox step removed, since the wire is already the `int` ordinal; rejected narrowing to a named property-only skip or a property-only fix, both of which would leave `Mood?` unsupported everywhere `Temperament?` (the strictly more complex wrapped case) already binds. As built: 7 files, 15 edit sites, wider than the ADR's original 6-site estimate because both the Kotlin and C# projection halves needed widened `require`s the original draft missed. Fixture: `test-library/.../cat/NullableEnumSample.kt` (`MoodJournal`, `soothe`, `Cat.matchesMood`, `napMood`); tests: `IntegrationTests/NullableEnumTests.cs` (15 facts), `Tier1BareNullableEnumTest` (4 cells). Split out, not fixed: a bare `Char?` property is the same Direct-route crash class, deferred with the existing `Char?` item's UTF-16-width reasoning (closed by [ADR-098](098-narrow-primitive-and-char-collection-components.md)'s 2026-09-22 amendment); the `NULLABLE`-always-maps-to-`SKIPPED_UNSUPPORTED_RETURN` wording bug for a parameter offender
- [081](081-value-class-collection-components.md): Forward, value-class collection components (Accepted): closes the value-class half of ADR-073's deferred `List`/`Map`/`Set` component gap, shipped as the full option rather than a narrowed one. A value-class component (element, map key, or map value) crosses as its **underlying**, projected per element at the boundary in codegen, not in the runtime-generic `Wrap<T>`/`FromHandle<T>` helpers: `isWrappableComponent()` gains a `ValueClass` branch admitting the four ADR-077 underlyings (String, wrappable primitive, ObjectHandle, and an enum-underlying value class via its `int` ordinal, with the top-level bare-`Enum` case staying `false`), and `elementKotlinTypeName`/`loweredCollectionExpression` gain the composition to re-wrap per element on the Kotlin side. Binds at input positions (every `List`/`Map`/`Set` call position ADR-073 covers), collection property setters, method returns, and property getters; the read side (return, getter) was previously verified to bind and then throw at runtime (`NugetMarshal.FromHandle<ChartId>` falling to a reflective `Activator.CreateInstance` that finds no matching constructor), and that defect is closed in the same feature rather than deferred. No forward-ABI change: no new exports, same `nuget_list_*`/`nuget_map_*`/`nuget_set_*` signatures, only the generated export bodies change. Fixture: `test-library/.../clinic/ValueClassCollectionsSample.kt` (`ChartBook`); tests `IntegrationTests/ValueClassCollectionTests.cs` (11 facts), `Tier1ValueClassCollectionComponentTest` (4 cells). Deferred, each to its own named sibling ROADMAP item: nullable value-class components, bare enum/narrow-primitive components, nested-collection components, the reverse direction. Found during implementation, out of this ADR's scope: `ForwardCallablePlanner.nullableResultShape()` has no `Collection` branch, so a nullable collection **method return** (`List<T>?`) has no planner route at all and is silently skipped, for every component type, pre-existing and not introduced here
- [082](082-value-class-inherited-members.md): Product decision (Accepted): ratifies [ADR-064](064-forward-unsupported-declaration-diagnostics.md)'s `SKIPPED_INHERITED_MEMBER` skip as the permanent behaviour. Inherited and delegation-forwarded members (e.g. `CharSequence` via `by value`) stay out of a value class's export set forever; consumers reach the supertype's own API through the always-present underlying property, which is strictly richer than any bridged subset could be. Matches ordinary-class behaviour (declared-only, verified silent there), ObjC/JVM interop precedent, and the C# strongly-typed-ID idiom (`.Value`). Zero code shipped. Known, accepted limitation: the filter's signal is simple-name matching, so an author-declared member whose name merely collides with a supertype member's is also skipped under the same diagnostic
- [083](083-nullable-collection-components.md): Forward, nullable collection components (Accepted): a null list element, set element, or map value crosses the bridge by riding a null pointer in the component slot, both write and read, for every wrappable component kind including a value class; closes the write-side gap plus an adjacent verified read-side bind-then-NPE bug and the `List<String?>` parameter crash. Nullable map keys stay a named skip (`Dictionary` can't hold `null`) at input positions, and (2026-09-22 amendment) at every result position too — a return, a property, and a nested component all render `ReadMap<K?, V>`, which is `CS8714` against that helper's `where TKey : notnull`; the `Chart.aliases` fixture cell flips from ADR-075's ineligible-setter example to settable, since its element nullability was exactly what made it ineligible
- [084](084-csharp-implemented-interfaces.md): C#-implemented Kotlin interfaces at general parameter positions (Accepted): closes the ADR-040 v1 boundary (`NugetMarshal.HandleOf` throwing `NotSupportedException` for a C#-implemented `IPet`) with a per-interface bridge factory export, one fnPtr+ctx slot per member (`val` getters and arity 0-2 methods returning `Unit`/a primitive/`Boolean`/an enum/`String`/`String?`); `HandleOf` falls back to the bridge instead of throwing. Lifetime: the Kotlin-side bridge object owns a `createCleaner` invoking a C#-supplied release function pointer when Kotlin's GC collects it (release is GC-timed, never prompt; `nuget_gc_collect` export exists for deterministic tests). Identity: a `nuget_csharp_token` probe resolves a stored C#-implemented object handed back to C# to the original instance (`Assert.Same` holds), C#-side only. **Falsified and corrected while implementing commit 2**: the Decision's `ConditionalWeakTable`-reuse design (one bridge per C# object, preserving Kotlin-side `===` too) cannot work, because the C# state would need a strong reference back to the Kotlin bridge to free it later, which cycles with the strong reference the bridge needs the other way to keep the C# object alive; the shipped design mints one bridge per *crossing* instead, and the factory handle is disposed as a transfer handle at the call site, so Kotlin-side `===` across repeated crossings of the same C# object is **not** preserved, a documented divergence from the ADR's original claim. An interface with a `var` property or any other member outside the v1 slot vocabulary (object/collection slots, `suspend`, generics) plans no factory at all, silently, and keeps the ADR-040 throw
- [085](085-kotlin-implemented-csharp-interfaces.md): Kotlin-implemented C# interfaces passed back to C# (Accepted), the exact inverse of ADR-084: a plain Kotlin `class Goat : IFeedable` (no `NugetHandleOwner`) passed at an interface-typed position mints a C#-side `IFeedableBridge : IFeedable` holding one Kotlin-minted `staticCFunction` function pointer per member plus a ctx `StableRef`, via a factory thunk registered as two extra slots (factory + `IFeedableKotlinToken`) on that interface's own ADR-054 registration, not the shared `<runtime>` one. Lifetime is inverted from ADR-084: a C#-side `KotlinRefHandle : SafeHandle` releases the ctx `StableRef` (`nuget_kotlin_release`) when the .NET GC collects the bridge, verified both for a dropped bridge (a few forced collections) and a stored one (survives collection and still resolves). Kotlin-side identity only (`assertSame(goat, sanctuary.featured)`), mirroring ADR-084's C#-side-only identity; C#-side `ReferenceEquals` across two crossings of the same Kotlin object is not preserved (one bridge per crossing, same posture as ADR-084 as shipped). **Corrected during implementation**: the string-return wire is not `value.cstr.getPointer(nativeHeap)` (does not compile, `getPointer` needs an `AutofreeScope`) but `nativeHeap.allocArray` + a per-byte copy + a trailing NUL (`nugetKotlinString`); the identity probe is a per-interface registered slot beside the factory, not a shared runtime-registered thunk; the bridge class keeps the `I` prefix (`IFeedableBridge`, not `FeedableBridge`); the release counter uses the same `compareAndSet` loop as `NugetRegistry.record`, since `AtomicInt` has no `incrementAndGet`/`addAndFetch`; and v1 ships with no per-slot `try`/`catch` fast-fail, so an exception inside a slot terminates via Kotlin/Native's unhandled-callback path without naming the member. v1 narrower than the Decision's sketch: bound-object-handle parameters are out of the slot vocabulary (named-skipped, not bridged); interface inheritance (`ITagged`) is not Kotlin-implementable at all (no flattened derived factory); every out-of-scope shape is a named `skipped_kotlin_bridge` diagnostic, never silent
- [086](086-object-interface-slots-kotlin-bridge.md): Reverse, object- and interface-typed slots for a Kotlin-implemented C# interface (Accepted): widens ADR-085's slot vocabulary to bound-object handles and bound interfaces (nullable included) at method parameters, returns, and property getter/setter slots, on the existing GCHandle-IntPtr wire (`cfnType` maps all three reference kinds to `COpaquePointer?`, verified). Ownership is one rule, the receiving side owns the handle: a slot **parameter** is a transfer handle C# mints (`GCHandle.Alloc`) and the Kotlin wrapper's existing `createCleaner` → `freeGcHandleFn` path frees, so a Kotlin implementation may safely *store* the received object (the capability borrow semantics could not give); a slot **return** is always a fresh transfer handle the C# bridge member resolves (`.Target`) and frees in `finally`, freshness coming from a new per-interface conditional `dupHandleFn` thunk for handle-backed values (contract tag bumps to `kotlin_bridge_v2`) and from ADR-085's factory for plain-Kotlin impls (still one bridge per crossing; the reuse item stays open). Identity: interface-typed slot parameters lower through the existing `nuget{Iface}Value` token probe, so a Kotlin impl round-tripping C# → Kotlin resolves to the original object; a C# object round-tripping Kotlin → C# is the original managed instance by GCHandle definition. Rejected: borrowed parameter handles (dangle when the impl stores the object), borrowed return pointers (cleaner race, designed away rather than spiked), and collections in v1, on sequencing: the reverse pipeline has **no** BCL collection mapping at any position (`List<int>` is `skipped_unbound_generic_instantiation`, ADR-072), so collection slots wait for the Phase 10 collections item. Structs, generic instantiations, `Task`, unbound object types stay named-skipped
- [087](087-kotlin-slot-exceptions.md): Reverse, exceptions from Kotlin-implemented C# interface members (Accepted), two stages sharing one catch: stage 1 wraps every ADR-085 slot body in `try`/`catch` that prints one line naming the C# member (`IFeedable.Describe`) and rethrows, so today's anonymous unhandled-callback termination becomes attributed at zero ABI risk (spiked: the naming line prints, then the ordinary `Uncaught Kotlin exception` termination, exit 134); stage 2 gives every slot a trailing error out-param (the slot-boundary mirror of ADR-024) whose catch writes a `StableRef<NugetError>` envelope reusing the forward ADR-027/028/029 infrastructure verbatim, the C# bridge member checks it and throws through `NugetErrorNative.BuildException`, so a Kotlin-implemented member throws the same catchable `KotlinException` hierarchy as every forward call. Spiked outside the repo: a Kotlin `try`/`catch` inside a `staticCFunction` invoked from .NET main/pool threads catches, writes the envelope through `IntPtr*`, returns a dummy, and the host process survives repeated throws. Stage 2 must bump the `kotlinBridgeContractHash` version tag (the errOut arity change is exactly the same-arity drift `slotCount` cannot see). Open wiring (inferred, fails loudly not silently): reverse bindings referencing the public `NugetError` class across generated modules, and reverse shims reaching the forward `internal NugetErrorNative` across namespaces
- [088](088-kotlin-implemented-interfaces-at-forward-positions.md): Forward, bound C# interfaces at ordinary forward positions (Accepted): a Kotlin class implementing a bound C# interface (`test.menagerie.IFeedable`, the ADR-070 stub) can now be taken as a parameter or returned from an ordinary forward Kotlin API (`Farm.adopt`/`Farm.resident`), surfacing the **original** `Test.Menagerie.IFeedable` in the generated C#, not a re-projected duplicate (the shipped enum-duplication precedent, deliberately not extended). The ADR-070 pure interface stub goes public via an `interfaceStubIsPublic` predicate, not a blanket flip: public iff nothing in its declared surface (or a base interface's) is an internal bound-class/struct/generic-instance stub, so `IKeeper` (whose members reference the internal bound class `Ferret`) stays internal and off the forward export scan. A new cross-pipeline manifest (`bound-types.json`), written by the plugin and read by the KSP processor, carries each bound interface's Kotlin name, original C# name, and ADR-085 Kotlin-implementability, since `nuget.boundPackages` alone can't invert to the C# spelling. Wire: the reverse pipeline's own GCHandle machinery, `nuget{Iface}Value` on the way in, a per-interface `dupHandleFn` (widened from ADR-086's slot-only gating to every plannable interface, since a forward return is not a slot) on the way out. v1: non-nullable interfaces at ordinary parameters and method/top-level-function returns only; nullable, property, and collection-component positions are a named `SKIPPED_BOUND_TYPE_POSITION` skip, and a return of a non-Kotlin-implementable interface is `SKIPPED_UNIMPLEMENTABLE_BOUND_INTERFACE`, both diagnosed rather than silent. Transitive NuGet restore confirmed end to end: a consumer with no direct `TestDependency` reference still resolves `Test.Menagerie.IFeedable` through `TestLibrary`'s own nuspec dependency
- [089](089-bridge-reuse-per-kotlin-object.md): Reverse, bridge reuse per Kotlin object (Accepted): repeated crossings of the same Kotlin object implementing a bound C# interface now hand C# the **same** bridge instance while C# keeps it alive, restoring C#-side `ReferenceEquals` that ADR-085/086 had documented as unavailable. Each `{Iface}Bindings.kt` gains a per-interface identity-keyed table (`kotlin.native.identityHashCode`, verified by konanc probe to accept a null receiver and return `0`) holding the minted bridge **weakly**, via two new interface-agnostic C# thunks (`WeakenGcHandle`/`ResolveGcHandle`) riding the shared `<runtime>` registration, which grows from 1 to 3 slots and moves `NUGET_RUNTIME_CONTRACT_HASH` accordingly. A weak handle never roots the bridge (spike-verified), so the table cannot recreate the strong cross-runtime cycle that falsified ADR-084's original reuse design. Table access is a CAS spinlock on `AtomicInt`, matching `NugetObjectHandle.free`'s style. Eviction happens in `nuget_kotlin_release`, guarded by the releasing bridge's own ctx pointer so a late finalizer can never evict a newer live entry for the same object; the evicted weak handle is freed **lazily** (queued, drained on the next mint) rather than from inside the finalizer-thread release call, since that call is already a C#-to-Kotlin P/Invoke and a Kotlin-to-C# re-entry from there was never spiked. A race beyond the Decision's own text was found and closed: two threads minting for the same object concurrently can both succeed, and the losing store's weak handle is queued for eviction rather than leaked. Supersedes ADR-085/086's "one bridge per crossing" consequence lines for a **live** bridge (documented via addendum on those ADRs, not rewritten); once C# drops every reference and the .NET GC collects the bridge, the next crossing still mints a fresh one
- [090](090-ordinary-class-method-overloads.md): Forward, ordinary-class method overloads (Proposed): mirrors ADR-082's value-class overload numbering verbatim onto `classEntries`. Spike-verified crash today: two same-name methods on any class abort `packNuget` at `planFor`'s duplicate-plans guard (the same message the amendment predicted), and the identical crash shape exists on objects, companions, top-level functions, and extension functions (verified, recorded as separate roadmap items). First declared overload keeps `${prefix}_$name` / symbol `$owner.$name`, the n-th gets `${prefix}_${name}_$n` / `$owner.${name}_$n` (n from 2, declaration order, structurally-skipped namesakes consume numbers, inherited ones never do); C# surface stays one natural overload set. Both emitters go catalog-driven via a new `classMethods(owner)` accessor (owner-exact, since `interfaceEntries` shares the CLASS origin), `invocationExpression`'s CLASS branch consumes `invocation.member`, `isOverride`/`isVirtual` move to planning time onto the plan, the private extern *names* follow the numbering (CS0111 on shared wire shapes otherwise, both `methodNativeImport` and the body projection derive from the public name today, verified), and the ADR-034 `ERROR_CSHARP_SIGNATURE_COLLISION` check is replicated from the value-class path (verified absent for ordinary-class methods)
- [091](091-constructor-default-parameters.md): Forward, constructor default parameters (Proposed): `Cat(name: String, lives: Int = 9)` becomes a natural C# constructor overload set (`new Cat("Mouse")` works, Kotlin computes the default) via JvmOverloads-style synthesis: for each trailing run of defaulted parameters, the planner adds one CONSTRUCTOR-origin plan per omitted suffix, continuing ADR-034's `${prefix}_create_$n` numbering after the secondaries. C# optional parameters are impossible, not just rejected: KSP's `KSValueParameter` exposes only `hasDefault: Boolean`, no expression/value API (verified against the pinned 2.3.10 sources), and middle defaults get no overload since the generated wrapper is a plain positional Kotlin call. Zero Kotlin-emitter changes (arguments derive solely from the plan's parameters, verified); both constructor emitters go catalog-driven via a new `constructors(owner)` accessor (ADR-090's template, retiring the per-declaration `planFor` loops), and the ADR-034 `ERROR_CSHARP_SIGNATURE_COLLISION` check covers synthesized-vs-real collisions for free since it compares the full rendered constructor list. `expect`/`actual` primaries consult `expectsByName` (the actual's params all report `hasDefault=false`, ADR-074's named trap); expect-class secondaries, method/function defaults, value-class constructors, and partial `Copy(...)` are recorded out of scope
- [092](092-snapshot-versioning-dsl.md): Publish, snapshot versioning DSL (Accepted): `snapshot = true` in `nuget { publish { } }` mints `<version>-snapshot.<epochMillis>` at execution time (the fixture's proven always-out-of-date task + file-backed provider wiring, promoted from `test-library/build.gradle.kts`), and always emits an MSBuild props file (default `<rootProject>/build/<PackageId>Versions.props`, override via `versionPropsFile`) that the .NET consumer imports and references as `Version="$(<SanitizedId>Version)"`. Property name sanitization is spike-verified: MSBuild rejects dots (MSB5016) and leading digits (MSB4025), so the rule is drop everything outside `[A-Za-z0-9_]`, `_`-prefix a leading digit, append `Version` (`PeopleInSpace.Kotlin` gives `PeopleInSpaceKotlinVersion`). Rejected: a `snapshot("1.0.0")` setter competing with `version =`, a `version = snapshot(...)` marker (impossible on `String?`), consumer-side floating versions, and opt-in props emission (a snapshot without props is unreferenceable). Out of scope: the fixture's `PackTestDependency`/`dependencyVersions` override, nupkg retention, publishing to real feeds
- [093](093-multi-rid-package-inputs.md): Publish, multi-RID package inputs (Accepted): `prebuiltRuntimes = file(...)` in `nuget { publish { } }` points at a `runtimes/`-shaped tree (`<rid>/native/*.{dll,dylib,so}`) built by another CI host, merged with locally linked RIDs into the one existing pack pass, so one host packs the single publishable package (LIMITATIONS item 5). Disabled-link targets are excluded from `nativeLibDirs` at configuration time (logged, not silent), which makes strict validation possible: a produced-but-empty local RID dir errors (retiring `PackNugetTask.kt:67`'s silent `continue`), an empty prebuilt RID dir errors with the expected layout printed, and a RID both locally linked (enabled) and prebuilt errors as a matrix misconfiguration; unknown RID names only warn (open RID set). Rejected: per-RID `nativeLib("win-x64", ...)` calls (hand-typed RID strings, N declarations synced to the CI matrix), both shapes at once, and merging finished `.nupkg`s (random psmdcp UUID per pack, no NuGet merge tooling). Out of scope: cross-compilation, per-RID managed assemblies, split `NativeAssets`-style packages, `GeneratedBindingsCheck` changes
- [094](094-reflection-free-generic-dispatch.md): Forward, reflection-free generic dispatch (Accepted): replaces the two reflection shapes in the type-erased generics bridge (`Activator.CreateInstance` on `internal X(IntPtr)` at 2 emit sites, `GetField("_handle")` at 9, one of which, the generic-function object-argument branch, is itself 2 emit sites, corrected post-implementation) with a generated `Dictionary<Type, Func<IntPtr, object>>` factory registry in the root `NugetMarshal` plus an internal global-namespace `INugetHandle` interface implemented explicitly by every `_handle`-declaring wrapper (including all six arity shapes of the `KotlinFunc`/`KotlinSuspendFunc`/`KotlinSuspendAction` function wrappers, not one each), the Mac Catalyst / AOT blocker from the PeopleInSpace 0.3.0 integration's LIMITATIONS item 1 and ADR-038's step 2. Replace outright, no `CSharpProfile` toggle for dispatch (the registry has no JIT downside; the profile stays scoped to `[LibraryImport]`). Spike-verified: a public class in a child namespace implementing the internal global-namespace interface compiles, and a static-field-initialized registry is populated before the first `FromHandle<T>` call, so no `[ModuleInitializer]`. The premise that `NugetMarshal` is per-namespace was falsified: one helper per file in the root namespace, all other namespaces nest under it, so one registry with `global::`-qualified entries covers the assembly. Registered: every concrete `CirClass` with a handle ctor, every sealed subclass, and signature-observed closed generic instantiations (55 factory entries, 62 explicit `INugetHandle` implementations in the `test-library` fixture); misses throw a named `NotSupportedException` (consumer-invented closed generics lose accidental `Activator` support). Seam: `CirTranslator.kt:424` where `CirMarshalHelper` is built from the fully populated `namespaces` list. AOT claims beyond JIT stay inferred; the `PublishAot` smoke lane is ADR-038 step 4, out of scope here along with callback AOT work and `[LibraryImport]`
- [095](095-static-route-overloads.md): Forward, overloads on the four static export routes (Accepted): extends ADR-090's numbering to the four routes its spike verified crashing (`objectEntries`, `companionEntries`, `topLevelEntry`, `extensionEntry`), each with its own counter scope: per-object, per-companion, per-(package, name) for top-level, and per-(package, name) receiver-agnostic for extensions (the receiver is absent from the symbol, verified, so `Cat.pat`/`Dog.pat` in one package crash today too and are fixed by the same rule). The `_$n` suffix composes after `toCName` (`toCName(name)_$n`); `member = name` rides `staticEntry` and `topLevelNullablePrimitivePlan` (the Kotlin call-site emitter already consumes `member` for all origins, verified). Object/companion emission goes catalog-driven via `objectMethods(owner)`/`companionMethods(owner)`; top-level and extensions keep their declaration walks (namespace+file-class and `{Receiver}Extensions` grouping need the node) with a fail-fast node-identity `planFor(declaration)` against a `node` added to `Planned`. Extern names follow the numbered symbol tail at the three verified sites (`static`, `extension`, `staticLegacyTwoCall`; no ADR-090-style hidden third site, `renderSyncErrorCheckMethod` reads `nativeName` for statics); the ADR-034 `ERROR_CSHARP_SIGNATURE_COLLISION` check extends per generated C# container, companion statics checked together with the owning class's instance methods
- [096](096-function-default-parameters.md): Forward, function default parameters (Accepted): applies ADR-091's `@JvmOverloads` trailing-suffix synthesis to the five function routes (top-level, class method, `object`, companion, extension), so `fun greet(name: String, loud: Boolean = false)` becomes `Greet("hi")` in C#. Synthesized entries are appended after each route's declared pass, continuing that scope's ADR-095 counter (per-class/object/companion name, per-(package, name) for top-level, receiver-agnostic per-(package, name) for extensions), so every shipped export renders byte-identically. Class/object/companion need zero emitter changes (already catalog-driven via `classMethods`/`objectMethods`/`companionMethods`, verified); top-level and extensions crash today at `planFor(declaration)`'s `require(planned.size <= 1)` because a synthesized entry shares its source node (verified in source), so the singular accessor is deleted (not kept alongside: it is a trap for the next route to synthesize) in favour of `plansFor(declaration)` at four call sites, with the duplicate-plan invariant restated over non-synthesized plans. `override` methods, the interface route and value-class methods synthesize nothing (absent, never wrong, output); `expect` defaults are consulted through `expectsByName` for the **top-level `actual fun` only** (it is keyed by qualified name, verified, but `.toMap()` collapses expect overloads, hence a uniqueness guard), with class/object/extension/companion expect consultation explicitly deferred. Extern-name uniqueness and the `ERROR_CSHARP_SIGNATURE_COLLISION` check both cover synthesized entries with no change (verified). Not verified: `hasDefault` on the four non-constructor declaration kinds
- [097](097-enum-collection-components.md): Forward, enum collection components (Accepted): a bare enum at a `Map`/`Set`/`List` component position crosses as its `Int` ordinal, projected per element at the C# call site (`Select(x => (int)x)`) and re-wrapped Kotlin-side (`Mood.entries[it as kotlin.Int]`), reusing ADR-081's already-shipped value-class-over-enum wire verbatim: zero new native exports, zero new `Wrap<T>` branches. Spike-verified that `ROADMAP.md:133` was right and `:136` wrong: `List<Mood>`/`List<Short>`/`List<Char>` parameters bind and compile today, then throw `NotSupportedException` out of `Wrap<T>` at the first call (only `Map`/`Set` run the narrower `isWrappableComponent()` gate), and `List<List<String>>` aborts the whole KSP run in `elementKotlinTypeName`. The read side is broken too and the repo already knew: `FromHandle<Mood>` reaches `Materialize` with no `Factories` entry, which is why `Chart.Moods` is asserted shape-only. So `List`'s callable-input gate narrows from `isBridgeableComponent()` to `isWrappableComponent()`, converting every remaining bind-then-throw and KSP-crash `List` shape into a named `SKIPPED_UNSUPPORTED_INPUT` (ADR-083's template one level up); the loss set is exactly the set that throws or crashes today, so nothing working is removed. `Chart.moods` flips from get-only to settable. Sharpest edge: the C# projection and `componentLowering` must change in lockstep, including the nullable arm (`isWrappableComponent()`'s `Nullable` branch admits `List<Mood?>` automatically), or a boxed `Int` meets `it as Mood` and `ClassCastException`s at runtime with no compiler complaint. Deferred: the six narrow primitives (now priced as six `nuget_wrap_*` exports + six `typeof(T)` branches, and shown *not* to need `:136`'s "type-specialized boxing" reframe), `Char` (the only member genuinely coupled to the `Char?` width item: no `nuget_wrap_char` **or** `nuget_unwrap_char` exists), and the nested-collection capability
- [098](098-narrow-primitive-and-char-collection-components.md): Forward, narrow-primitive and `Char` collection components (Accepted): part A adds six `nuget_wrap_*` exports (`byte`/`ubyte`/`short`/`ushort`/`uint`/`ulong`), six `typeof(T)` branches in `Wrap<T>` mirroring `FromHandle<T>`'s existing ten, and six `PrimitiveKind`s in `isWrappableComponent()`, so `List`/`Set`/`Map` parameters and collection property setters over the six bind instead of skipping; zero Kotlin-lowering and zero C#-projection changes (unlike ADR-097's enums, the C# static element type *is* the wire type), and the nullable spellings (`List<Short?>`) come along unavoidably via ADR-083's `Nullable` branch. Part B mints `nuget_wrap_char`/`nuget_unwrap_char` and puts `[MarshalAs(UnmanagedType.U2)]` on **every** `char` slot the generator emits. Spike-verified against the real `libtest.dylib` and the real shipped signature: the already-shipped bare `Char` **parameter** silently corrupts every non-ASCII character (`Tag('é')` arrives Kotlin-side as `'Ã'`, `'日'` as `'æ'`, one-byte ANSI truncation of the UTF-8 encoding), and the `Char` **return** loses it to `U+FFFD`; `CharSet.Unicode`, `[MarshalAs(UnmanagedType.U2)]` and a `ushort` wire are all three exactly correct, so the per-slot attribute wins on ADR-069's precedent (`ROADMAP.md:146`'s "needs a `UShortVar` + `ushort` narrowing, not a `MarshalAs`" is wrong on both halves: the Kotlin side already emits `KChar` = `unsigned short`). Verified additive: `contractHash`/`slotCount` are reverse-bridge-only, no forward hash exists, and the shim and the `.dylib` ship in one `.nupkg`. Fixture flips ADR-097's asserted-absent `MoodBoard.logSpans(List<Short>)` cell to a working one and adds one `Readings` class. Deferred at the time: bare `Char?` (crashed `packNuget`, needed ADR-079's has-value fan-out) — closed by this ADR's 2026-09-22 amendment, which binds `Char?` at every ordinary position via a `ushort`-shaped `valueOut`/has-value pair. Lone surrogates (do not round-trip, new ROADMAP item) and nested collections stay deferred
- [099](099-nested-collection-components.md): Forward, nested collection components (Accepted): a nested collection component (`List<List<String>>`, `Map<String, List<Mood>>`, `Set<List<String>>`, arbitrary depth, every outer/inner kind combination) crosses as the inner collection's own native handle, in the same pointer-shaped component slot every other component already uses; no new native export, no ABI change. `componentWireCsharpType(Collection) = "IntPtr"` and `componentWireExpression` recurses through the existing `collectionCreateArgument`, so `CreateList<IntPtr>(rows.Select(x => CreateList<string>(x)))` composes for free with ADR-081/083/097. `Wrap<T>` gains an `IntPtr` identity branch plus an `out bool owned`, and each of the three factories disposes the element box after `Add`/`Put` only when it owns it (an `INugetHandle` component's `Wrap` return is the wrapper's own live handle, never owned, or `List<Patient>` use-after-frees); this closes `ROADMAP.md:144`'s happy-path box leak for scalars and nested handles alike as a side effect of answering the ownership question, and rejects `ROADMAP.md:143`'s Kotlin-side-dispose fix as unsafe for the same reason. The read side gets three new `finally`-guarded helpers, `ReadList`/`ReadSet`/`ReadMap`, which also close the shipped bind-then-throw landmine where a nested *return* (`fun grid(): List<List<String>>`) bound and compiled but threw `NotSupportedException` out of `FromHandle<T>` at the first element read. `isWrappableComponent()` gains a recursive `Collection` arm, widening the gate everywhere it is consulted (callable input, `Map`/`Set` positions, constructor parameters, and collection property setters, so `var grid: List<List<String>>` becomes settable for free); its `Nullable` arm gets a deliberate guard so `List<List<String>?>`, a nullable *nested collection*, stays a named `SKIPPED_UNSUPPORTED_INPUT` rather than binding with a write projection that has no null arm, while a nullable *leaf* under nesting (`List<List<String?>>`) is admitted. `ROADMAP.md:142` (result handle leaks if materialization throws mid-loop) is not closed, but is no longer reproduced per nesting level, since every inner level now goes through a `finally`-guarded helper; the outer loop is unchanged.
- [100](100-forward-diagnostic-delivery.md): Forward, diagnostic delivery (Accepted): every forward `SKIPPED_*`/`INFO_*` diagnostic was already computed by ADR-064's sink but reached nobody, verified by execution on two independent counts: the KSP stdout transport never reaches the Gradle console (even KSP's own `loaded provider(s)` line is absent at `--info`, a Worker API stdout-attribution gap, gradle/gradle#21395), and a normal `packNuget` reports the KSP task `FROM-CACHE` then `UP-TO-DATE` on consecutive runs, so a transport that only speaks during a task action is silent on every incremental build. Fix: the processor writes `NugetDiagnostics.json`, each already-rendered `ForwardDiagnostic.format()` message verbatim, as a declared KSP task output via `codeGenerator.createNewFile(...)` (so it is *restored* on a cache hit, where no task action runs at all), and a new never-up-to-date `NugetReportDiagnosticsTask` ahead of `packNuget` re-emits every line through Gradle's own `Task.logger.warn`, the same call the reverse direction already makes. No change to which declarations are reported or their severity; delivery only. Tested by a real build run twice (`scripts/verify-forward-diagnostics.sh`), since the existing Tier 1 assertion proves only the producer (`Tier1Harness` injects its own `RecordingKSPLogger`, bypassing the production transport entirely).
- [101](101-unexported-supertype-skip.md): Forward, unexported supertype skip (Accepted): an exported class implementing an interface outside the export set (GitHub #42, Koin's `KoinComponent`) rendered `public class Api : IKoinComponent` with no `IKoinComponent` ever generated (CS0246), because `CirClassTranslator.kt:58-67` prefixes `I` onto every `ClassKind.INTERFACE` supertype with no export-set check. Fix, as shipped: filter `cls.superTypes` against the qualified-name-keyed `exportedTypes` (built at `CirTranslator.kt:74-84`, verified) before prefixing, and emit a new `SKIPPED_UNEXPORTED_SUPERTYPE` at the kept class through the same `ForwardDiagnosticSink.emit(listOf(ForwardDiagnostic(...)), logger)` shape `translateClass` already uses at `:981`. The hint does not reuse `UNEXPORTED_DEPENDENCY_TYPE.diagnosticHint()` as first drafted: `include(...)` cannot admit a supertype-only interface (the ADR-066 closure has no `superTypes` edge), so the shipped hint says so directly instead of pointing at a fix that would not work. The class's own members still bind, and `ForwardClassMembership.kt`'s predicate binds the interface's defaulted members on the class too (verified for in-module interfaces and for a klib-origin interface, the shape the Tier 1 test added coverage for), and `CirClass.interfaces` has exactly one consumer (`CirClassRenderer.kt:195`), so the dropped entry is the whole C# fix. Rejected: an empty marker interface (mints a public type in the wrong namespace for a declaration the author excluded) and widening the ADR-066 closure to supertypes (drags the framework in; `include(...)` is the escape hatch). Amended 2026-09-05: the base-class hole is closed the same way. `forwardSuperClass(exportedTypes)` gates on the export set (verified identical between the translator's `exportedTypes` and the planners' `exportedObjectHandles`), all four readers pass it, the translator alone emits `SKIPPED_UNEXPORTED_SUPERTYPE` through a shared `keepsSupertype` helper keyed by a private `SupertypeKind { INTERFACE, BASE_CLASS }` with a base-specific hint, and the dropped base's public members bind on the derived class. The load-bearing claim that KSP's `getAllFunctions()`/`getAllProperties()` surface a klib base class's members is now Verified by execution, not just by the fixture's Tier 1 red
- [102](102-aot-safe-forward-callbacks.md): Forward, AOT-safe forward callbacks (Accepted): replaces every `GCHandle`-pinned delegate + `Marshal.GetFunctionPointerForDelegate` on the forward callback surface (per-call lambdas ADR-036, stored callbacks ADR-037, interface bridges ADR-039/084, `Flow`/`StateFlow` ADR-026/065, suspend ADR-019/020) with ~16 shared per-signature `[UnmanagedCallersOnly]` static thunks whose ctx is the GCHandle of the existing generated closure, dispatched through the `userData`/`ctx` pointer every callback ABI already echoes (re-verified this session in generated mingwX64 `CNameExports.kt`/`Interop.cs`, all five shapes; all 16 delegate signatures already blittable). Zero Kotlin-side/ABI changes; no `[ModuleInitializer]` registration (deliberate deviation from a literal ADR-041 port: C# hands the constant `&Thunk` address inside each C#-initiated call). Motivated by the verified Mac Catalyst full-AOT `ExecutionEngineException` on first `Flow` collection; deletes the `MtouchInterpreter` mitigation and covers NativeAOT (mechanism spiked green under `PublishAot=true` win-x64). Defines the AOT publish smoke lane (plain console app, `PublishAot=true`, exit-code assert in ci.yml, both win-x64 and osx-arm64) closing ADR-038 step 4. Decided at the gate: every thunk body catches and calls `Environment.FailFast`
- [103](103-duration-mapping.md): Forward, `kotlin.time.Duration` → `System.TimeSpan` (Accepted): the second known-scalar branch ADR-076 deferred, a `BridgeType.Duration` that clones `BridgeType.Instant`'s row at every seam and wires as one `INT64` of `TimeSpan` ticks (signed, full `Int64` domain, so C# → Kotlin is total and never throws, unlike Instant's non-negative wire). Two Duration-specific findings, both konanc-2.4.10-spike-verified against the real 2.4.10 stdlib and confirmed by a real KSP round trip: Duration is a `value class` over a discriminator-bit `Long`, so the classifier check must sit at `ForwardBridgeTypeClassifier.kt:99` **before** the `isValueClass()` branch at `:117` (which without the fix mints a broken `private rawValue` binding that fails to compile, not a silent skip), and Duration's finite ms band (±146M years) *exceeds* `TimeSpan`'s ±29,228 years, the reverse of the backlog note's claim, so the Kotlin-side helper needs a range `require` plus a boundary-second frac guard whose absence provably wraps `Long` silently (`...685s + 478ms` → negative raw). `Duration.INFINITE`/`NEG_INFINITE` throw via `errorOut` (rejecting `toJavaDuration`'s saturation precedent, whose destination holds `Long.MAX` seconds, and `Timeout.InfiniteTimeSpan`, a magic -10000 ticks). Precision contract: 100ns truncation out of Kotlin; into Kotlin, exact within ±146 years, silent 1ms granularity beyond, matching `Long.nanoseconds`' own documented banding
- [104](104-reverse-thunk-error-channel.md): Reverse, managed-exception error channel (Accepted): every user-code reverse `[UnmanagedCallersOnly]` thunk gains one trailing `IntPtr* errOut`, catches `Exception`, writes `GCHandle.ToIntPtr(GCHandle.Alloc(ex))` and returns `default`; the generated Kotlin stub allocates the slot, checks it before touching the return value or any struct out-pointer, and throws a single `NugetManagedException(managedType, message)`. Reads type/message through two new `nuget_runtime_register` slots (3 → 5) and frees the handle with the already-registered `freeGcHandleFn`. Neither shipped error envelope inverts: ADR-024 and ADR-087 are both Kotlin-owns-the-envelope read via `DllImport`, and Kotlin can only reach managed code through a registered pointer, so no C# → Kotlin envelope exists today. Chosen over two flat string out-params because the three deferred Phase 11 items (type mapping, stack trace, `InnerException` chain) then land as accessor slots only, never re-touching thunk arity; the cause chain cannot be flat at all. Load-bearing: `contractHash`'s input string has no version tag (verified `RirBridging.kt:878-947`), so the arity change is invisible to ADR-054 and the hash **must** gain a `reverse_v2:` tag, mirroring ADR-087's `kotlin_bridge_v2:`, or a stale shim registers cleanly and the channel then either silently vanishes (old shim, extra arg ignored under cdecl) or writes a GCHandle through an undefined pointer on the throw path (new shim, old dylib). Nothing spiked; every mechanism piece is composed from shipped repo code. `MenagerieRoundTripTests.cs:301` does not pass on the channel alone (its type and stack-trace assertions need the deferred items); recommended to relax it. Forward Phase 7 callbacks want the identical shape
- [105](105-sealed-property-position.md): Forward, sealed types at property positions (Accepted): closes GitHub #54 (`val shape: Shape?` skipped as `SKIPPED_UNSUPPORTED_PROPERTY`) and the #52 follow-on (`List<Shape>` property skips named). Spike-verified against a real `packNuget` + `GeneratedBindingsCheck` build: the Kotlin side is already right (one `StableRef.create`, null in-band, `shape_get_type` reads `asStableRef<Shape>()`), a `List<Shape>` property already compiles through `NugetMarshal.FromHandle<Shape>` and the issue-#40 `viaFromHandle` factory, and the *only* defect is `ForwardCirPropertyProjection.kt:227/258` spelling `new Shape(nativeResult)` on the ADR-009 abstract base (exactly two CS0144s, nothing else). Decision: the classifier keeps minting `SpecializedProtocol("sealed helper ...")` for every position but now carries `sealedHandle = ObjectHandle(..., viaDiscriminator = true)`; the property planner unwraps it (recursing through `Nullable` and `Collection` components) and the projection spells `Shape.FromHandle(nativeResult)`, the same idiom the shipped top-level sealed return uses. Five files; the fifth is a one-line `isWrappableComponent` gate so a `MutableList<Shape>` property plans get-only (existing ADR-075 read-only diagnostic) instead of opening the never-exercised sealed collection write path. Rejected: rendering every `ObjectHandle` getter via `NugetMarshal.FromHandle<T>` (dictionary hop, runtime instead of compile-time failure for abstract non-sealed), a new `BridgeType` variant (duplicates every `ObjectHandle` arm), and a classifier-level flip (double-emits top-level sealed returns against `FunctionExports.kt`, drags ROADMAP line 32 in). Deferred: sealed collection components at return/parameter positions (different route; and `fun x(): List<Shape>` appears to drop *silently* today via `SEALED_PROTOCOL`, contrary to the ROADMAP line, inferred from reading), value class over sealed at a property, and the mutable-collection setter
- [106](106-uuid-mapping.md): Forward, `kotlin.uuid.Uuid` → `System.Guid` (Accepted): GitHub #56 part 3, a third known-stdlib type alongside `Instant`/`Duration`, but over the RFC 9562 hex-dash **`String`** wire rather than a new two-`INT64` binary shape, since `Uuid` is 128 bits and does not fit any existing scalar slot; the string wire is exactly ADR-103's file list with `BridgeType.String` as the neighbour row and zero new plan shapes, the human-chosen narrowness winner over the binary alternative (recorded in full under Alternatives with its .NET spike evidence). Binds at property (get and set), constructor parameter, method parameter, method return, top-level return, and the static (`object`) route; `Uuid?` rides the null-pointer sentinel, not a has-value channel, since a `String?` slot already carries "absent" for free. Exact and total in both directions (`Uuid.toString()`/`Uuid.parse()` and `Guid.ToString()`/`Guid.Parse()` are both lossless round-trips over the same 36-character form), `Guid.Empty` ⇄ `Uuid.NIL`. As shipped, the fixture object is `MicrochipRegistry`, not the `ChipRegistry` this ADR's prose names (already taken by the ADR-069 fixture in the same package). Deferred: `List<Uuid>` and other collection components, `Flow<Uuid>`, `Uuid` as an extension receiver
- [107](107-throwable-property-mapping.md): Forward, `Throwable`-typed properties as a constructed, unthrown `System.Exception` (Accepted): GitHub #56 part 2, a property declared `kotlin.Throwable`/`Throwable?` or a stdlib subtype rides the same error envelope a *thrown* exception already crosses on (`buildError`/`NugetErrorNative.BuildException`), so the getter returns `NugetErrorNative.BuildException(nativeResult)` instead of throwing; static type is `System.Exception?` (or non-nullable `System.Exception`), always in fact an `IKotlinException`, never identity-preserving (a fresh object per read). Binds at a property getter only, on both an ordinary exported class/data class (planner route) and a **sealed subclass** (legacy ADR-009 route, human decision 1); binds get-only, since C# cannot mint a typed Kotlin `Throwable` to satisfy a setter. As shipped: the fixture sealed class is `Issue56LoadState`, not `LoadState` (a same-named sealed class in another package crashes KSP, a Phase 4 ROADMAP item); the "Files touched" checklist missed `ForwardPropertyPlanner.isPlannable`'s `else -> error(...)` arm; the refused setter's diagnostic needed a new `ForwardDroppedPropertySetter.reason` field rather than reusing the ADR-075 collection wording; and the classifier's supertype walk is gated to klib/stdlib-origin declarations only, so a module-local non-exported `Throwable` subclass keeps skipping named, and an `expect`/`actual` pair actualized onto a `Throwable` keeps ADR-074's skip rather than silently mapping. Deferred: `Throwable` at a method return or parameter position, `List<Throwable>`
- [108](108-result-return-mapping.md): Forward, `Result<T>` at return positions unwraps with `getOrThrow()` (Accepted): GitHub #56 part 1, an ordinary (non-suspend) callable returning `kotlin.Result<T>` lowers to `T` in the planner before the return shape is looked up, and the Kotlin emitter appends `.getOrThrow()` inside the export's existing `try`, so `Result.failure(e)` is indistinguishable at the boundary from `throw e` and rides the shipped ADR-024/028/029 exception channel unchanged; `Result<Unit>` → `void` (proves the rewrite happens before the emitter's `(call.result == VOID) == (publicSignature.result == Unit)` guard). Zero wire change, zero C# projection change: the public return type was already `plan.publicSignature.result`. A `Result<T>` whose payload `T` has no return shape of its own (sealed base, `Flow`, unexported type) falls back to the original `Result`'s own `VALUE_CLASS` named skip rather than silently adopting `T`'s reason, the load-bearing fallback rule that keeps a `droppedFromCSharp = false` legacy deferral from silently dropping the method. Deliberate semantic loss, human-confirmed: a C# caller cannot distinguish a modelled `Result.failure` from an unexpected throw; a non-throwing `Try`-style pair is deferred, additive later (Alternative 2). As shipped, correction to this ADR: a `Result` at a parameter position skips as `SKIPPED_UNSUPPORTED_TYPE`, not `SKIPPED_UNSUPPORTED_INPUT` as the Diagnostics section states (`ForwardPlanSkipReason.VALUE_CLASS` maps position-independently). Property, parameter, value-class-own-member, and `suspend fun` return positions keep their existing named skip (the last is a separate silent-emit defect, tracked in ROADMAP Phase 6, not fixed here)
- [109](109-duplicate-type-hazard.md): Forward, duplicate-type hazard across two published packages (Accepted): when this module's ADR-066 export closure admits a dependency-module type whose package another forward publisher in the same Gradle build also exports, the processor now emits `WARNING_DUPLICATED_DEPENDENCY_TYPE` (verb "Duplicating", not the severity-keyed "Skipping": nothing is skipped, the type still exports), once per admitted type per covering publisher. Mechanism: the plugin registers a lazy `nuget.publishedScopes` KSP option (`Provider<String>` via `KspExtension.arg(String, Provider)`, resolved after every project is evaluated so no `afterEvaluate` cross-project read or `evaluationDependsOn` circularity is needed), walking `rootProject.allprojects` for every publisher and encoding each one's ADR-063 predicate as `<packageId>:<include1|include2>:<exclude1|exclude2>` entries `;`-joined, self included; the processor (`forward/ForwardPublishedScope.kt`'s `PackageScope`/`PublishedScope`/`parsePublishedScopes`, `NugetContext.publishedScopes`) drops the self entry by `nuget.namespace` and matches each admitted cross-module type against the rest **by package** (a klib declaration carries no module identity). ADR-066's "shared models NuGet" remedy is retracted: two published packages are two native libraries with separate Kotlin runtimes, so handles cannot cross between them, leaving one umbrella publisher or `exclude("<pkg>")` as the only structural remedies. Covers both hazard shapes (a dependency that itself publishes; two sibling publishers over a shared non-publishing dependency, the latter a heuristic since only the other publisher's own KSP run knows whether its API reaches the type). As shipped, corrections to this ADR: `PublishedScope.covers` takes `(packageName: String, qualifiedName: String?)`, not a `KSClassDeclaration`; the Tier 1 fixture's other publisher is named `OtherLib`, not `DepModels`; the malformed-entry cell lives in the unit-level `ForwardPublishedScopeTest.kt`, not the KSP-round `Tier1DuplicatedDependencyTypeTest.kt`. `KspExtension.getArguments()` exposing a resolved Provider-backed option is now Verified (KSP 2.3.10 source, `arguments get() = apOptions.get()`); the option's **delivery** through KGP/KSP is Verified by a temporary real-build probe (removed after use), but its **timing** claim (resolved only after every project is evaluated) stays Inferred, priced as a ROADMAP item for a two-publisher fixture. Known gap, documented rather than fixed: a publisher with an empty effective include (no `rootPackage`, no `include`) cannot be matched and stays silently unwarned. Pre-existing, not fixed: the `SKIPPED_UNEXPORTED_DEPENDENCY_TYPE` hint's `include(...)` advice contradicts this ADR's `exclude(...)` remedy for the same type (ROADMAP Phase 2)
- [110](110-top-level-function-pascal-case.md): Forward, top-level functions PascalCase in C# (Accepted): the last camelCase position (`Arithmetic.add`) joins every other route (`Arithmetic.Add`) by changing `topLevelEntry`'s `publicName` plus the two legacy routes (`translateFunction`, `translateGenericFunction`); native `@CName` exports, symbols and ADR-095 numbering untouched. Breaking in 0.6.0 (about 390 call sites in `IntegrationTests`); the two collisions PascalCase newly creates diverge from the Proposed plan: `val name` + `fun name()` (CS0102) fails the build (`ERROR_CSHARP_NAME_COLLISION`), `fun greeting()` in `Greeting.kt` (CS0542) renames the file class to `Kt` instead of skipping (`INFO_FILE_CLASS_RENAMED`); a third shape, CS0119 on a sealed-returning function named like its own return type, needed a `global::`-qualification fix. Amended 2026-09-20: an `object`'s own `val`/`var` properties now bind as static properties on its static class too (a new `ForwardPropertyPosition.OBJECT`, projected into `CirObject`'s existing member list, no CIR change), with a `const val` file-anchored lookup fix, inherited-member flattening (properties and methods), a `SKIPPED_UNEXPORTED_SUPERTYPE` warning per dropped supertype regardless of its own export status, a fatal `ERROR_CSHARP_NAME_COLLISION` for a property/method name clash, and a named `SKIPPED_UNSUPPORTED_PROPERTY` skip for a `Flow`/`StateFlow`/lambda-typed property (which also closed the same silent drop, pre-existing, on a companion or top-level property)
- [111](111-sealed-subclass-properties-on-the-property-plan.md): Forward, sealed-subclass properties move onto the ADR-062 property plan (Accepted): the hand-rolled ADR-009 property loop in `SealedClassExports.kt`, `translateSealedClass` and `CirSealedRenderer.kt` is replaced by `ForwardPropertyPlanner.sealedSubclassProperties` (mirrors `classProperties`, `superClass = null`, export names keep the `${sealed}_${sub}_get_x` prefix via `getExport`/`setExport`) plus the shared `addForwardPropertyPlanExports` / `ForwardCirPropertyProjection.classProperty` / `propertyNativeImports`, looked up by `propertyFor("$sub.$prop")` with a residual legacy branch for lambda properties only. Closes four reading-verified legacy bugs in one deletion (ROADMAP Phase 3 lines 26-29: `.ordinal` on `Enum?`, nullable-reference getter called twice leaking a `StableRef`, `bool` imports without `MarshalAs(I1)`, every non-List getter swallowing the error slot with `out _`) and the `isReferenceType` skip that dropped Instant/Duration/Uuid/value-class/interface properties on a sealed subclass. Rejected: four point fixes (a fourth `checkedGetter` copy, skip list stays) and a C#-only migration (two halves planned by different code, `_has_value` naming special-cased). Consequences: presence export renames from `{prefix}_get_x_has_value` to the plan's bare `{prefix}_get_x`, `var` gains setters, six Tier 1 tests respell, `SEALED_CLASS` stays a legacy route for discriminator/dispose/data methods, no ABI double count by construction (`csharpLegacy` drops `ordinaryNames`). One inferred claim that would break silently if wrong: `isForwardPlannableMemberOf(sub, null)` must admit base-declared inherited properties
- [112](112-sealed-interface-mapping.md): Forward, sealed interface takes the ADR-009 sealed-class route (Accepted): an *eligible* sealed interface (non-generic, every subclass a nested `CLASS`/`OBJECT` with no other superclass, no sub-interfaces) now enters `rootSealedClasses`, leaves `rootInterfaces` and the closure's `INTERFACE` bucket, and renders exactly as a sealed class (`public abstract class Pulse`, nested `sealed` subclasses, `_get_type`, `FromHandle`, every ADR-105 position for free); `IPulse` is never declared. `isSealedSubclass()` recognises an eligible sealed-interface parent, so its nested subclasses stop vanishing as `SKIPPED_NESTED_DECLARATION`. An ineligible one keeps today's behaviour (`IMixed` interface route, `SKIPPED_SEALED_POSITION` at every member position) plus a new `SKIPPED_INELIGIBLE_SEALED_INTERFACE` naming the reason. All four ledger claims held on implementation; an ineligible parent's subclass initially also got `SKIPPED_NESTED_DECLARATION` on itself, two warnings for one hierarchy, since fixed by the 2026-09-10 amendment below (`isArmOfIneligibleSealedInterface()`), which suppresses the arm's own warning so the parent's is the only one. Breaking: the flat `IShape`/`IPulse` name disappears for any consumer that referenced it
- [113](113-interface-declaration-on-the-forward-plan.md): Forward, generated C# interfaces are projected from the forward plan, not Kotlin simple names (Accepted): issue #112, `packNuget` was green but the generated `Interop.cs` failed to compile (CS0102, CS0738 x2, CS0535 x2), because the interface route spelled every member's C# type as a bare `IntPtr` object-handle guess rather than reading each member's real bridge type, so an implementing class's actual wrapper type (e.g. a value-class or collection projection) never matched the interface it claimed to implement. Fix: `translateInterface` builds its member list from `ForwardCallablePlanCatalog.propertyFor`/`planFor` (the same symbol scheme the ADR-040 backing class already uses), so a member's C# type comes from `plan.type.csharpType()`/`plan.publicSignature`, the same source the class route itself reads. Plans every exported interface's declaration (a second, declaration-only catalog) while keeping the emitted `foo_*` exports reachability-driven, unchanged. A skipped member is omitted from `IFoo` silently, since the implementing class's own route already reports the skip; a new CS0102 property/method name-collision guard is interface-route only, the ordinary class route stays unguarded (ROADMAP Phase 4)
- [114](114-collection-parameters-on-legacy-flow-and-suspend-routes.md): Forward, collection parameters on the Flow/StateFlow and suspend legacy routes (Accepted): GitHub #109, a `List`/`Set`/`Map` parameter on either legacy route generated Kotlin that did not compile (`types: List` with no type argument). Fix: the parameter crosses as a `COpaquePointer` handle to the same boxed wire container the synchronous route already uses; Kotlin converts it eagerly, before `launch`, and C# creates the handle immediately before each native call and disposes it immediately after that call returns, never holding it live across the callback boundary. Rejected: a `kref`-versus-`IntPtr` crossing that spike-verified as compiling but unmarshalled, a silent memory-safety hazard traded for a loud build failure, named explicitly so a future implementer does not reach for it
- [115](115-opt-in-marker-declarations.md): Forward, `@RequiresOptIn`-marked declarations are out of the exported surface (Accepted): issue #113. A declaration carrying a `@RequiresOptIn`-meta-annotated marker, at any `RequiresOptIn.Level`, is never part of the forward-exported surface and is skipped with a named `SKIPPED_OPT_IN_MARKER` diagnostic rather than being exported and forcing every C# consumer to accept the Kotlin author's own experimental-API contract sight unseen. An `exportMarkers(...)` escape list for a library whose public API sits entirely behind one experimental marker is deferred (ROADMAP Phase 3)
- [116](116-sealed-subclass-methods-on-the-callable-plan.md): Forward, sealed-subclass methods move onto the ADR-062 callable plan (Accepted): issue #115, ROADMAP Phase 3 line 39. A public method a sealed subclass itself declares (including its own `override fun`), on any arm kind, now plans and exports the same way an ordinary class method does (`${sealed}_${sub}_${name}[_n]`, overloads, default-argument omitting overloads, every parameter/return shape), declared-only: an inherited base `open fun` body an arm does not override renders on no arm. A `suspend`, `Flow`-returning, lambda-parameter, or generic method on an arm has no legacy route to fall back on, so it is a new named skip, `SKIPPED_UNSUPPORTED_COMBINATION` (`SEALED_SUBCLASS_UNROUTED`), instead of the prior silent absence; binding the `suspend` case was deferred to a follow-up (closed by [ADR-118](118-suspend-route-sealed-arm-owners-and-overload-numbering.md)). Rejected: re-keying the legacy class-method/suspend routes to sealed subclasses (wrong export prefix, wrong C# node type, re-opens the one-type-two-classes duplicate issue #54 closed); a fourth hand-rolled sealed method loop (ADR-111's own rejected alternative, repeated). Amended by ADR-118 (2026-09-09): the `suspend` deferral is lifted, `SEALED_SUBCLASS_UNROUTED` no longer covers `SUSPEND`; the property/Flow case is closed by [ADR-124](124-flow-route-sealed-arm-owners.md), and a further amendment (2026-09-11) re-keys the per-call lambda-parameter route to arms too. Amended 2026-09-13: the stored-callback (ADR-037) and interface-bridge (ADR-039) `addX`/`removeX` pair also re-keys onto the arm's own export prefix, so `Job.Running.AddTicker(Action<string>)` binds the same `IDisposable` subscription `Cat.AddMoodListener` does; `SEALED_SUBCLASS_UNROUTED` now covers only a generic method and a `suspend` lambda parameter, neither of which has a route on an ordinary class either
- [117](117-forward-abi-collision-names-owning-declarations.md): Forward, ABI entry-point collisions name the owning Kotlin declarations (Accepted): issue #106. When two Kotlin declarations derive the same C entry point (two same-simple-name classes in different packages, a user `fun dispose()` beside the generated `Dispose`, two `suspend` overloads), `packNuget` used to abort with a raw `IllegalArgumentException` naming only the mangled symbol (`radio_play_collect`). Now it fails with a named `ForwardDiagnostic` `ERROR_C_ENTRY_POINT_COLLISION`, pointing at the first owner's own Kotlin source and naming every owning declaration as `pkg.Owner.member(ParamTypes)` with `file:line` per owner: fine (declaration-level) for every plan-routed export and the suspend legacy route via a KotlinPoet `FunSpec` tag resolved against the plan catalog or held directly; coarse (class-level) for the remaining legacy routes (sealed, Flow, generic, the generated `Dispose`) via a `members`-range attribution in `generateCNameWrappers`, since their raw-text C# imports carry no owner to tag. All three duplicate guards (`assertMatches`'s two arms, `csharpLegacy`'s conflicting-imports arm) share one message body. Still fails safe: the round returns before `CNameExports.kt` is written. Does not fix the unqualified export-prefix scheme itself (see [backlog](../backlog/two-exported-types-same-simple-name-different.md)); the hint's "rename one declaration" is the interim remedy
- [118](118-suspend-route-sealed-arm-owners-and-overload-numbering.md): Forward, the legacy suspend route gains sealed-arm owners and overload numbering (Accepted): issue #115 and ROADMAP line 54 in one change. Two `suspend` overloads on a class or a sealed arm are one C# overload set, the second taking `${prefix}_${name}_2_async` / `Native_${Name}_2Async` off `ForwardCallablePlanCatalog.overloadSuffix` read at all three composition sites (`SuspendFunctionExports`, and both C# projections, which are extracted into one `suspendMembers` function); the suffix reaches `CirDllImport.entryPoint`, `CirDllImport.name` **and** `CirMethod.nativeName`, since numbering only the first two binds a second overload's body silently to the first overload's extern. A `suspend fun` a sealed arm **declares** binds as `Task<T> ${Name}Async` under the arm's export prefix, with the arm carrying its own `_scopeHandle`, `GetOrCreateScope()`, `IAsyncDisposable` and `DisposeAsync` (the base stays `: IDisposable, INugetHandle`); declared-only on all three halves, so a base `open suspend fun` no arm overrides binds nowhere. `SEALED_SUBCLASS_UNROUTED` no longer covers `SUSPEND`; `Flow`, lambda-parameter, generic and `SUSPEND_CALLBACK_PROTOCOL` arm members still do. Top-level `suspend` overload numbering stays a ROADMAP line
- [119](119-collection-returns-on-the-legacy-suspend-route.md): Forward, collection returns on the legacy suspend route (Accepted): issue #122, a `suspend fun` returning `List<T>` rendered `Task<List>` / `TaskCompletionSource<List>` / `new List(resultPtr)` off the return's simple name, CS0305 in the consumer's build while `packNuget` stayed green, newly reachable on a sealed arm since ADR-118. [ADR-114](114-collection-parameters-on-legacy-flow-and-suspend-routes.md)'s return-side twin: a `suspend` return is classified `Plain` / `Marshalled` / `Refused` with the planner's own `isBridgeableComponent()` admission; a marshalled `List`/`Set`/`Map` is pinned per-element-projected exactly as the ordinary route's result (`collectionResultProjection`) and read back through `NugetMarshal.ReadList`/`ReadSet`/`ReadMap` (the same `nuget_list_*` helpers the property route uses) into `Task<IReadOnlyList<T>>` spelled by `forwardPublicCsharpType()`, so both spellings on one class agree; every other generic return (`Pair`, `Result<T>`, `Flow<T>`, `List<T>?`, a user generic) is absent and named `SKIPPED_UNSUPPORTED_RETURN` instead of emitting non-compiling C#, which closes the ADR-108 `Result` probe cell. Also closes a gate gap ADR-118 opened: a sealed arm's declared suspend members never reached the `needs*Support` helper walk, so an arm-only collection would have called `nuget_list_count` against a library that never exported it. Rejected: `Task<object>`/`Task<IntPtr>`/non-generic `IEnumerable` (a second spelling for one Kotlin type), refuse-everything, moving the route onto the callable plan
- [120](120-live-stableref-counter-and-leak-harness.md): Forward, a live `StableRef` counter and leak harness (Accepted): the KSP processor emits one `NugetHandles` object (`kotlin.concurrent.AtomicLong`, opt-in-free on K/N 2.4.10 by konanc spike) with a `retain`/`release` pair that every generated `StableRef.create(...).asCPointer()` / `asStableRef<T>().dispose()` routes through (5 emitted shapes, 14 renderer files), plus one export `nuget_live_handles` read from C# as `internal static long NugetMarshal.LiveHandles`, beside `Native_dispose`; the ADR-055 contract picks the pair up by name with no registration. A new `LeakTests/LiveHandleTests.cs` (its own process, moved out of `IntegrationTests/` per Amendment 2, 2026-09-09) snapshots, runs N crossings, settles with `GC.Collect()`/`WaitForPendingFinalizers()`/`NugetBridge.GcCollect()`, and asserts the count is back, one fact per crossing family; its red case swaps a throwing factory into the mutable `NugetMarshal.Factories` dictionary to make the outer returned-collection loop throw mid-materialization, proving the still-open Phase 4 list-handle leak before the harness is trusted. Forward-only: the plugin's 10 reverse-side ADR-085 mints are a priced, deferred scope fork; no `NUGET_RUNTIME_CONTRACT_HASH` change
- [121](121-kotlin-object-collectability-after-last-dispose.md): Forward, Kotlin object collectability after the last dispose (Accepted): a `Morgue` fixture in `test-library` holds one `kotlin.native.ref.WeakReference` (needs `@OptIn(ExperimentalNativeApi::class)`, konanc-verified) over a forward object; C# disposes the wrapper, drives the existing `NugetBridge.GcCollect()` in a polled loop, and asserts `Morgue.IsAlive()` goes false. Two shapes: a plain `Cat`, and a `CatEventSource` watched across the stored-callback closure seam, plus a held-token negative control. Spike showed a disposed `StableRef` is only collectable after a GC, and only when no frame still holds the pointer, so each step is its own P/Invoke and the assertion is eventual.
- [122](122-handle-parameters-on-the-legacy-routes.md): Forward, handle parameters on the legacy Flow and suspend routes (Accepted): issue #126, a sealed-arm/sealed-base/ordinary-class parameter on a `Flow`-returning or `suspend` member rendered a public `Watch(IntPtr observation)` while the Kotlin `@CName` export declared the real Kotlin type, so five overloads collapsed onto one C# signature (CS0111) and even a single one was uncallable (`internal` handle constructors are the only source of that pointer). `packNuget` stayed green; the consumer's build broke. [ADR-114](114-collection-parameters-on-legacy-flow-and-suspend-routes.md)'s third application: `ForwardLegacyParameterShape` gains a `Handle` arm for anything classifying (after `sealedAsHandle()`) to `BridgeType.ObjectHandle`, with public C# type `forwardPublicCsharpType()`, native slot `IntPtr`, argument `x._handle` through a new `CirParameter.nativeArgumentExpression` (deliberately not `collectionCreate`, whose create/`finally`-dispose block a borrowed handle must never get), and a `COpaquePointer` Kotlin slot dereferenced into a prelude local **before** the `collectForCSharp`/`launchForCSharp` call (ADR-128) so the coroutine holds a strong reference across a mid-flow dispose. `Plain` narrows from "no type arguments" to "a scalar": every remaining non-scalar shape (enum, `Instant`/`Duration`/`Uuid`, value class, interface, nullable object, unexported class) is `Refused` and named `SKIPPED_UNSUPPORTED_INPUT` rather than silently emitting a public `IntPtr`, which is what makes the issue's "no public member exposes `IntPtr`" invariant true for arbitrary input; `Tier1StructuralInteropCsTest` enforces it over the whole generated file. The ABI slot moves from a `kref_<T>` struct to `void*` (verified in generated `CNameExports.kt` before/after); no ADR-055 re-baseline, because that check maps both spellings to `POINTER` and was blind to the defect by construction. Enum binding deferred, refused loudly in the meantime. Amended 2026-09-20 ([ADR-151](151-bytearray-mapping.md)): the same defect class also lived in the pre-ADR-062 legacy list/map/set *return* branches (`CirFunctionTranslator.kt`), which inspected only the return and left a refusing parameter open to a degraded public `IntPtr`; closed with a `check(...)` regression guard, pinned by `tier1/Tier1SkipMeansAbsentTest.kt`. Amended 2026-09-26 (issue #299): a nullable primitive/`Char`/`String` *parameter* on the same four legacy routes was still bound non-null; now threaded as the plan route's `${name}HasValue` pair (scalar) or a single nullable pointer (`String`), which satisfies this ADR's "nullable threading on the legacy routes is done once, or not at all" rule for every scalar and `String` (a nullable class handle stays refused). Default arguments on these routes remain unhonoured. No new `LiveHandleTests.cs` row: neither shape mints a handle
- [123](123-collection-elements-on-the-flow-routes.md): Forward, collection elements on the Flow and StateFlow routes (Accepted): issue #127, a `Flow`/`StateFlow` whose element is a Kotlin collection spelled that element `global::Demo.Kotlin.Collections.Set`, a namespace that does not exist, with the type argument dropped, CS0234 in the consumer's build while `packNuget` stayed green. `qualifiedElementCsType` (`cir/CirTypeMapping.kt:225`) runs a Kotlin builtin through `mapPackageToNamespace` as if it were a user type and never reads `type.arguments`. The parameter on the same declaration was already right (ADR-114) and the suspend return was fixed the same way (ADR-119), so this is the third application of classify-then-marshal-or-refuse, at the element position: `ForwardLegacyFlowElementShape` (`Plain`/`Marshalled`/`Refused`), `forwardPublicCsharpType()` for the spelling, `legacyCollectionRead` behind a new per-member `Func<IntPtr, T>? read` on `KotlinFlow<T>`/`KotlinFlowEnumerator<T>`/`KotlinStateFlow<T>` (trailing, optional, `internal` ctors, so every shipped member's text is byte-identical), and `collectionResultProjection` on the Kotlin emission so a value-class or enum component leaves as its wire value. The runtime half was broken independently of the spelling: `FromHandle<T>` has no collection branch (`cir/CirMarshalRenderer.kt:129`) and the Kotlin side boxed `value as Any` unprojected. Refused, named: a nullable collection element, a collection of a sealed base, every other generic element (`Pair`, a user generic, a nested `Flow`), and any collection element on ADR-068's suspend-StateFlow route, whose `nuget_stateflow_collect`/`_value` exports are module-wide and have no per-member projection seam. `qualifiedElementCsType` gains a `check` that no `kotlin.`/`kotlinx.` package reaches the user-type speller, which makes the issue's "no emitted type name mixes the root package with a kotlin package" invariant unrepresentable rather than merely tested. Amended 2026-09-20 ([ADR-151](151-bytearray-mapping.md)): `kotlin.ByteArray` had no route at the Flow element position, so a `Flow<ByteArray>` element fell through to the user-type speller and this `check` fired as a `packNuget` crash rather than a named skip; a bare `suspend fun (): ByteArray` had the mirror gap and rendered an undeclared `Task<ByteArray>` (CS0246). Both now route through ADR-151's `ByteArray` component and bare shapes
- [124](124-flow-route-sealed-arm-owners.md): Forward, the Flow route on sealed-arm owners (Accepted): issue #129, a `Flow<T>` / `StateFlow<T>` declared on a sealed subclass did not bind. The method form was named `SKIPPED_UNSUPPORTED_COMBINATION` (`SEALED_SUBCLASS_UNROUTED`, detail `FLOW_PROTOCOL`); the property form was dropped in **silence**, because `ForwardPropertyPlanner.recordDropped` returns early for a `LEGACY_ROUTED_PROTOCOLS` type on the assumption a named legacy route re-emits it, which is false for an arm. [ADR-118](118-suspend-route-sealed-arm-owners-and-overload-numbering.md)'s mirror, site for site, one route over: the flow property and flow method emitters lift out of `ClassExports.kt` into `exports/FlowExports.kt`, `flowMembers` and the flow-property branch lift out of `translateClass` (the owner's C# name is load-bearing, it is baked into `ObjectDisposedException(nameof(...))` and must name the arm), an arm loop lands beside ADR-118's under `attributing(subclass)` with the `${sealed}_${sub}` prefix, `CirSealedSubclass` gains `flowMembers` and `hasSuspendMethods` widens to "owns a coroutine scope", and `CirSealedRenderer` gains an `isFlow` property arm **before** its `usesLegacyNativeImport()` lambda arm (which would otherwise have emitted a lambda-shaped extern no Kotlin export backs) plus a `require` that makes the mistake unrepresentable. Properties are all-properties per ADR-111's `superClass = null`, methods declared-only per ADR-116, and both halves apply ADR-114/119/123's refusals, so a base-declared flow property binds on every arm while a base-declared flow method binds nowhere. A flow-bearing arm becomes `IAsyncDisposable` off one boolean, so an arm carrying suspend *and* flow members emits exactly one `_scopeHandle`. `SEALED_SUBCLASS_UNROUTED` now covers generic and callback-protocol arm members only, and the refused walk switches from a `SUSPEND` filter to `isForwardLegacyAsyncRoute()` so a refused arm flow member stays named rather than regressing to silent. Sealed interfaces (ADR-112) get the route for free through the same `sealedClasses` list, which also completes the question ADR-118 left open
- [125](125-sealed-interface-sibling-arms.md): Forward, a sealed interface's arms may be declared beside it (Accepted): issue #130 asks whether [ADR-112](112-sealed-interface-mapping.md)'s nesting rule is essential to the reconstruction or incidental to how the arms are discovered. Incidental, verified: discovery is `getSealedSubclasses()` on both sealed routes, `isNested` is a rendering flag only, and `CirSealedRenderer` has outdented a sibling arm to namespace level since ADR-009's issue-#54 amendment. Nesting was implicitly buying two guards, so the predicate change is not literally one line: a nested class has exactly one enclosing declaration (a top-level one may implement two sealed interfaces, which C# single inheritance cannot express) and a nested enum is never declared by `rootEnums`, the one root bucket without the `!isSealedSubclass()` filter its siblings carry (an admitted top-level enum arm would be declared both as `public enum Pitch` and as `public sealed class Pitch : Tone`, CS0101 in every consumer). So the nesting test goes and two named refusals replace it: an `enum class` arm (a C# enum admits only an integral base, `CS1008`, verified by spike) and an arm implementing more than one sealed interface (counted with `isSealedInterface()`, not the eligible predicate, so two interfaces sharing an arm do not ask each other for eligibility forever). `rootEnums` gains the filter anyway, unreachable and deliberately so. The cascade issue #130 reports (a refused interface takes out the constructor **and** `copy` of every `data class` holding one, since `planOrSkip` drops a whole callable on an ineligible input) disappears with its cause; the issue's option 3 (keep the interface refused but stop the cascade) is declined, because C# has nothing to pass for a required parameter of a type that has no C# spelling. Rejected: boxed enum arms with a `Value` property (priced at 6 to 8 files, deferred), an opt-in flag (nothing unsafe left to consent to), rewording the diagnostic alone. An arm's extra interfaces (`CharSequence`) stay silently dropped
- [126](126-extension-class-per-declaring-package.md): Forward, one `{Receiver}Extensions` class per declaring package (Proposed): the merged extension class for an **unexported** receiver (`String`, primitives, any stdlib type) had no home package, so `CirTranslator` took `namespaceOf(funcs.first().packageName)`, the package of whichever extension KSP visited first. Adding a same-receiver extension in another package therefore relocated every existing one: the fixture's root-package `meowify`/`isPurring` rendered inside `namespace TestLibrary.Cat` because `cat`'s `String.tag` was seen first, and the function group and property group each picked independently, so `GetWordCount` sat alone in a second `StringExtensions` class in `TestLibrary`. Both extension loops now key on `(namespace, receiverSimpleName)` and both use `mergeStaticClass`, so placement is a group key rather than a `first()` lookup and no declaration can influence another's: an unexported receiver's class lands in the *declaring* package's namespace, the rule classes, objects, enums and per-file static classes already follow (and the rule Kotlin/JVM's `org.example.AppKt`, Swift Export's per-package `extension` blocks and `System.Linq.Enumerable` all follow, inferred from docs). An **exported** receiver keeps the receiver's own namespace, deliberately unchanged and already deterministic. Rejected: one root namespace for every unexported-receiver extension (two lines, but it makes extensions the one declaration kind that ignores its package, hides a `Cat`-package extension from a `using TestLibrary.Cat;` consumer, and piles a large library's stdlib extensions into one root class). Zero wire change, verified: export names are receiver-derived (`string_meowify`), so `CNameExports.kt` is byte-identical and the ADR-055/ADR-117 checks never see this. Consumer-breaking at source level: `Meowify`/`IsPurring` move from `TestLibrary.Cat` to `TestLibrary` and `Tag` to `TestLibrary.Reserved` (upgrade note), and the fixture cell parked in `cat/` to dodge the relocation moves back beside its family in `reserved/`
- [127](127-nuget-runtime-library.md): Forward, ship the fixed `nuget_*` ABI once as an `export()`ed `nuget-runtime` Kotlin/Native library (Accepted): Phase 14 bullets 1 to 3. The ~500 declaration-independent lines of every `CNameExports.kt` (64 `nuget_*` exports in the fixture, 66 in the emitter vocabulary: `NugetHandles`, `NugetError`/`buildError`, the scalar wrap/unwrap, collection, callback, scope/job/stateflow exports, the .NET ticks conversions, `NugetCSharpBridge`) move to a `:nuget-runtime` KMP module (`macosArm64`/`macosX64`/`linuxX64`/`mingwX64`, `api` on coroutines, published like the processor) that the plugin adds as `api` and `export()`s on every `SharedLibrary`, resolved as `findProject(":nuget-runtime") ?: "io.github.xxfast:nuget-runtime:$PLUGIN_VERSION"`. Spiked, verified on Kotlin 2.4.10 `macosArm64`: a `@CName` in a dependency klib reaches the consumer `.dylib` **only** through `export()` (an `api` dependency the consumer calls is not enough), `export()` of an `implementation` dependency fails at link, and an `internal @CName` is never exported. The `needs*` gating in `NugetProcessor.kt` and its `addNuget*HelperExports` emitters are deleted, closing the `EntryPointNotFoundException` defect class behind ADR-068/071/075/124. Public surface behind a `@RequiresOptIn` `NugetRuntimeApi` marker; skew guard is the plugin pin plus a compile-time `NugetRuntimeAbi1` anchor the generator references (an ADR-054-style startup arm waits for the C# twin). The C# side is unchanged. No new DSL
- [128](128-launch-for-csharp-runtime-helper.md): Forward, `launchForCSharp` / `collectForCSharp` runtime helpers (Accepted): Phase 14 bullet 4. Four processor emitter templates (`SuspendFunctionExports.kt` top-level and method, `FlowExports.kt` property and method) and five runtime sites (`nuget_suspend_func{0..3}_invoke`, `nuget_stateflow_collect`) repeated the same `launch(ATOMIC)` / result-mint / cancel-rethrow / `buildError` shape. Two non-inline, non-generic helpers in `nuget-runtime` own it; the generated body returns the already-minted `COpaquePointer?` (or, for Flow, drives an `emit` lambda from inside its own `.collect`), so every per-route marshalling variant and the `NugetHandles.retain` order stay where they are and `LeakTests` baselines cannot move. The Flow source is evaluated inside the coroutine (a `Flow` parameter would have moved a throwing method off the `onError` path); the top-level and method routes pass their own scope; `nuget_scope_drain` (no try/catch) and `_bridge_create` (not a launch at all; the ROADMAP mention was wrong) stay out. No new `@CName`, `Interop.cs` unchanged. Shipped as PR-D (runtime + its own five sites + `nativeTest` + gated Tier 1 stub) then PR-E (generator), both verified by running, 2026-09-12: 45 `launchForCSharp(` and 47 `collectForCSharp(` sites in the fixture's regenerated `CNameExports.kt`, roughly 1,100 fewer generated Kotlin lines, zero `scope.launch(`/`CoroutineStart.ATOMIC`/`catch (e: CancellationException)` left on these routes, `NugetHandles.retain(` down from 2,012 to 1,828 (exactly the per-export job and error-arm mints the helpers now own, every result/item mint unchanged and in order), `IntegrationTests` (1,723) and `LeakTests` (28) unchanged
- [129](129-nuget-runtime-version-export.md): Forward, a 67th runtime export, `nuget_runtime_version` (Accepted): the `nuget_runtime_version` export Phase 14's `nuget-runtime` bullet ADR-127 deferred. `nuget-runtime` gains a build-time-baked `String` constant and a `@CName("nuget_runtime_version")` export on the same `const char*` wire `nuget_error_message` already uses; `ForwardAbiContract`'s runtime-name set and comment move from 66 to 67. The forward `Interop.cs` gains its first always-emitted helper outside the `needsMarshalHelper` gate, `NugetRuntime`, with a `[ModuleInitializer]` that, under the existing `NUGET_INTEROP_TRACE`/`NUGET_INTEROP_TRACEFILE` variables, prints `[nuget:interop] runtime <version> loaded from <library>` for every consumer at assembly load, not just one using the reverse bridge. The attribute is wrapped in `#pragma warning disable CA2255`/`restore` since `Interop.cs` compiles into the consumer's own library assembly. A public C# `Version` member, folding the version into an `EntryPointNotFoundException`, and the reverse trace line carrying the version (blocked on a `nativeMain`/per-target `api` visibility spike) stay deferred. Amended 2026-09-20: the `needsMarshalHelper` gate this ADR contrasted `NugetRuntime` against is deleted; `NugetMarshal`/`NugetErrorNative` are unconditional too now
- [130](130-reverse-error-envelope-on-runtime.md): Reverse, fold the reverse error envelope onto the runtime's `NugetError` (Accepted): Phase 14 bullet 5. The reverse bridge's own `NugetKotlinError` class, structurally identical to the forward runtime's `NugetError`, is deleted; a shared `internal expect fun nugetKotlinError(t: Throwable): COpaquePointer` in the generated `nativeMain` file gets a per-target `actual` in `mingwMain`/`posixMain` that mints the runtime's own `NugetError` via `buildError(t)` under `@OptIn(NugetRuntimeApi::class)`, the same seam `freeManagedString` already uses to cross the source-set boundary. A spiked alternative, moving the reverse bindings' srcDir into the per-target source set so the generated code could name the runtime klib directly, is infeasible: the reverse bindings are consumer-facing types the author's own `nativeMain` code imports, so moving them down breaks the fixture's own compile. Allocation stays a raw `StableRef`, not `NugetHandles.retain`, so `LeakTests` baselines don't move. The C# `NugetKotlinErrors` shim, the eight `nuget_kotlin_error_*` exports, and the thrown `KotlinException` family are all unchanged. Separately observed, not fixed: a latent plugin wiring bug where the reverse bindings' srcDir silently attaches to no source set when a build script never names `nativeMain`
- [131](131-suspend-route-sealed-base-return.md): Forward, a suspend route return of the sealed base itself reads through `FromHandle` (Accepted): ADR-118's amendment split this out. `suspend fun next(): Job` rendered `t.SetResult(new Job(resultPtr))` against `public abstract class Job`, CS0144, failing the whole generated `Interop.cs`. `legacyReturnShape` now gates on the declaration's `Modifier.SEALED` before its non-generic early return, classifies through `classify(type).sealedAsHandle()`, and yields a new `ForwardLegacyReturnShape.Discriminated(handle, nullable)` (an ineligible sealed type is `Refused`, named `SKIPPED_UNSUPPORTED_RETURN` instead of uncompilable C#); both C# translators read it off the same `asyncResultRead` seam ADR-119 minted, completing with `Job.FromHandle(resultPtr)` (nullable: guarded on `IntPtr.Zero` first). Covers all three suspend owners (class, sealed arm, top level) and an eligible sealed interface base (ADR-112/125) the same way. Kotlin half unchanged: it already pins the `StableRef` on the concrete arm. Not folded in: `global::` qualification (ADR-118's open item) and a suspend return of a collection of a sealed base (`List<Shape>`, ROADMAP Phase 6), a separate mapping decision
- [132](132-extension-receiver-shapes.md): Forward, every admitted extension receiver shape lowers like a parameter (Accepted): the receiver renderers (`ForwardKotlinPlanEmitter.receiverExpression`, `ForwardCirPlanProjection.extension`'s `receiverArgument`) special-cased only `ObjectHandle`, `ValueClass`, and `Nullable(ObjectHandle)`, falling through to the bare name `receiver` for every other shape the planner already admitted, an unresolved Kotlin reference and/or CS1503 for a bare `Interface` (`fun Pet.describe()`) or `Nullable(ValueClass)` (`fun CatId?.orAnonymous()`) receiver. Both `else` arms are deleted; the receiver now enters `resultProjection` as a `ForwardPublicParameter` at the head of the input list, sharing `callArgument`, all three preludes, and both cleanups with every declared parameter, so an interface receiver gets its ADR-084 stage-3 `HandleOf`/`Dispose` lifecycle for free (Row 6b in `LeakTests/LiveHandleTests.cs`). The three previously-handled shapes render byte-identically; ADR-077/105 Tier 1 pins pass unmodified. Fixture-verified: bare `Interface` (Kotlin `Cat`, an anonymous object, and a C#-implemented `Dog : IPet` all dispatch) and `Nullable(ValueClass(String))`; routed by construction, no fixture: `Nullable(Interface)`, `Nullable(ValueClass(ObjectHandle))`, `Enum`, `Uuid`, `Instant`, `Duration`, `Collection`, `BoundInterface`, and their remaining admitted nullable forms. New named skip `RECEIVER_FAN_OUT` (`SKIPPED_UNSUPPORTED_INPUT`) for the has-value fan-out receivers (`Int?`-style `Nullable(Primitive)`, `Nullable(Enum)`, `Nullable(Instant)`, `Nullable(Duration)`, `Nullable(ValueClass(Primitive|Enum))`): the plan model admits one RECEIVER-role slot at index 0, and a fan-out pair would land it at index 1, failing `validateRoles` outright (would have crashed the processor rather than mis-rendering, inferred, closed in the same change). CS1929 asymmetry verified by a standalone `dotnet build` probe: `CatId` generates a `readonly record struct`, so only the `CatId?` overload is emitted and `new CatId("x").OrAnonymous()` fails to bind from C# (a forwarding non-null overload was considered and deferred). **Amended 2026-09-14**: extension properties take the same lowering. `ForwardPropertyPlanner`'s `supportedReceiver` became one exhaustive `isSupportedReceiver()` admitting `Interface`, `Nullable(Interface)`, and `Nullable(ObjectHandle)`; the Kotlin emitter's setter-value lowering was lifted to a name-keyed `inputLowering` reused for the receiver access expression; the C# projection's `setterPrelude`/`setterCleanup`/`valueArgument` were re-keyed by name, the getter wrapped in `forwardCirHandleScope` so a C#-implemented receiver's transfer handle disposes in `finally` (Row 6h), and both `else -> "receiver"` arms deleted. `val Pet.summary`, `val Cat?.nameOrStray`, and a receiver-only `val Sitter.address` (proving the property plan's arm of ADR-135's reachability walk) are fixture-verified; `var Pet.tag` over an interface receiver is a Tier 1 compile pin only. Decided narrower than the parameter set: `Enum`, `Uuid`, `Instant`, `Duration`, and the remaining nullable spellings still skip named on the property route. **Amended 2026-09-20**: that narrowing is mostly lifted, plus a `Collection` receiver and a bound C# interface receiver (ADR-088) folded in. `ForwardPropertyPlanner.isSupportedReceiver()` now admits `Enum`, `Uuid`, `Instant`, `Duration`, `Nullable(String)`, `Nullable(Uuid)`, and `Nullable(ValueClass)` over a `String`/`ObjectHandle` underlying; `Nullable(Collection)` and `Nullable(BoundInterface)` stay refused (the former an explicit allowlist gap versus the function-receiver route, the latter ADR-088 parity). Fixture `cat/ReceiverParityExtensions.kt`, one declaration per receiver mechanism. Closed three pre-existing defects along the way: a non-`partial` `{Enum}Extensions` class for an enum with its own properties (CS0260, both routes), the `NullableDispatch` setter arm building outside the receiver's handle scope (CS0103, `var Pet.napQuota: Int?`), and `CollectionHelperTracker.trackProperty` never tracking a property's *receiver* (CS0103, `val List<String>.longestName`). The nullable string-wire receiver import is now spelled through one shared predicate, `BridgeType.isNullableStringWire()`, on both routes. `LeakTests/LiveHandleTests.cs` rows 6i/6j/6k; 6k is a smoke test, not a leak pin (the bound-interface receiver mints a C# `GCHandle`, not a Kotlin `StableRef`, so this harness cannot observe a pure `GCHandle` leak there)
- [133](133-nested-types.md): Forward, every public nested `class`/`object`/`interface`/`enum class` under a supported owner is declared as a real C# nested type (Accepted): generalises ADR-009's sealed-arm nesting and closes ADR-064's 2026-09-07 "deferred alternative." A non-generic, non-`inner` `class` or `object` owner (root or admitted dependency) now declares its children through the same translators/renderer a root declaration uses, at any depth (`Aviary.Middle.Inner`), with `@CName` prefixes carrying the whole enclosing chain (a new `nativePrefix()` replacing ~20 bare-simple-name sites) and no released top-level entry point changing. The interface `I`-prefix now attaches to the last segment only (`Outer.IListener`), with the ADR-040 backing wrapper nested beside it; a nested enum's extension class is hoisted to namespace level (CS1109). Deferred, still a named `SKIPPED_NESTED_DECLARATION`: an `inner class`, a generic, `enum class`, `interface`, or sealed owner, and a nested `value class`. New owner-scope collision cells (`ERROR_CSHARP_SIGNATURE_COLLISION`, CS0542/CS0102) and a new named skip `OBJECT_POSITION` (a member positioned with any `object`, nested or not, is CS0722) round out the change. Verify green, 1787/828
- [134](134-nested-types-under-deferred-owners.md): Forward, three more of ADR-133's deferred nesting owners are admitted (Accepted): an `interface` owner (including an ADR-112 ineligible `sealed interface`, still `public interface`), a sealed base or arm owner (including an ADR-112 eligible `sealed interface`, rendered `public abstract class`), and a nested `value class` candidate under any admitted owner, declared as a nested `readonly record struct`. `enum class`, generic, and `inner class` owners stay a permanent named skip. ADR-133's "`I` on the last segment only" interface rule is reversed to "`I` on every enclosing interface segment" (`ICage.Bar`), except an eligible sealed interface segment, which carries no `I` at all. The owner-walk gate-order hazard for an eligible sealed interface (claimed by the interface arm instead of the sealed arm, silently losing its children) is guarded and now pinned by a Tier 1 cell. No new handle kind, no new leak row; a pre-existing hazard verified in passing: a sealed arm's only constructor is `internal Arm(IntPtr handle)`, and `new Base.Arm(9)` compiles (`int` converts to `nint` implicitly) and access-violates at runtime. Verify green, 1801/834
- [135](135-interface-parameter-reachability.md): Forward, interface parameter positions join the ADR-084 bridge reachability set, and a failed `HandleOf` never reaches the dispose in `finally` (Accepted): `reachableInterfaceNames` (`NugetProcessor.kt` ~:1171-1198) walked only `publicSignature.result`/property type, so an interface reachable only at a parameter position, an ADR-132 extension receiver, or a property setter got no ADR-040 wrapper and no ADR-084 bridge plan at all; a C# implementation passed there crashed the host with an unlocated Kotlin `NullPointerException` instead of throwing. The walk now also covers `publicSignature.parameters` and the native call's RECEIVER-role ABI slots (methods and properties both), closing the gap ADR-084's own Detection rule and ADR-133's 2026-09-13 amendment had already claimed as true. Independently, `NugetMarshal.HandleOf(object, out bool owned)` assigned `owned = true` before the inner mint could throw, so a `NotSupportedException` from an interface that plans to `null` (an out-of-vocabulary member, e.g. a `var`) was overwritten by a `finally` disposing `IntPtr.Zero`; `owned` is now set only after a handle is actually minted, and both interface cleanups (`ForwardCirPlanProjection.kt`, `ForwardCirPropertyProjection.kt`) guard on `!= IntPtr.Zero`, the same guard `collectionCleanup` already had. Fixture: `nested/CatteryDesk.kt` (`Boarding.Clerk` nested, `Doorman` top-level, `Sitter` receiver-only, `ScratchLog` plans-to-null). Tests: `InterfaceParameterTests.cs` (4 facts), `LiveHandleTests.cs` rows 6c/6d, one Tier 1 cell. Deferred: a build-time named skip for the plans-to-null case (today a managed throw at call time); whether an extension **property** receiver needs the same walk has no fixture. Verify green: 1817 IntegrationTests, 38 LeakTests.
- [136](136-csharp-identity-on-async-interface-reads.md): Forward, a C#-implemented interface resolves back to its original instance on the suspend and `Flow` reads too (Accepted): `interfaceReturnExpression` (`forward/ForwardCirPlanProjection.kt`) gained a `handle` parameter (default `nativeResult`) so the one resolve-then-wrap expression the sync plan route already used (`NugetMarshal.TryResolveCSharp(...) ? csharpOriginal : new Pet(...)`) can be spelled against any local; `legacyInterfaceRead` and `legacyInterfaceElementReadArgument` (`forward/ForwardLegacyRouteCollections.kt`) now call it instead of spelling a bare `new Pet(h)`. A `Dog` stored as an `IPet` and handed back through a `suspend fun` or emitted by a `Flow<IPet>` now round-trips to the caller's own instance (`Assert.Same` holds) instead of arriving as a second wrapper over its own bridge; `TryResolveCSharp` frees the transfer handle it resolves, so the freeing site moves from the consumer's `using` to the read. No ABI, Kotlin emission, or contract-hash change. Fixture: `cat/PetSitter.kt` (`take`, `handBackLater`, `wards()`). Tests: `BidirectionalTests.cs` two `Assert.Same` facts, `LiveHandleTests.cs` rows 6e/6f, one Tier 1 cell. Deferred: the generic-function route's `FromHandle<TResult>` still constructs a fresh wrapper. Discovered alongside: a `suspend fun`/`Flow` member on a class with a Kotlin superclass generates non-compiling C# (the scope-owner emission only fires on a base-less class), unrelated to this ADR's own change, tracked on the ROADMAP. Verify green: 1819 IntegrationTests, 40 LeakTests. **2026-09-20 amendment:** the nullable arms of both legacy reads are now fixture-covered (`PetSitter.handBackLaterOrNull`/`watching`/`watchingNow`, top-level `strayPetLaterOrNull`) and compile under `GeneratedBindingsCheck`; a plain `Flow<T?>` nullable element also binds now (ADR-065's 2026-09-20 amendment), a separate fix on the same date. Verify green: `:nuget-processor:test` 968/0, IntegrationTests 2001/0, LeakTests 63/0.
- [138](138-pack-time-interop-compile-check.md): Tooling, `packNuget` compiles the generated C# before it packs (Accepted): a new `nugetCompileInterop` task (`NugetCompileInteropTask.kt`), registered in the same `afterEvaluate` block as `packNuget` and depended on by it, writes a throwaway `build/nuget-compile/interop-check.csproj` with `GeneratedBindingsCheck`'s property set plus `AllowUnsafeBlocks`, one `<Compile Include>` per file `packNuget` would stage (the KSP resources dir, plus `nugetGenerateShims`'s output when the project binds a package), and one exact-version `[v]`-pinned `PackageReference` per bound dependency, then runs `dotnet build` and fails with the compiler's text (captured from stdout, verified empty on stderr) when the exit code is non-zero. When `dotnet` is not on `PATH` it logs a warning and returns, keeping the documented "no .NET SDK to publish" promise for a forward-only author; the docs are updated to say so. Closes the ROADMAP Phase 4 item discovered alongside ADR-133: a CS0101/CS0128-class defect in `Interop.cs` no longer ships silently through a green `packNuget` until the next full verify. Deferred: an opt-in strict mode that fails instead of warns when `dotnet` is absent, `check`-lifecycle wiring, and multi-TFM compile (the check compiles `net8.0` only, matching `GeneratedBindingsCheck`'s target).
- [141](141-inner-class-outer-instance-constructor.md): Forward, a public Kotlin `inner class` is declared as a C# nested type whose constructor takes the outer instance first (Accepted): `Hearth.Sunbather(Hearth outer, int minutes)`, export `hearth_sunbather_create(outer, minutes, error)`, Kotlin body `outer.asStableRef<Hearth>().get().Sunbather(minutes)`. Closes ADR-134's Alternative 3 deferral. The receiver reuses ADR-132's extension-receiver projection (`ForwardCirPlanProjection.constructor()` now mirrors `extension()`), so a primitive-only inner constructor keeps the outer off the trivial fast path, and ADR-091's trailing-default truncation keeps the receiver by construction. The outer crosses borrowed; the inner instance's own reference keeps it alive, so disposing the outer first is safe (`InnerClass_OuterDisposedFirst_StillReadsTheOuter`). A user's own `outer`-named parameter now renames to `outer_` (`outer` joined `PLAN_OWNED_NAMES`), the same move `value` already gets; no CS0100 is reachable, correcting the ADR's own Proposed-time claim. Deferred, still a named `SKIPPED_NESTED_DECLARATION`: an `inner class` as an owner of its own nested types (inner-of-inner), an `inner class` under a sealed owner, a generic `inner class`, and an inner class of a generic outer. Fixture `nested/Inner.kt`; two new leak rows (1d, 1e, the second a fault-injection row for a throwing `init`). Verify green: 1943 IntegrationTests, 52 LeakTests.
- [147](147-generic-class-methods.md): Forward, generic classes move onto the ADR-062 callable plan with a `BridgeType.TypeParameter` kind (Accepted): a public method declared on a generic class (`Crate<T>.describe(tag: T)`) now binds as an instance method on the C# generic carrier instead of being silently absent, closing ROADMAP Phase 4's generic-base item. `translateGenericClass`, `renderGenericClass`, `exports/GenericClassExports.kt`, `CirGenericClass` and `ForwardAbiLegacyRoute.GENERIC_CLASS` retire; a generic class is now an ordinary `CirClass` with type parameters, reached through the same translator, renderer, and ABI coverage every class uses. `T` at a parameter, return, constructor parameter, or property getter (optionally nullable) crosses as the boxed handle the property getter already used (`Wrap<T>`/`FromHandle<T>`); the receiver reads back as `asStableRef<Crate<Any>>()`, one erased non-null argument per class type parameter (or the bound, for `T : Pet`). The thirteen `create_<width>`/`create_object` constructor variants collapse into one boxed `create`. `T` nested in a collection/lambda/`Flow`/value class, a `var` property's setter, `suspend`/`Flow`/stored-callback members, the method's own type parameter, a generic subclass, and an `interface`'s own type parameter all stay named skips (v1 scope). Fixture: `parcel/Parcel.kt`'s `Crate<T>`/`LabelledCrate`. Verify green: 918 processor, 1907 integration, 47 leak tests.
- [149](149-supported-arities-of-a-partially-unsupported-signature.md): Forward, supported arities of a partially unsupported signature bind (Accepted): a C# consumer calling a Kotlin function or constructor with a trailing run of defaulted parameters gets one omitting overload per suffix length that is fully bridgeable, even when a longer arity of the same declaration is not. Closes GitHub #229 / ROADMAP Phase 4 line 48, the mapping half ADR-064's 2026-09-09 amendment left open. Standing cell `hubWithEvents(settings: Settings = Settings(), events: Flow<Int>? = null)`: `HubWithEvents()` and `HubWithEvents(Settings)` bind; the events arity stays absent and still warns `SKIPPED_UNSUPPORTED_INPUT` naming `events`. Same rule on every ADR-096 function route plus sealed base/arm copies of the Planned gate; constructors already behaved this way (ADR-091) and gain a pin. Numbering keeps the holes (unsuffixed declared skip unused, first surviving synthesized `_2`). Does not admit `Flow` at an input (#131). No new handle kind, no new `LiveHandleTests` row: `HubWithEvents()` mints the same `Hub` handle `Hub()` already does, the one-arg form borrows `Settings` the same way `Hub(settings, ...)` does; existing Row 8b stays. Verify green: IntegrationTests 1867/1867, LeakTests 44/44.
- [150](150-kdoc-to-csharp-xml-docs.md): Forward, KDoc on an exported Kotlin declaration becomes an XML doc comment on its generated C# declaration (Accepted): `<summary>` from the first paragraph, `<param>` from `@param` (every parameter of the C# member or none, since a partial set is CS1573), `<returns>` from `@return` on a non-void member, `<exception cref>` from `@throws` through `CirErrorRenderer`'s `BuildMapped` table (unmapped types cref `KotlinException`), `@suppress` opts out; everything else is dropped in v1. One parser (`forward/ForwardKdoc.kt`), one `CirDoc` value on the CIR filled from `ForwardPublicSignature.doc` so an ADR-096 omitting overload drops the omitted `<param>`s, one renderer beside ADR-064's `renderRemarks`. Verified by spike on KSP 2.3.10: `docString` strips only the comment fence and ` * ` prefixes, tags stay inline, a `SYNTHETIC` member inherits its owner's KDoc (so only `Origin.KOTLIN` is read), the `actual` is null and the `expect` carries the text, a dependency jar carries nothing. Verified by spike under `GeneratedBindingsCheck`'s csproj properties: CS1570/1571/1572/1573/1574 are all errors, an empty `<param>` tag is warning-free. `expect` member docs need a new `ExpectIndex.docOrNull` (the index holds top-level declarations only). No handle, no leak row. **Amended 2026-09-20**: the v1 deferred scope now ships. Later paragraphs become `<para>` children of one `<remarks>` (ADR-064's own remark last, byte-identical when it is the only content); a fenced code block becomes a `<code>` element instead of being dropped; a backtick span becomes `<c>`; a `[Link]` becomes a real `<see cref>` only when `CirFile.resolveDocLinks()` finds it names exactly one non-generic type the file itself declares, `<c>` with the author's spelling otherwise (a member, a parameter, and a generic type always fall back); `@property` documents the C# property and the same-named primary-constructor parameter as a fallback; `@constructor` documents the primary constructor, with class-level `@param` as its parameters; `@see` becomes `<seealso cref>` when it resolves, else a closing "See also:" paragraph. Also corrects two ADR-150 sentences that claimed an `actual`'s own KDoc is always null: it is read when the `actual` itself carries one (`PlatformApiMingw.kt`'s `beaconLabel`/`nuzzle` rows), and only a bare `actual` falls back to the `expect`'s text.
- [151](151-bytearray-mapping.md): Forward, `kotlin.ByteArray` maps to `byte[]` over a materialized `StableRef` handle, the `Collection` wire with a `memcpy` (Accepted): `ByteArray`/`ByteArray?` now surface as `byte[]`/`byte[]?` at property, primary-constructor parameter, method parameter, method return and top-level return, taking the same one-`POINTER`-slot, `MATERIALIZED`-ownership row `Collection` already uses, with the per-element loop replaced by three new runtime exports (`nuget_bytes_create`/`nuget_bytes_count`/`nuget_bytes_copy`) and a `NugetMarshal.ReadBytes`/`CreateBytes` pair; a `byte[]` is a fresh copy on every crossing, never a view, and an empty array round-trips as empty, never `null` (the `addressOf(0)`-on-empty guard in `nuget_bytes_copy` is load-bearing, verified by konanc spike). Also: an unmapped `kotlin.*`/`kotlinx.*` stdlib type now skips `SKIPPED_UNSUPPORTED_TYPE` naming the type, instead of `SKIPPED_UNEXPORTED_DEPENDENCY_TYPE` (the fall-through no longer sets the unexported-dependency flag for a stdlib qualified name). Fixture `test-library/.../cat/Payload.kt`; the `Issue112Sample` unbridgeable stand-in moves from `ByteArray?` to `Sequence<Int>` since `ByteArray?` is bridgeable now (ADR-113 gains an "as shipped" note). Three new `LeakTests/LiveHandleTests.cs` rows (3d, 4c, 4d): `ByteArrayParameter_ReturnsToBaseline`, `ByteArrayReturn_MaterializedResult_ReturnsToBaseline`, `EmptyByteArrayReturn_ReturnsToBaseline`. Deferred: `UByteArray`/`IntArray`/`Array<T>`, `ByteArray` as an extension receiver, and the reverse direction. Amended 2026-09-20: a `ByteArray` now also binds as a `List`/`Map` component (return, parameter, property, constructor parameter, nested, nullable) and at the bare `suspend`/`Flow` shapes (`Task<byte[]>`, `KotlinFlow<byte[]>`), reusing the existing list/map wire with no new runtime export; `Set<ByteArray>` and a `ByteArray` map key stay a declined, not deferred, skip (`ForwardPlanSkipReason.BYTE_ARRAY`, identity-versus-copy). Fixed three pre-existing defects along the way: a `Flow<ByteArray>` processor crash, a lying bare `Task<ByteArray>` suspend return (both dated in [ADR-123](123-collection-elements-on-the-flow-routes.md)), and the general legacy-generic-return-route gate that inspected only the return and left a refusing parameter open (dated in [ADR-122](122-handle-parameters-on-the-legacy-routes.md)).
- [152](152-task-to-suspend-fun.md): Reverse, a C# `Task`/`Task<T>` method binds as a Kotlin `suspend fun` over a begin/end thunk pair (Accepted): the mirror of ADR-019, with ADR-041's registered-thunk call mechanism and ADR-104's error channel. `Begin` starts the `Task` and attaches one continuation that calls back with the task's `GCHandle`; `End` is `buildThunkMethod`'s ordinary return half unchanged, calling `GetAwaiter().GetResult()`, so a faulted task rethrows the original exception (not `AggregateException`) as a catchable `NugetManagedException`, and every already-supported return kind works with no new marshalling. A trailing `Async` is stripped unless the declaring type has a method whose C# name equals the stripped name (`Read` + `ReadAsync` → `read()` + `readAsync()`). `nuget-runtime` gains one public function, `awaitForKotlin` (`NugetAwait.kt`), reached through an `internal expect`/`actual` pair the same way ADR-130 wired the error envelope. Fixed alongside: `ResolveByteArray` now broadcasts a single-byte `NullableAttribute` across a multi-node type tree instead of refusing it (an ADR-072 Decision 7 gap), and a member referencing an interface from a different bound C# namespace now imports its `nuget{Name}Value` resolver, verified for the parameter position (`Kennel.BoardAsync(IFeedable)`); the return position stays unverified. Deferred, each a named skip: `ValueTask`/`ValueTask<T>`, async on a struct method, a bound interface member, or a generic class, and `Task` at a parameter/property/constructor/type-argument position (`IAsyncEnumerable<T>` was deferred here too, since shipped as its own mapping, [ADR-156](156-iasyncenumerable-to-flow.md)). Fixture `TestDependency/Kennel.cs`. Three new `LeakTests/LiveHandleTests.cs` rows (9, 9a, 9b): `ReverseAsync_AwaitedCall_ReturnsToBaseline`, `ReverseAsync_AlreadyCompletedTask_TightLoop_ReturnsToBaseline`, `ReverseAsync_FaultedTask_ReturnsToBaseline`. Verify green: IntegrationTests 1985/1985, LeakTests 55/55.
- [153](153-reverse-cancellation-token.md): Reverse, cancelling the awaiting coroutine cancels a bridge-owned `CancellationToken`, and a cancelled C# task surfaces as `CancellationException` (Accepted): the mirror of ADR-022 on top of ADR-152. A `CancellationToken` parameter on an async method elides when it's the only one, at any position, default or not; `Begin` mints a `CancellationTokenSource`, passes `cts.Token`, and returns its `GCHandle` (instead of `void`), which `awaitForKotlin` stores and releases exactly once, cancelling it from `invokeOnCancellation` or disposing it from a `finally`. Two new `nuget_runtime_register` slots (5 → 7, new contract hash): `ReleaseCancellation(handle, cancel)` and `managedErrorKind(err)`, the latter an `is OperationCanceledException` test (not a name match, so a user subclass and `TaskCanceledException` both map) that `nugetThrowManagedError` uses to throw stdlib `CancellationException(message, cause = NugetManagedException)` instead of the plain exception, at every managed-throw site (sync and async). `FooAsync()` beside `FooAsync(CancellationToken)` folds to one `suspend fun foo()`, keeping the token overload (`info_cancellation_overload_folded`). A sync token-taking method, two or more tokens, `CancellationToken?`, or a token on a constructor/property stays a named skip, `info_cancellation_token_not_yet_mapped`. A C# cancellation the Kotlin coroutine never asked for still throws `CancellationException`, ending that coroutine silently unless caught, the known kotlinx footgun; documented, not fixed. Fixture `TestDependency/Kennel.cs`. Three new `LeakTests/LiveHandleTests.cs` rows (9j-9l): `ReverseAsync_CancelledTokenCall_ReturnsToBaseline`, `ReverseAsync_CompletedTokenCall_ReturnsToBaseline`, `ReverseAsync_AlreadyCompletedTokenCall_TightLoop_ReturnsToBaseline`; they pin the `ctx`/`Task`-handle baseline only, since the CTS `GCHandle` is a .NET handle `nuget_live_handles` doesn't count (exactly-once CTS release is pinned by `AwaitForKotlinTest`'s counting fake instead). Verify green: IntegrationTests 1994/1994, LeakTests 58/58.
- [154](154-forward-dependency-type-admission.md): Forward, an additive `admit(...)` verb by package or qualified type name for dependency-type admission, warn-and-skip stays the default (Accepted): issue #247 asked whether `include(...)`'s two jobs (own-file roots, and the only route into ADR-066's reachability closure for a dependency type) should split, and what the un-admitted default should be. `admit(vararg types: String)` adds entries to the closure's admission predicate only, by the same matcher `exclude` uses (`isUnderPackage`, a qualified type name or a package prefix); it never picks roots and never replaces `include`/`rootPackage` (the #55/#60 decision stays untouched), and an empty `admit` list admits nothing (a guard load-bearing against admitting the whole classpath when `include`/`rootPackage` are also unset). Admitting a type admits only that declaration, not a package walk: a sibling type or member typed with something else still skips named, with a hint that reads `add admit("<qualified type>")` (never `include(...)`, since #55's replacement trap doesn't apply to an additive verb); a nested refused type is admitted through its *owner* (package segments plus the outermost type), while `exclude` keeps the full nested name. Default stays unchanged (warning, named skip, `<remarks>` per #276); opt-in `strictDependencyTypes = true` escalates the two refusals an author can repair by name (`NOT_INCLUDED`, `CROSS_MODULE_ADMISSION_DISABLED`) to `ERROR_UNEXPORTED_DEPENDENCY_TYPE`, leaving `EXCLUDED_BY_CONFIG` (already a deliberate omission) and `EXPECT_IN_DEPENDENCY` (unrepairable by any scope entry) as warnings. Ships with the value-class gate ROADMAP line 38 asked for: a closure-refused top-level dependency value class is now a named `SKIPPED_UNEXPORTED_DEPENDENCY_TYPE` skip instead of an undeclared `global::...` spelling with no diagnostic, with an explicit `kotlin.Result` carve-out (ADR-108). Amends [ADR-063](063-forward-declaration-level-export-scoping.md) (cross-reference only, mechanism untouched) and [ADR-066](066-forward-export-reachability-closure.md) (the "escape hatch is `include(...)`" sentence is superseded by `admit(...)`; both dependency hints are now `admit`-shaped). Documented gap: [ADR-109](109-duplicate-type-hazard.md)'s cross-publisher duplicate detection matches by package only, so a type admitted by name in another publisher is invisible to it. Rejected: letting `include` also match a qualified name (keeps the two jobs fused); admitting any reachable closed `enum class` regardless of scope; failing the build by default. Fixture `test-models/.../dev/other/bytype/Waterbowl.kt` + `dev/other/bykind/Tuft.kt`, reached from `test-library/.../test/admission/Storeroom.kt`; one new `LeakTests/LiveHandleTests.cs` row, `AdmittedDependencyClass_ReturnedHandle_ReturnsToBaseline`. Verify green: processor 1113, IntegrationTests 2108, LeakTests 74, plugin 557.
- [155](155-csharp-collections-in-kotlin.md): Reverse, a BCL collection (`IReadOnlyList<T>`, `List<T>`, `IReadOnlySet<T>`/`HashSet<T>`, `IReadOnlyDictionary<K,V>`/`Dictionary<K,V>` and the rest of the twelve mapped definitions) binds as a Kotlin `List<T>`/`Set<T>`/`Map<K,V>`, over one pointer to a `[count][8-byte slot]...` buffer, eager copy (Accepted): matched by `TypeReference` namespace+name only (resolution scope differs per definition). Every position renders the **read-only** Kotlin type, decided 2026-09-21 against the ROADMAP line's `MutableMap` wording and against forward ADR-011's mutability mirror, since the value is a copy and a mutable Kotlin type would let `roster.tags().add(x)` compile and change nothing in C#. `IEnumerable<T>` binds as an eager `List<T>` (`ToArray()` before crossing; an infinite or huge source is a documented constraint, not a bug). The shim casts the buffer-built container to the C#-declared definition so an `IList<T>`/`List<T>` overload pair dispatches to the right thunk instead of colliding (`CS0121` uncast); a C# overload set that collapses to one Kotlin signature purely from a collection parameter (`AddRange(IEnumerable<T>)`/`AddRange(List<T>)`) is dropped as a whole with a new `skipped_overload_set`, never all but one (ADR-072 Decision 5). New sealed `RirCollectionType` (no `mutable` flag), zero new runtime registration slots, no `REVERSE_ABI_TAG` bump. Element vocabulary: primitive/`Char`/`String` (nullable)/bound enum (ordinal)/bound class or interface handle (nullable, fresh `GCHandle` per slot); map keys never nullable. Deferred, each a named skip: arrays (`skipped_array`), `Task<collection>` (`info_async_not_yet_mapped`), struct/nested/`Nullable<T>`/generic-instance/type-parameter/`object` elements (new `skipped_collection_element`), a collection at a struct-member or Kotlin-implemented-interface-slot position (`skipped_kotlin_bridge`/`skipped_collection_position`), immutable/concurrent/`Queue`/non-generic collections (existing `skipped_unbound_generic_instantiation`, hint text corrected). Fixture `TestDependency/Roster.cs`; leak row `LeakTests/CollectabilityTests.cs` `CollectionHandleElements_AfterTheKotlinWrappersClose_AreCollected` (reverse handles are uncounted by `NugetMarshal.LiveHandles`, so no `LiveHandleTests` row). `describe()` renders `collection<args>` without the C# `definition`, so `IList<int>` and `List<int>` share one `contractHash`; accepted, both halves regenerate together. Verify green: `scripts/verify.sh --plugin`, IntegrationTests 2122/0, LeakTests 75/0, plugin 577/0.
- [156](156-iasyncenumerable-to-flow.md): Reverse, a bound C# `IAsyncEnumerable<T>` method binds as a Kotlin `Flow<T>` that pulls `MoveNextAsync` over ADR-152's begin/end pair (Accepted): pull, not the literal ADR-026 push mirror, since the reverse bridge's completion machinery is exactly-once and cannot call back into Kotlin repeatedly (`NugetAwait.kt:23-47`). Per method two registration slots, `Enumerate` (calls the method, `GetAsyncEnumerator(cts.Token)`, mints one enumeration `GCHandle`) and `Current` (the ordinary sync return half); three shared runtime slots, `MoveNextBegin`/`MoveNextEnd`/`DisposeEnumeration` (`nuget_runtime_register` 7 → 10). `collect` runs the C# method (cold, per-collect); collector cancellation stops the C# enumeration promptly only when the source marks its token `[EnumeratorCancellation]`, otherwise at the next element, and no element is delivered after cancellation; a mid-stream throw surfaces as `NugetManagedException`; disposal is fire-and-forget, so a throwing C# `finally` is dropped, sequenced C#-side as cancel-CTS, await-pending, `DisposeAsync` (a direct dispose during a pending step throws `NotSupportedException`, verified by spike). Element vocabulary is ADR-152's `Task<T>` vocabulary. The plugin now adds `kotlinx-coroutines-core` as `api` on the consumer's `nativeMain` unconditionally (`COROUTINES_VERSION` in `nuget-plugin/build.gradle.kts`), so a generated `nativeMain` declaration may now name `kotlinx.coroutines.flow.Flow`, and nothing else from kotlinx, amending [ADR-130](130-reverse-error-envelope-on-runtime.md)'s Variant 1 finding that a `nativeMain` file cannot reference a declaration reachable only through a per-target `api`. Deferred, each a named skip: `IAsyncEnumerable<T>` at a parameter, property, constructor, or type-argument position; `IAsyncEnumerable<T>?`; a nullable *value* element (`int?`, `Kennel.NullableTicks()`); struct, bound-interface, and generic-class owners. Fixture `TestDependency/Kennel.cs`. Three new `LeakTests/LiveHandleTests.cs` rows: `ReverseAsyncEnumerable_FullCollect_ReturnsToBaseline`, `ReverseAsyncEnumerable_CancelledMidStep_ReturnsToBaseline`, `ReverseAsyncEnumerable_ThrowsMidStream_ReturnsToBaseline`; they pin the per-step `ctx` `StableRef` baseline only, since the enumeration and CTS `GCHandle`s are uncounted by `nuget_live_handles` (ROADMAP's reverse-handle tracking item).
- [157](157-enum-armed-sealed-interface.md): Forward, an enum arm of a sealed interface binds as a boxed, handle-backed arm (Accepted): GitHub issue #236, the deferred alternative 2 of [ADR-125](125-sealed-interface-sibling-arms.md). A `sealed interface` whose arms are `enum class`es (alone or mixed with `data class` arms) was refused by one branch (a C# `enum` admits only an integral base, `CS1008`), which cascaded to `SKIPPED_SEALED_POSITION` on every member typed with it. Each enum arm now renders `public sealed class {Enum}Arm : Base`, a `StableRef` to the enum entry, with a public constructor from the C# enum, a `Value` getter, and managed `Equals`/`GetHashCode`/`ToString` over `Value`; the discriminator, `FromHandle`, and every ADR-105 position are the existing sealed route, unchanged. The C# `enum` is still declared exactly once and keeps its ADR-006 extension methods (`arm.Value.Patch()`); no implicit `Enum`-to-`Base` conversion in v1 (it would mint an undisposable handle). A declared type already named `{Enum}Arm` refuses the interface by name instead of colliding. The box constructor and `Value` getter are ADR-062/ADR-111-style plan entries (`ForwardCallableOrigin.ENUM_ARM_BOX`, `ForwardPropertyReceiver.EnumArm`), not hand-written exports. A hierarchy ineligible yesterday and shipped as `IMarking` now ships as `abstract class Marking`, a generated-API break, same precedent as ADR-112/ADR-125. Fixed alongside, a pre-existing ADR-006 defect: an enum member typed as another enum used to lower to `IntPtr` on both halves (`public static IntPtr Patch(this Swirl swirl)`); it now lowers to the ordinal and returns the C# enum. Fixture `test-library/.../issue236/EnumArmedSealedSample.kt`; two new `LeakTests/LiveHandleTests.cs` rows (1f, 1g), modelled on Row 1c. Deferred: an arm extending another class, enum properties projected onto the box, `Value` caching, implicit conversion, nullable `Marking?` inputs. `docs/backlog/fromhandle-no-enum-branch.md`'s `Flow<Marking>` hole is untouched.
- [158](158-reverse-delegate-parameters.md): Reverse, a C# delegate parameter (`Func<>`/`Action<>`/`Predicate<T>`/`Comparison<T>`/`Converter<,>`/a package-declared `delegate`) of a method or constructor on an ordinary bound class binds as a Kotlin function type, called with an ordinary Kotlin lambda (Accepted): the mechanism is the ADR-085/086/087/089 Kotlin-implemented-interface bridge with one slot, not the ADR-036 forward machinery inverted. Fixes a pre-existing mis-binding: a package-declared delegate is a sealed class over `System.MulticastDelegate` in metadata, and the reader used to extract it as an ordinary `RirClass` with an unusable `Invoke`-only handle, silently dropping its real constructor and adding two `BeginInvoke`/`EndInvoke` noise diagnostics; a return-position delegate bound by accident through that mis-classification and now becomes a named `skipped_delegate_position`. A new `RirDelegateType` carries every shape; a custom delegate also emits a Kotlin `typealias` carrying its C# name. Wire: one transfer `GCHandle` per crossing, a per-shape C# holder (`KotlinRefHandle` ctx + one `delegate* unmanaged[Cdecl]` slot) and factory registered on the declaring type, a matching Kotlin `staticCFunction` slot reused by lambda identity (ADR-089); Invoke arity up to 4 (a delegate-only policy lift, verified by spike; the interface bridge's own ceiling stays 2). Lifetime: the C# delegate owns the Kotlin lambda, released GC-timed once C# drops it, the one rule safe for both a per-call and a stored delegate; this subsumes the ROADMAP Phase 13 stored-delegate line. Errors ride the existing ADR-087/104 four-hop channel unchanged. An overload set differing only by delegate shape (the `Task.Run` shape) binds every member with a new `info_delegate_overload_ambiguity` note, since a bare Kotlin lambda is always an overload-resolution ambiguity against such a pair (verified by spike, Kotlin 2.4.10) while a typed function value or anonymous function resolves. Also: every ctx `StableRef` this mechanism mints (a delegate's, and the pre-existing ADR-085 interface bridge's) is now minted/released through a counted `NugetHandles` pair over the ADR-130 expect/actual seam, so `nuget_live_handles` sees a Kotlin object living inside a C# bridge for the first time; amends [ADR-085](085-kotlin-implemented-csharp-interfaces.md) and [ADR-120](120-live-stableref-counter-and-leak-harness.md). Fixture `TestDependency/Workshop.cs`; three new `LeakTests/LiveHandleTests.cs` rows (13-15: `ReverseDelegate_PerCallCapturingLambda_ReturnsToBaseline`, `ReverseDelegate_StoredThenOwnerDisposed_ReturnsToBaseline`, `ReverseDelegate_ThrowingLambda_ReturnsToBaseline`). Deferred: async delegates (folded into the ROADMAP Phase 13 `Task`-typed-slot line), a delegate at a return/property/field position, a struct/bound-interface/generic-class delegate member, arity above 4, generic/nested custom delegates.
- [159](159-async-member-on-kotlin-subclass.md): Forward, one coroutine scope per instance, owned by the first async level in a Kotlin inheritance chain (Accepted): before this ADR the scope emission (`_scopeHandle`, `GetOrCreateScope()`, `IAsyncDisposable`, `DisposeAsync`) fired only on a base-less class while the flag driving `Dispose()`'s cleanup came off a raw `getAllFunctions()` scan, so an async member anywhere but the root of a chain generated non-compiling C# (`CS0103`/`CS0108`/`CS0122`/`CS0535`), and the one shape that did compile (a base's `Flow` method, no async member on the subclass) dropped the scope cleanup from the subclass's `override Dispose()` and leaked it. `ForwardScopeOwnership.kt`'s `forwardScopeOwner` is now the single selector both the C# renderer and the Kotlin export builder read: the root-most class in the kept chain that *projects* a scope-using member (every ADR-114/119/123/147/opt-in refusal applied) owns the scope; a class whose only async member was refused owns nothing; an `override suspend fun` over a kept base's member is not re-projected (Kotlin's own dynamic dispatch reaches it through the base's export); an abstract owner declares `public abstract ValueTask DisposeAsync();` and each concrete class below carries the body as `override`; `GetOrCreateScope()` widens to `internal`. Also here: the ADR-034 `ERROR_CSHARP_SIGNATURE_COLLISION` guard now runs over the async and Flow-route methods (previously `CS0111` in the generated file), and `using System.Threading` is unconditional, fixing a pre-existing `CS0103: Interlocked` break in a coroutine-free module. Fixture `test-library/.../test/lounge/NapLounge.kt`; `LeakTests/LiveHandleTests.cs` rows 13/13a/13b.
- [160](160-callback-parameter-on-the-forward-plan.md): Forward, a per-call callback parameter is projected off the ADR-062 plan (Accepted): a member taking a per-call lambda and also returning its own value (`fun countTicks(listener: (Int) -> Unit): Int`) failed `packNuget` with a forward ABI mismatch, since the hand-written route always declared the extern's return as `IntPtr`; a scalar lambda return and a non-lambda parameter beside the lambda were broken the same way. `BridgeType.Callback(parameters, result)` fans out to two native slots (`${name}Ptr`/`${name}UserData`, the ADR-080 `HasValue` fan-out precedent), and both halves project from that one classification, so the method's own return reuses the plan's whole result matrix (scalar, `String`, an exported object, its nullable twin, an enum) and mixed parameters, top-level, extension and sealed-arm positions bind. The admitted payload set is a primitive by value, `String`, an exported object or interface, or an enum; the admitted lambda-result set is `Unit`, a primitive by value, or `String`. `translateCallbackMethod`/`addLambdaParamMethodExport` retire for every planned position; a member left on the legacy route that still cannot be marshaled is refused by name on both halves (`legacyRefusedCallbackMember`) instead of a build failure. Ownership is ADR-036's, unchanged. Still refused by name: `Char` on either axis, an object or enum lambda result, a lambda whose payload or return is nullable, a `suspend` lambda, a lambda nested in a collection, a callback at a result position, and a callback parameter on a constructor, a data class's `copy()`, an enum-arm box constructor, or a value-class member. A lambda parameter whose own type is nullable is not refused: it falls through silently (`CALLBACK_PROTOCOL`) to the legacy route, which binds it and guards it with `ArgumentNullException.ThrowIfNull` (ADR-036's 2026-09-22 amendment). `LeakTests` rows 13/13a measure the one cell that mints a handle (an exported-object outer return) at 5000 iterations.
- [161](161-csharp-callback-exception-into-kotlin.md): Forward, a C# callback exception reaches Kotlin as a catchable `NugetManagedException`, a materialisation fault ends the stream instead of the process, and a late invocation is a key-table miss (Accepted): supersedes ADR-102's fail-fast exception discipline for the four user-code thunk families (per-call lambda, stored callback, ADR-039 listener bridge, ADR-084 bridge slot). Part A wraps the `Flow`/`suspend` completion closures so a materialisation throw faults the stream/`Task` and cancels the collector instead of crashing; no ABI change (the runtime callback thunks stay 4/3/1/2-ary with no `errOut`). Part B adds a trailing `IntPtr* errOut` to every user-code thunk, a new 71st runtime export `nuget_managed_error_create` (additive, no `NugetRuntimeAbi1` bump), and a Kotlin `nugetCallbackCall` helper that checks the slot before the return value and throws `NugetManagedException` (or `CancellationException` for a cancelled C# callback, mirroring ADR-153); a shared `[ThreadStatic]` stash on `NugetErrorNative` rethrows the *original* C# exception, uncaught, on every route (not only per-call, a generalisation past the research memo's scoped recommendation), whenever the escaping Kotlin error is exactly the managed-exception type. This is a deliberate deviation from ADR-104's "should not diverge" note: the slot carries a Kotlin-owned holder pushed by C# through one export, not a `GCHandle` read through registered accessors, since a forward-only library has no registration step to hang accessors off. Part C replaces the `GCHandle` ctx for stored callbacks and bridge slots with a never-reused key table (`GCHandle` slot reuse under free-then-alloc is deterministic, not merely possible), so a late invocation is a lookup miss: dropped for a `void` listener, reported as `ObjectDisposedException` for a value-returning one. Fixture `test-library/.../test/cat/CallbackFaults.kt`; `IntegrationTests/CallbackFaultTests.cs`; `LeakTests/LiveHandleTests.cs` rows 14/14a/14b/14c. Deferred: folding the reverse template's own `internal NugetManagedException` onto the runtime type over the ADR-130 expect/actual seam; `@NugetRuntimeApi` on `NugetManagedException` itself, unlike every other public runtime export, left for a human decision; a per-failed-`Flow`-item handle leak; a dropped late call's handle-passed argument leak; a re-crossed cancellation losing its original .NET type.
- [162](162-per-declaration-error-containment.md): Forward, generator-only, a `guarded(...)` boundary contains a planner/renderer/emitter invariant per declaration instead of aborting the whole KSP round at the first occurrence (Accepted): a new `ERROR_INTERNAL_GENERATOR_FAILURE` kind, installed at three families of existing per-declaration loops (plan time, via a new `ForwardPlanSkipReason.INTERNAL_FAILURE` that leaves a `Skipped` catalog entry rather than omitting one — omitting one crashes the Kotlin emitter before the fatal gate and masks every later failure, verified by spike; the Kotlin half's `generateCNameWrappers` loops; the C# half's per-declaration `translate` loops), plus a whole-`process()` catch as the last resort for work no declaration owns. Fatal everywhere by human decision, correcting the ROADMAP line's own "continues, as the `SKIPPED_*` diagnostics do" wording: only a plan-time skip is structurally safe against `ForwardAbiContract.assertMatchesPlan`'s cross-half requirement, and two policies for one kind is worse than one. Closes ADR-100's own deferred "`ERROR_*` visibility" question, verified by spike: `logger.error` is visible on a real console, located, for every failure of a round, so the aggregate-throw alternative is dropped. Folded in, same path: `fun dispose()` on a class, sealed base, or sealed arm now reports `ERROR_CSHARP_SIGNATURE_COLLISION` (CS0111 wording) instead of the generic `ERROR_C_ENTRY_POINT_COLLISION` two phases later, via a new `reservedSignatures` parameter on `emitCsharpSignatureCollisions`; `enumParamsUnsupported` (`CirFunctionTranslator`) gets its own kind, `ERROR_UNSUPPORTED_ENUM_PARAMETER_ROUTE`, the last fatal forward diagnostic that lived outside `ForwardDiagnosticKind`; `NugetDiagnostics.json` and `ForwardDiagnosticRecord` gain additive `file`/`line` fields so `NugetReportDiagnosticsTask` composes a leading `<path>:<line>: ` on its console re-emit, while `ForwardDiagnostic.format()` itself is deliberately not reordered (KSP's own Gradle logger already prefixes that location on the `e:`/`w:` path). Amends [ADR-064](064-forward-unsupported-declaration-diagnostics.md) (two new kinds), [ADR-100](100-forward-diagnostic-delivery.md) (closes its `ERROR_*`-visibility deferral; additive JSON fields), [ADR-117](117-forward-abi-collision-names-owning-declarations.md) (`fun dispose()` no longer reaches its entry-point-collision route). No fixture in `test-library/` (an `ERROR_*`-triggering declaration there would break `packNuget`); every new test is Tier 1 in-process or a JVM unit test of the guard itself, since no legal Kotlin shape shipped in this repository reaches a raw `error(...)` today. No new handle kind, no `LiveHandleTests` row.
- [163](163-export-symbol-package-qualification.md): Forward, every `@CName` export symbol is qualified by library and declaring package, always (Accepted): `<sanitised libraryName>_<package relative to rootPackage>__<the name it had>`, minted once in a new `ForwardSymbolTable` (`NugetContext.symbols`) every planner, CIR translator and legacy `exports/` builder asks, replacing the zero-argument `nativePrefix()` that let three families (raw sealed prefixes, bare top-level names, the non-class extension-receiver fallback) mint an unqualified symbol. Closes two same-simple-name declarations in different packages colliding on one C entry point (`a.Kitten`/`b.Kitten`, two `rollCall()`, two `LoadState` sealed classes, an enum extension property all verified by spike to collide before this ADR) and closes a top-level function whose bare name matched a mingwX64 import-library export (`fun signal(dbm: Int)`, silently dropped from the DLL's export table by `ld.lld`'s MinGW auto-exporter, `EntryPointNotFoundException` with no build diagnostic; a non-empty leading segment is verified by spike never to be such a name). A qualified top-level call is now spelled fully qualified in the generated Kotlin rather than imported by simple name (an ambiguity in the one generated file otherwise; the default package keeps its import, having nothing to qualify with), `{Iface}BridgeState` takes the same qualification in C# identifier form (CS0101 became reachable the moment entry points stopped colliding), and the legacy top-level routes now derive the public C# name and `_native` extern stem from the declaration name rather than the entry point. A sanitised library name of `nuget` is a new `ERROR_RESERVED_LIBRARY_NAME` (ADR-127's reserved space). Always mangled, never only on collision (a forward symbol has no cross-release contract, ADR-095). Rides along in the same file: ROADMAP line 76, a top-level function's generic return type rendering its outer type unqualified across packages (CS0246); issue #111 had already qualified the type arguments there. `ERROR_C_ENTRY_POINT_COLLISION` stays the backstop for a collision inside one package and owner, reworded hint. Amends [ADR-095](095-static-route-overloads.md) (the "qualifying export prefixes is its own item" note is superseded), [ADR-117](117-forward-abi-collision-names-owning-declarations.md) (worked example and hint text), [ADR-133](133-nested-types.md) ("never its package" is no longer true). Verified by execution on the packed mingwX64 DLL: `objdump -p` shows `test_signal` and no bare `signal`/`read`/`abort`/`exit`; 2682 `test_`-prefixed exports; every `@CName`/`EntryPoint` under `test_` or `nuget_`. No new handle kind, no `LiveHandleTests` row.
- [164](164-optional-default-parameters.md): Forward, every Kotlin defaulted parameter binds as one widened C# signature instead of a trailing-omitting overload set (Accepted): a non-nullable defaulted type widens to its nullable C# form with `null` meaning unset, an already-nullable one widens to a new `Optional<T>` struct, and the Kotlin side dispatches a `when (mask)` over the presence bits so any subset can be set by name in one call. A default before a required parameter stays required-but-nullable; a type with no non-null routing at all (Flow, sealed, bound interface) is dropped from the trailing run and Kotlin always evaluates it; a lambda a constructor stores past the call is dropped the same way, since ADR-160 frees its per-call handle before a later invocation could run it. `Copy` gets the same widened shape, so a partial copy works. Caps at 8 widened parameters per callable, `WARNING_DEFAULT_PARAMETER_CAP_EXCEEDED` naming the rest. Retires ADR-096's interface-route exclusion, since the interface route now widens the same as every other route. Supersedes [ADR-091](091-constructor-default-parameters.md) and [ADR-096](096-function-default-parameters.md); their trailing-omitting overload machinery is removed.
- [165](165-publish-nuget-task.md): Publish, `publishNuget` pushes the packed `.nupkg` from Gradle over plain HTTP with no .NET SDK (Accepted): named `repositories { nuget("name") { url; apiKey } }` in `nuget { publish { } }`, tasks `publishNugetTo<Name>Repository` plus aggregate `publishNuget`, `<name>ApiKey`/`<name>Username`/`<name>Password` Gradle-property lookup mirroring `maven-publish`, `--dryRun`, `--skipDuplicate` (409 as warning), one `java.net.http` multipart `PUT` to the feed's `PackagePublish/2.0.0` `@id` with `X-NuGet-ApiKey` and `X-NuGet-Protocol-Version: 4.1.0`. Rejected wrapping `dotnet nuget push` (reintroduces the SDK prerequisite), docs-only, and a `localFeed`-only task.
- [166](166-const-value-from-the-evaluated-constant.md): Forward, a `const val`'s C# value is the compiler's evaluated constant, read reflectively off KSP2's internal analysis symbol, never source text (Accepted): fixes a one-line `object`/companion body and a `;`-separated pair, which used to emit illegal C#, and silently wrong values for underscores inside a string, a template, `\$`, a shift, `Int.MIN_VALUE`, and an expression over another const. A dependency companion const (no `containingFile`) now binds instead of being silently dropped. No source-text fallback: an unreadable value (a future KSP bump) is a named `SKIPPED_UNREADABLE_CONST_VALUE`, guarded by a Tier 1 probe cell. Supersedes the klib-fallback claim in ADR-110's Amendment.
- [167](167-interface-super-interfaces.md): Forward, `interface Derived : Base` renders C# interface inheritance (`IPet : INamed, IAged, IDisposable`) instead of flattening (Accepted): `CirInterface.superInterfaces`, a direct-exported-supers-only base list (`keepsSupertype`, type arguments spelled, an unexported super dropped with `SKIPPED_UNEXPORTED_SUPERTYPE`); `IDerived` declares its own members only, decided **lexically** (not KSP's `parentDeclaration`, which mis-owns a substituted generic override); an identical-signature override is omitted (else CS0108 under warnings-as-errors); a diamond (two kept supers declaring the same member) is redeclared `new`; a covariant override is a named skip (`SKIPPED_UNSUPPORTED_COMBINATION`), reached through the super at the super's type; an unexported super's members re-home onto the derived interface (the ADR-101 mirror). Fixes two pre-existing shipped build breaks: the ADR-084 bridge read inherited members off a base-list-less `IFoo` (CS1061), and a class implementing a generic interface dropped its type argument (CS0305, `KibbleJar : IHolder<int>`). Also fixes a KSP2 `SYNTHETIC`-member false exclusion (a two-super merged member had no bridge slot) and a false-positive `SKIPPED_UNSUPPORTED_TYPE ... T` warning for a member ADR-113's type-parameter carve-out already restores. Bridge selection now reads the parameter's **declared** interface (`NugetMarshal.HandleOf<T>` passes `typeof(T)` to `NugetBridge.HandleFor(object impl, Type declared)`), not runtime `is` order; see ADR-084's amendment. Fixture `test-library/.../test/lineage/{Lineage,Moggy}.kt`, unexported super `.../hidden/Pedigree.kt`. `LeakTests/LiveHandleTests.cs` rows 6n/6o.
- [168](168-interface-var-explicit-setter.md): Forward, an interface `var` widened over a read-only base class gets an explicit setter implementation (Accepted): `ITally` now renders `hasSetter = plan.setter != null` (closing ADR-113's own deferral), so a `var` interface property is `{ get; set; }` for every implementer; the one shape that would otherwise regress, a class overriding both an exported base class's `open val` and an exported interface's `var` with one `override var` (CS0546 refuses the public setter), keeps a get-only public `override` and gains an explicit `ITally.Count { get; set; }` beside it, spelled with the same helper the base list uses. A refused setter with no interface to attach to (`Throwable?`, ADR-107) stays get-only with the ordinary named skip; a sealed arm gets no explicit member (its base list never names its interfaces) and now runs the same read-only-base guard, rendering a get-only override instead of CS0546. Breaking: a C# class implementing a `var`-bearing `IFoo` without a setter no longer compiles.
- [169](169-handle-constructor-out-marker.md): Forward, every generated class's internal handle constructor becomes `internal X(IntPtr handle, out NugetHandleTag tag)` (Accepted): `Interop.cs` compiles into the consumer's own assembly, so the always-emitted `internal X(IntPtr handle)` was a real overload candidate a bare numeric or nullable-numeric literal could reach, loudly (`CS0121` for a nullable-numeric constructor parameter, ROADMAP line 26) or silently (`new Circle(5)` wrapping stable-ref address `0x5` for a `double`/`long`/unsigned/generic-typed parameter, or `new Flag(5)` wrongly compiling for a `bool`/`char` parameter). An `out` argument has no implicit conversion, so no ordinary call can bind it; a by-value marker and `[OverloadResolutionPriority(-1)]` were both spiked and rejected (a new two-argument hole, and a `net8.0`/LangVersion-12 `CS9202` respectively). Deletes ADR-164's `Settings(int)` delegating-constructor workaround as superseded, and fully closes the older, broader "missing constructor overload can silently resolve to the handle constructor" ROADMAP item ADR-148 had only narrowed.

## By mapping

The ADRs that decided each mapping on
[Supported features](../topics/supported-features.md), by section of that page. The page
itself carries no ADR links; when a new ADR changes a mapping, add it to the mapping's line
here. A line lists every ADR that shaped the mapping, oldest first, so the newest number is
the current decision.

### Primitives

- `Char` → `char`: [062](062-forward-callable-plan.md), [098](098-narrow-primitive-and-char-collection-components.md)
- `T?` (nullable primitive) → `T?`: [002](002-nullable-two-call-pattern.md), [053](053-nullable-reference-types-in-kotlin.md), [061](061-method-return-marshalling.md), [069](069-nullable-boolean-marshalling.md), [098](098-narrow-primitive-and-char-collection-components.md)
- `String?` ⇄ `string?`: [002](002-nullable-two-call-pattern.md), [053](053-nullable-reference-types-in-kotlin.md), [062](062-forward-callable-plan.md)
- exported class type, nullable (`Foo?`), as a **parameter** → `Foo?`: [002](002-nullable-two-call-pattern.md), [062](062-forward-callable-plan.md)
- `kotlin.time.Instant` / `Instant?` → `System.DateTimeOffset` / `DateTimeOffset?`: [076](076-instant-mapping.md)
- `kotlin.uuid.Uuid` / `Uuid?` → `System.Guid` / `Guid?`: [106](106-uuid-mapping.md)
- `kotlin.time.Duration` / `Duration?` → `System.TimeSpan` / `TimeSpan?`: [103](103-duration-mapping.md)
- `ByteArray` / `ByteArray?` → `byte[]` / `byte[]?`: [151](151-bytearray-mapping.md)

### OOP constructs

- `class` ⇄ `class`: [005](005-object-return-semantics.md), [051](051-csharp-objects-as-opaque-handles.md), [053](053-nullable-reference-types-in-kotlin.md), [064](064-forward-unsupported-declaration-diagnostics.md), [066](066-forward-export-reachability-closure.md), [094](094-reflection-free-generic-dispatch.md), [101](101-unexported-supertype-skip.md), [133](133-nested-types.md), [134](134-nested-types-under-deferred-owners.md), [141](141-inner-class-outer-instance-constructor.md), [162](162-per-declaration-error-containment.md)
- dependency-module type (cross-module export) → same class, generated into this module's package: [066](066-forward-export-reachability-closure.md), [109](109-duplicate-type-hazard.md), [154](154-forward-dependency-type-admission.md)
- constructor ⇄ `new Foo(...)`: [052](052-csharp-instance-constructors-in-kotlin.md), [057](057-csharp-overload-sets-in-kotlin.md), [064](064-forward-unsupported-declaration-diagnostics.md), [091](091-constructor-default-parameters.md), [164](164-optional-default-parameters.md), [169](169-handle-constructor-out-marker.md)
- `data class` → `class`: [008](008-data-class-mapping.md)
- `interface` ⇄ `interface`: [040](040-interface-return-type-mapping.md), [070](070-csharp-interfaces-in-kotlin.md), [084](084-csharp-implemented-interfaces.md), [085](085-kotlin-implemented-csharp-interfaces.md), [086](086-object-interface-slots-kotlin-bridge.md), [087](087-kotlin-slot-exceptions.md), [088](088-kotlin-implemented-interfaces-at-forward-positions.md), [089](089-bridge-reuse-per-kotlin-object.md), [113](113-interface-declaration-on-the-forward-plan.md), [133](133-nested-types.md), [134](134-nested-types-under-deferred-owners.md), [135](135-interface-parameter-reachability.md), [167](167-interface-super-interfaces.md), [168](168-interface-var-explicit-setter.md)
- `abstract class` → `abstract class`: [075](075-collection-property-getter-setter-independence.md), [101](101-unexported-supertype-skip.md)
- `sealed class` → `abstract class`: [009](009-sealed-class-mapping.md), [062](062-forward-callable-plan.md), [064](064-forward-unsupported-declaration-diagnostics.md), [105](105-sealed-property-position.md), [111](111-sealed-subclass-properties-on-the-property-plan.md), [112](112-sealed-interface-mapping.md), [116](116-sealed-subclass-methods-on-the-callable-plan.md), [118](118-suspend-route-sealed-arm-owners-and-overload-numbering.md), [124](124-flow-route-sealed-arm-owners.md), [125](125-sealed-interface-sibling-arms.md), [134](134-nested-types-under-deferred-owners.md), [148](148-sealed-subclass-constructors.md), [157](157-enum-armed-sealed-interface.md)
- `object` ⇄ `static class`: [007](007-top-level-function-class-naming.md), [048](048-kotlin-stub-generation-from-reverse-ir.md), [049](049-csharp-registration-shim-generation.md), [060](060-adversarial-forward-fixture.md), [075](075-collection-property-getter-setter-independence.md), [101](101-unexported-supertype-skip.md), [110](110-top-level-function-pascal-case.md), [133](133-nested-types.md)
- `data object` (in sealed) → sealed subclass: [009](009-sealed-class-mapping.md), [111](111-sealed-subclass-properties-on-the-property-plan.md), [116](116-sealed-subclass-methods-on-the-callable-plan.md)
- `enum class` ⇄ `enum`: [006](006-enum-mapping.md), [064](064-forward-unsupported-declaration-diagnostics.md), [066](066-forward-export-reachability-closure.md), [080](080-bare-nullable-enum.md), [097](097-enum-collection-components.md), [105](105-sealed-property-position.md), [133](133-nested-types.md), [157](157-enum-armed-sealed-interface.md)
- `data class` ← `struct` (`readonly record struct`, etc.): [014](014-value-class-mapping.md), [056](056-csharp-structs-in-kotlin.md), [057](057-csharp-overload-sets-in-kotlin.md), [058](058-csharp-shape-b-structs-in-kotlin.md), [059](059-nested-struct-components-in-kotlin.md)
- `annotation class` ⇸: [064](064-forward-unsupported-declaration-diagnostics.md)
- `kotlin.sequences.Sequence<T>` ⇸: [064](064-forward-unsupported-declaration-diagnostics.md), [075](075-collection-property-getter-setter-independence.md)
- declaration marked with an author's `@RequiresOptIn` marker ⇸: [115](115-opt-in-marker-declarations.md)
- `expect`/`actual class`/`interface`/`enum class`/`value class`, `fun`, `val`, `object` → ordinary declaration: [074](074-expect-actual-declarations.md)

### Properties and top-level declarations

- member property (get/set) ⇄ property (get/set): [051](051-csharp-objects-as-opaque-handles.md), [053](053-nullable-reference-types-in-kotlin.md), [075](075-collection-property-getter-setter-independence.md), [101](101-unexported-supertype-skip.md)
- instance method return (object, `T?`, `List`/`Map`/`Set`, enum, `Char`, `String?`, `Int?`, …) → matching C# return type: [061](061-method-return-marshalling.md), [062](062-forward-callable-plan.md)
- top-level function → `static class` method, `PascalCase`: [007](007-top-level-function-class-naming.md), [062](062-forward-callable-plan.md), [064](064-forward-unsupported-declaration-diagnostics.md), [110](110-top-level-function-pascal-case.md)
- extension function → static method: [061](061-method-return-marshalling.md), [062](062-forward-callable-plan.md), [077](077-value-classes-at-ordinary-positions.md), [105](105-sealed-property-position.md), [126](126-extension-class-per-declaring-package.md), [132](132-extension-receiver-shapes.md), [133](133-nested-types.md)
- extension property → static accessor: [013](013-extension-property-mapping.md), [075](075-collection-property-getter-setter-independence.md), [077](077-value-classes-at-ordinary-positions.md), [088](088-kotlin-implemented-interfaces-at-forward-positions.md), [105](105-sealed-property-position.md), [126](126-extension-class-per-declaring-package.md), [132](132-extension-receiver-shapes.md), [133](133-nested-types.md), [135](135-interface-parameter-reachability.md)
- `const val` → `const`: [166](166-const-value-from-the-evaluated-constant.md)

### Generics

- `class<T>` ⇄ `class<T>`: [010](010-generics-mapping.md), [072](072-closed-constructed-generics-in-kotlin.md), [083](083-nullable-collection-components.md), [101](101-unexported-supertype-skip.md)
- `class<T>(...)` constructor ⇄ typed constructors: [147](147-generic-class-methods.md)
- a method declared on a `class<T>` → instance method on the generic carrier: [147](147-generic-class-methods.md)
- `<T : Bound>` constraint → `where T : ...`: [015](015-generic-type-constraint-mapping.md)
- `out T` / `in T` variance → `out T` / `in T`: [016](016-generic-variance-mapping.md)
- `typealias` → C# alias / underlying: [018](018-type-alias-mapping.md)

### Collections

- `List<T>` → `IReadOnlyList<T>`: [011](011-collection-type-mapping.md), [061](061-method-return-marshalling.md), [062](062-forward-callable-plan.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md), [120](120-live-stableref-counter-and-leak-harness.md)
- `MutableList<T>` → `IList<T>`: [011](011-collection-type-mapping.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md)
- `Map<K,V>` → `IReadOnlyDictionary<K,V>`: [011](011-collection-type-mapping.md), [062](062-forward-callable-plan.md), [073](073-map-and-set-parameters.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md)
- `MutableMap<K,V>` → `IDictionary<K,V>`: [011](011-collection-type-mapping.md), [062](062-forward-callable-plan.md), [073](073-map-and-set-parameters.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md)
- `Set<T>` → `IReadOnlySet<T>`: [011](011-collection-type-mapping.md), [062](062-forward-callable-plan.md), [073](073-map-and-set-parameters.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md)
- `MutableSet<T>` → `ISet<T>`: [011](011-collection-type-mapping.md), [062](062-forward-callable-plan.md), [073](073-map-and-set-parameters.md), [075](075-collection-property-getter-setter-independence.md), [081](081-value-class-collection-components.md), [083](083-nullable-collection-components.md), [097](097-enum-collection-components.md), [098](098-narrow-primitive-and-char-collection-components.md), [099](099-nested-collection-components.md)
- `T?` (nullable collection reference) → `T?`: [011](011-collection-type-mapping.md), [061](061-method-return-marshalling.md), [075](075-collection-property-getter-setter-independence.md)
- `List<T>` / `Set<T>` / `Map<K,V>` (always read-only, never `Mutable*`) ← `IEnumerable<T>`, `IReadOnlyCollection<T>`, `IReadOnlyList<T>`, `ICollection<T>`, `IList<T>`, `List<T>` (→ `List<T>`); `IReadOnlySet<T>`, `ISet<T>`, `HashSet<T>` (→ `Set<T>`); `IReadOnlyDictionary<K,V>`, `IDictionary<K,V>`, `Dictionary<K,V>` (→ `Map<K,V>`): [155](155-csharp-collections-in-kotlin.md)

### Functions and lambdas

- `(T) -> R` (Kotlin → C#) → `Func<>` / `Action<>`: [012](012-lambda-function-type-mapping.md), [066](066-forward-export-reachability-closure.md)
- `(T) -> R` parameter (C# → Kotlin) → `Func<>` / `Action<>`: [036](036-reverse-interop-mechanism.md), [102](102-aot-safe-forward-callbacks.md), [116](116-sealed-subclass-methods-on-the-callable-plan.md), [160](160-callback-parameter-on-the-forward-plan.md), [161](161-csharp-callback-exception-into-kotlin.md)
- stored callback parameter → `IDisposable` subscription: [037](037-stored-callbacks.md), [102](102-aot-safe-forward-callbacks.md), [161](161-csharp-callback-exception-into-kotlin.md)
- Kotlin lambda at a bound delegate parameter ← `Func<>` / `Action<>` / `Predicate<T>` / `Comparison<T>` / `Converter<,>` / package-declared `delegate` parameter: [085](085-kotlin-implemented-csharp-interfaces.md), [089](089-bridge-reuse-per-kotlin-object.md), [158](158-reverse-delegate-parameters.md)
- interface parameter (C# → Kotlin) → C# implements `I`-prefixed type: [039](039-interface-bridging.md), [102](102-aot-safe-forward-callbacks.md), [161](161-csharp-callback-exception-into-kotlin.md)
- `inline fun` → regular method: [017](017-inline-function-mapping.md)
- value class (`value`/`inline class`) → underlying type / record struct: [014](014-value-class-mapping.md), [035](035-value-class-primary-constructor-validation.md), [062](062-forward-callable-plan.md), [134](134-nested-types-under-deferred-owners.md)
- value class at an ordinary position → the same record struct: [014](014-value-class-mapping.md), [066](066-forward-export-reachability-closure.md), [077](077-value-classes-at-ordinary-positions.md), [079](079-nullable-primitive-enum-underlying-value-classes.md), [082](082-value-class-inherited-members.md), [105](105-sealed-property-position.md)
- value class as a `List`/`Map`/`Set` component → the same record struct, in `IReadOnlyList<T>`/`IReadOnlyDictionary<K,V>`/`IReadOnlySet<T>` etc.: [073](073-map-and-set-parameters.md), [077](077-value-classes-at-ordinary-positions.md), [081](081-value-class-collection-components.md)
- same-name function overloads ⇄ method overload set: [057](057-csharp-overload-sets-in-kotlin.md), [090](090-ordinary-class-method-overloads.md), [095](095-static-route-overloads.md), [133](133-nested-types.md)
- function/method default parameters → one widened signature: [091](091-constructor-default-parameters.md), [096](096-function-default-parameters.md), [149](149-supported-arities-of-a-partially-unsupported-signature.md), [164](164-optional-default-parameters.md)

### Exception handling

- thrown exception ⇄ `KotlinException`: [023](023-exception-propagation.md), [024](024-sync-exception-propagation.md), [049](049-csharp-registration-shim-generation.md), [104](104-reverse-thunk-error-channel.md)
- stack trace → `KotlinStackTrace` property: [027](027-stacktrace-propagation.md)
- `e.cause` → `InnerException`: [028](028-exception-cause-chain.md)
- `IllegalArgumentException` etc. → `ArgumentException` etc.: [029](029-exception-type-mapping.md)
- `Throwable` / `Throwable?` property → `System.Exception?`: [107](107-throwable-property-mapping.md)
- `Result<T>` return → `T`, failure thrown: [108](108-result-return-mapping.md)
- property getter / setter throws → propagated: [030](030-property-exception-propagation.md)
- constructor / `init` throws → propagated: [031](031-constructor-exception-propagation.md), [032](032-generic-constructor-exception-propagation.md), [033](033-value-class-constructor-exception-propagation.md), [034](034-secondary-constructor-exceptions.md), [035](035-value-class-primary-constructor-validation.md)

### Async and coroutines

- `suspend fun` → `async` / `Task<T>`: [019](019-suspend-function-mapping.md), [102](102-aot-safe-forward-callbacks.md), [118](118-suspend-route-sealed-arm-owners-and-overload-numbering.md), [131](131-suspend-route-sealed-base-return.md)
- `suspend fun` returning `T?` → `Task<T?>`: [019](019-suspend-function-mapping.md), [067](067-nullable-stateflow-mapping.md)
- `suspend fun` returning an interface type, top-level or nested ⇄ `Task<IFoo>`: [040](040-interface-return-type-mapping.md), [133](133-nested-types.md), [136](136-csharp-identity-on-async-interface-reads.md)
- `suspend () -> R` lambda → `KotlinSuspendFunc<R>` / `Task<R>`: [020](020-suspend-lambda-mapping.md), [102](102-aot-safe-forward-callbacks.md)
- structured concurrency → honoured: [021](021-structured-concurrency.md)
- coroutine cancellation → `CancellationToken`: [022](022-cancellation-token-support.md)
- in-flight async drain → `IAsyncDisposable`: [025](025-async-disposable.md), [159](159-async-member-on-kotlin-subclass.md)
- `Flow<T>` → `IAsyncEnumerable<T>`: [026](026-flow-mapping.md), [064](064-forward-unsupported-declaration-diagnostics.md), [102](102-aot-safe-forward-callbacks.md), [161](161-csharp-callback-exception-into-kotlin.md)
- `Flow<T?>` (nullable element) → `KotlinFlow<T?>`: [065](065-stateflow-mapping.md)
- `Flow<T>` of an interface element type (top-level or nested interface, class-method `Flow` positions only) ⇄ `KotlinFlow<IFoo>`: [040](040-interface-return-type-mapping.md), [133](133-nested-types.md), [136](136-csharp-identity-on-async-interface-reads.md)
- `StateFlow<T>` / `MutableStateFlow<T>` → `KotlinStateFlow<T>` / `KotlinMutableStateFlow<T>` (`.Value` + `IAsyncEnumerable<T>`): [065](065-stateflow-mapping.md), [071](071-mutable-stateflow-mapping.md)
- `StateFlow<T?>` / `StateFlow<T>?` → `KotlinStateFlow<T?>` / `KotlinStateFlow<T>?`: [067](067-nullable-stateflow-mapping.md)
- `suspend fun` returning `StateFlow<T>` → `Task<KotlinStateFlow<T>>`: [068](068-suspend-returning-stateflow.md)
- `List`/`Set`/`Map` parameter on a `Flow`/`StateFlow`-returning or `suspend` member → the collection type, never `IntPtr`: [114](114-collection-parameters-on-legacy-flow-and-suspend-routes.md)
- `List`/`Set`/`Map` return on a `suspend` member → `Task<IReadOnlyList<T>>` / `Task<IReadOnlySet<T>>` / `Task<IReadOnlyDictionary<K, V>>`: [119](119-collection-returns-on-the-legacy-suspend-route.md)
- A class, `object`, sealed base, or sealed subclass parameter on a `Flow`/`StateFlow`-returning or `suspend` member → the mapped C# type, spelled exactly as the return position on the same member, passed as `x._handle`: [122](122-handle-parameters-on-the-legacy-routes.md)
- A nullable primitive/`Char`/`String` parameter on a `Flow`/`StateFlow`-returning or `suspend` member → `int?`/`char?`/`bool?`/`string?`: [122](122-handle-parameters-on-the-legacy-routes.md)
- `List`/`Set`/`Map` element on a `Flow`/`StateFlow` property or method return → `KotlinFlow<IReadOnlyList<T>>` / `KotlinStateFlow<IReadOnlyList<T>>` / `IReadOnlySet<T>` / `IReadOnlyDictionary<K, V>`: [123](123-collection-elements-on-the-flow-routes.md)
- `suspend fun` returning `Unit`/`T` ← `Task`/`Task<T>`, instance or static: [152](152-task-to-suspend-fun.md), [153](153-reverse-cancellation-token.md)
- `IAsyncEnumerable<T>` method return, instance or static ← `Flow<T>`: [130](130-reverse-error-envelope-on-runtime.md), [156](156-iasyncenumerable-to-flow.md)

### Documentation comments

- KDoc (`/** ... */`, `@param`, `@return`, `@throws`, `@property`, `@constructor`, `@see`, `@suppress`) → `///` XML doc comment (`<summary>`, `<remarks>`, `<param>`, `<returns>`, `<exception cref>`, `<seealso cref>`): [064](064-forward-unsupported-declaration-diagnostics.md), [150](150-kdoc-to-csharp-xml-docs.md)

### Runtime and packaging

- the fixed `nuget_*` ABI (`NugetHandles`, `NugetError`, the scalar wrap/unwrap, collection, callback and coroutine exports, the .NET ticks conversions, `NugetCSharpBridge`, `nuget_runtime_version`) → the same `DllImport`s in `Interop.cs`, plus one more: [127](127-nuget-runtime-library.md), [129](129-nuget-runtime-version-export.md)
- every forward `@CName` export symbol → the matching `DllImport` `EntryPoint`: [163](163-export-symbol-package-qualification.md)
